---
phase: 10-prijsvergelijking-gevoeligheid
plan: 03
type: execute
wave: 3
depends_on: ["10-02"]
files_modified:
  - src/features/price-comparison/DiaPackageManager.tsx
  - src/features/price-comparison/PriceComparisonPage.tsx
  - src/features/price-comparison/ComparisonTable.tsx
autonomous: false
requirements:
  - PRIJS-06
  - PRIJS-08
  - GEVOEL-01
  - GEVOEL-02
  - GEVOEL-03
  - MODE-02

must_haves:
  truths:
    - "Accountmanager kan DIA-pakketten bekijken en prijzen aanpassen"
    - "Volledige vergelijkingspagina werkt end-to-end met alle Phase 10 features"
    - "Intern/extern toggle verbergt sales-signalen en gevoeligheid correct"
  artifacts:
    - path: "src/features/price-comparison/DiaPackageManager.tsx"
      provides: "DIA package configuration UI"
      contains: "DiaPackageManager"
  key_links:
    - from: "src/features/price-comparison/DiaPackageManager.tsx"
      to: "src/data/dia-packages.ts"
      via: "reads and allows editing DIA_PACKAGES"
      pattern: "DIA_PACKAGES"
---

<objective>
Build DIA package management UI and perform end-to-end visual verification of the complete Phase 10 feature set: dynamic comparison table, sales signals, hybrid scenario, sensitivity analysis, mode toggle, and period toggle.

Purpose: Final integration and human verification that all Phase 10 features work correctly together.
Output: DiaPackageManager component + verified working comparison page.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/10-prijsvergelijking-gevoeligheid/10-CONTEXT.md
@.planning/phases/10-prijsvergelijking-gevoeligheid/10-01-SUMMARY.md
@.planning/phases/10-prijsvergelijking-gevoeligheid/10-02-SUMMARY.md

@src/features/price-comparison/PriceComparisonPage.tsx
@src/features/price-comparison/ComparisonTable.tsx
@src/features/price-comparison/store.ts
@src/data/dia-packages.ts
@src/models/dia-packages.ts

<interfaces>
From src/models/dia-packages.ts:
```typescript
export interface DiaPackage {
  id: string;
  name: string;
  includedModuleIds: string[];
  pricePerStudent: number;
  minModules: number;
}
```

From src/data/dia-packages.ts:
```typescript
export const DIA_PACKAGES: DiaPackage[];
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: DIA Package Manager component and final integration</name>
  <files>src/features/price-comparison/DiaPackageManager.tsx, src/features/price-comparison/PriceComparisonPage.tsx</files>
  <read_first>
    - src/data/dia-packages.ts (DIA_PACKAGES data structure)
    - src/models/dia-packages.ts (DiaPackage interface)
    - src/features/price-comparison/PriceComparisonPage.tsx (where to add DiaPackageManager)
    - src/features/price-comparison/store.ts (diaPackageResult, recalculate)
    - src/features/school-profile/schemas/ (existing form patterns with react-hook-form + Zod)
  </read_first>
  <action>
    Per D-02: DIA packages are configurable by the accountmanager.

    **A. Create DiaPackageManager.tsx:**

    A collapsible section (like SensitivitySection pattern) titled "DIA-pakketconfiguratie". Only visible in internal mode.

    Content:
    1. Read DIA_PACKAGES from data file. Show as a list of cards.
    2. Each card shows: package name, included modules (as chips/tags), pricePerStudent (editable input).
    3. Price input: react-hook-form pattern is NOT needed here (no Zod schema, just a simple controlled input). On blur, update the DIA_PACKAGES price in local state and trigger store recalculate.
    4. Note: DIA_PACKAGES is a JS constant -- for runtime editing, use a Zustand slice or local useState that overrides the imported constant. Store the overrides in the price comparison store as `diaPackageOverrides: Record<string, number>` (packageId -> overridden pricePerStudent). The recalculate function reads these overrides.
    5. Show current package status: "Actief pakket: {name}" or "Geen pakket van toepassing (minder dan 3 modules)" based on diaPackageResult.

    Style: Use Tailwind. Cards with border-neutral-200, rounded-lg, p-4. Editable price input styled like PriceOverrideRow in ModuleDetailPanel (w-[120px] h-10 border text-right).

    **B. Update store.ts:**

    Add to state:
    ```typescript
    diaPackageOverrides: Record<string, number>; // packageId -> overridden pricePerStudent
    setDiaPackageOverride: (packageId: string, price: number) => void;
    ```

    In recalculate(), when building DIA packages for calculateDiaModuleCosts, merge diaPackageOverrides into the packages array (override pricePerStudent for matching packageId).

    **C. Add DiaPackageManager to PriceComparisonPage.tsx:**

    Add after the ComparisonTable section, before SensitivitySection, only in internal mode:
    ```tsx
    {isInternalMode && (
      <div className="mb-8">
        <DiaPackageManager />
      </div>
    )}
    ```

    **D. Final build check:**
    Run `npm run build` and `npx vitest run` to verify everything compiles and tests pass.
  </action>
  <verify>
    <automated>npm run build && npx vitest run -x</automated>
  </verify>
  <acceptance_criteria>
    - src/features/price-comparison/DiaPackageManager.tsx contains `export function DiaPackageManager`
    - src/features/price-comparison/DiaPackageManager.tsx contains `DIA-pakketconfiguratie`
    - src/features/price-comparison/DiaPackageManager.tsx contains `DIA_PACKAGES`
    - src/features/price-comparison/store.ts contains `diaPackageOverrides`
    - src/features/price-comparison/store.ts contains `setDiaPackageOverride`
    - src/features/price-comparison/PriceComparisonPage.tsx contains `DiaPackageManager`
    - src/features/price-comparison/PriceComparisonPage.tsx contains `isInternalMode`
    - `npm run build` exits 0
    - `npx vitest run` exits 0
  </acceptance_criteria>
  <done>DIA package manager allows editing package prices with reactive recalculation. Component integrated into PriceComparisonPage in internal mode only.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of complete Phase 10 feature set</name>
  <files>none</files>
  <action>
    Human verifies all Phase 10 features visually in the browser. No code changes -- pure verification checkpoint.
  </action>
  <what-built>
    Complete Phase 10 feature set:
    1. Dynamic comparison table with conditional JIJ column and 'Na overstap' column
    2. DIA package pricing (automatic at 3+ modules, [Pakketprijs] badge)
    3. Hybrid scenario with per-module savings
    4. Sales signal badges (green/yellow/red) in internal mode
    5. Intern/Extern mode toggle (hides sales signals + sensitivity in external mode)
    6. Per jaar / 3-jarig contract toggle with placeholder warning
    7. Sensitivity analysis (0%, 10%, 20% discount scenarios + break-even)
    8. DIA package configuration UI (editable prices)
    9. Break-even per module in detail panel
  </what-built>
  <how-to-verify>
    Start dev server: `npm run dev`

    **Test 1 - Basic comparison (PRIJS-01, PRIJS-02):**
    1. Open a school profile with 3+ modules selected (rekenwiskunde, nederlands, engels)
    2. Navigate to Vergelijking tab
    3. Verify: Table shows per-module costs and totals for Cito and DIA
    4. Verify: Bar chart shows grouped bars per module

    **Test 2 - DIA package pricing (PRIJS-06, D-01, D-03):**
    5. With 3 DIA modules: verify DIA column shows [Pakketprijs] badge
    6. Expand a module detail: verify it shows "DIA (Pakketprijs -- LVS Basispakket)" with package price formula
    7. Deselect a module to go below 3: verify DIA reverts to individual pricing

    **Test 3 - Conditional JIJ column (D-04, D-05):**
    8. On a school WITHOUT JIJ usage: verify only Cito and DIA columns visible
    9. On a school WITH JIJ as current provider for a module: verify JIJ column appears
    10. If JIJ has no prices: verify input placeholders shown

    **Test 4 - Hybrid scenario (PRIJS-08, D-06, D-08, D-09):**
    11. On a school using DIA for some modules: verify 'Na overstap' column appears
    12. Verify savings row shows euro amounts and percentages

    **Test 5 - Mode toggle (D-19, D-20, MODE-02):**
    13. Click "Extern": verify sales signal badges disappear, sensitivity section disappears, DIA config disappears
    14. Click "Intern": verify they reappear

    **Test 6 - Sales signals (D-17, D-18):**
    15. In internal mode: verify colored badges next to module names
    16. Hover badge: verify Dutch tooltip text

    **Test 7 - Sensitivity analysis (GEVOEL-01, GEVOEL-02, GEVOEL-03, D-12, D-13):**
    17. In internal mode: verify sensitivity section is visible under the table
    18. Expand it: verify scenario table with 3 rows (current, 10%, 20%)
    19. Verify break-even text: "DIA wordt goedkoper bij X% korting"

    **Test 8 - Period toggle (D-10, D-11):**
    20. Switch to "3-jarig contract": verify warning banner about placeholder prices
    21. Verify amounts update (Cito x2.85, competitors x3)

    **Test 9 - Price overrides still work (PRIJS-04):**
    22. Expand a module, change a price in "Prijs aanpassen"
    23. Click "Herbereken vergelijking"
    24. Verify all calculations (including sensitivity, hybrid, sales signals) update reactively

    **Test 10 - Differentiators (PRIJS-05):**
    25. Expand a module: verify "Onderscheidend vermogen" section shows Cito and competitor differentiators
  </how-to-verify>
  <verify>Visual inspection by user following the 10 test scenarios above</verify>
  <done>User confirms all 10 test scenarios pass: comparison table, DIA packages, JIJ conditional, hybrid scenario, mode toggle, sales signals, sensitivity, period toggle, price overrides, and differentiators all work correctly.</done>
  <resume-signal>Type "approved" if all tests pass, or describe specific issues to fix</resume-signal>
</task>

</tasks>

<verification>
- `npm run build` -- clean build
- `npx vitest run` -- all tests pass
- Visual verification of all 10 test scenarios by user
</verification>

<success_criteria>
- DIA package manager allows configuring package prices (PRIJS-06 + D-02)
- All Phase 10 features work together end-to-end
- Mode toggle correctly shows/hides internal-only content (MODE-02)
- Hybrid scenario, sensitivity, and sales signals all function with real school data
- User approves visual verification
</success_criteria>

<output>
After completion, create `.planning/phases/10-prijsvergelijking-gevoeligheid/10-03-SUMMARY.md`
</output>

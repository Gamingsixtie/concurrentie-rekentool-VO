---
phase: 09-ai-intake-prijsbeheer
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - src/models/__tests__/price-deviation.test.ts
  - src/features/school-profile/schemas/__tests__/intake-extraction.test.ts
  - src/features/school-profile/__tests__/diff-view-logic.test.ts
  - src/features/school-profile/__tests__/intake-merge.test.ts
  - src/hooks/__tests__/useSchoolPrices.test.ts
  - api/__tests__/extract-document.test.ts
  - src/features/school-profile/__tests__/document-extraction.test.ts
  - src/lib/__tests__/ai-intake-v2.test.ts
autonomous: true
requirements:
  - INTAKE-01
  - INTAKE-02
  - INTAKE-03
  - INTAKE-04
  - INTAKE-05
  - PRIJSMGT-01
  - PRIJSMGT-02
  - PRIJSMGT-03
  - PRIJSMGT-04

must_haves:
  truths:
    - "All test stub files exist and run without errors (all tests skipped or pending)"
    - "Each stub file imports from the correct source path that will be created in subsequent plans"
    - "Running npx vitest run shows all stubs as skipped/pending, zero failures"
  artifacts:
    - path: "src/models/__tests__/price-deviation.test.ts"
      provides: "Test stubs for INTAKE-04 and PRIJSMGT-02 — price deviation and staleness"
    - path: "src/features/school-profile/schemas/__tests__/intake-extraction.test.ts"
      provides: "Test stubs for INTAKE-02 — v2 extraction schema validation"
    - path: "src/features/school-profile/__tests__/diff-view-logic.test.ts"
      provides: "Test stubs for INTAKE-03 — diff computation (new/existing/conflict)"
    - path: "src/features/school-profile/__tests__/intake-merge.test.ts"
      provides: "Test stubs for INTAKE-05 — append-only merge logic"
    - path: "src/hooks/__tests__/useSchoolPrices.test.ts"
      provides: "Test stubs for PRIJSMGT-01 — school price CRUD hooks"
    - path: "api/__tests__/extract-document.test.ts"
      provides: "Test stubs for PRIJSMGT-03 — document text extraction"
    - path: "src/features/school-profile/__tests__/document-extraction.test.ts"
      provides: "Test stubs for PRIJSMGT-04 — document extraction preview"
    - path: "src/lib/__tests__/ai-intake-v2.test.ts"
      provides: "Test stubs for INTAKE-01 — extended SSE streaming and v2 schema parsing"
  key_links: []
---

<objective>
Wave 0: Create all test stub files required by Phase 9 plans so that automated verification can use specific test files instead of relying solely on build checks.

Purpose: The Nyquist validation rule requires every task to have an automated verify command that tests behavioral correctness, not just build success. These stubs establish the test files that plans 01-04 will populate with real assertions as they implement features.
Output: 8 test stub files with describe blocks and skipped/pending test cases matching the behavioral requirements.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/09-ai-intake-prijsbeheer/09-CONTEXT.md
@.planning/phases/09-ai-intake-prijsbeheer/09-RESEARCH.md
@.planning/phases/09-ai-intake-prijsbeheer/09-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create test stubs for foundation layer (Plan 01 coverage)</name>
  <files>
    src/models/__tests__/price-deviation.test.ts,
    src/features/school-profile/schemas/__tests__/intake-extraction.test.ts
  </files>
  <action>
    1. Create `src/models/__tests__/price-deviation.test.ts`:
       - `import { describe, it, expect } from 'vitest';`
       - describe('checkPriceDeviation') with it.todo() for:
         - 'returns no deviation when amount matches publication price'
         - 'returns deviation when amount >50% above publication price'
         - 'returns no deviation and null publicationPrice when no pub price exists'
         - 'returns correct percentDiff for partial deviation under threshold'
       - describe('getSchoolPriceStatus') with it.todo() for:
         - 'returns stale when verifiedAt is >6 months ago'
         - 'returns manual when priceType is agreed and recently verified'
         - 'returns verified when priceType is publication and recently verified'
         - 'returns unknown when source is empty or verifiedAt is null'
       - Add a commented import line: `// import { checkPriceDeviation, getSchoolPriceStatus } from '@/models/pricing';`

    2. Create `src/features/school-profile/schemas/__tests__/intake-extraction.test.ts`:
       - `import { describe, it, expect } from 'vitest';`
       - describe('IntakeExtractionSchemaV2') with it.todo() for:
         - 'parses object with contactPersonen array'
         - 'parses actiePunten array'
         - 'parses pipelineSignaal enum values'
         - 'preserves backward compatibility with v1 fields'
         - 'handles optional fields gracefully (no contactPersonen, no pipelineSignaal)'
       - Add a commented import line: `// import { IntakeExtractionSchemaV2 } from '@/features/school-profile/schemas/intake-extraction.schema';`
  </action>
  <verify>
    <automated>npx vitest run src/models/__tests__/price-deviation.test.ts src/features/school-profile/schemas/__tests__/intake-extraction.test.ts --reporter=verbose 2>&1 | tail -15</automated>
  </verify>
  <done>Both test files exist, vitest recognizes them, all tests show as todo/skipped with zero failures</done>
</task>

<task type="auto">
  <name>Task 2: Create test stubs for UI logic, hooks, and streaming (Plans 02-04 coverage)</name>
  <files>
    src/features/school-profile/__tests__/diff-view-logic.test.ts,
    src/features/school-profile/__tests__/intake-merge.test.ts,
    src/hooks/__tests__/useSchoolPrices.test.ts,
    api/__tests__/extract-document.test.ts,
    src/features/school-profile/__tests__/document-extraction.test.ts,
    src/lib/__tests__/ai-intake-v2.test.ts
  </files>
  <action>
    1. Create `src/features/school-profile/__tests__/diff-view-logic.test.ts`:
       - describe('DiffView logic') with it.todo() for:
         - 'marks extracted module as new when not in existing school profile'
         - 'marks extracted module as existing when already in school profile'
         - 'marks contact as conflict when name matches but details differ'
         - 'defaults new items to checked and existing items to disabled'
         - 'allows user to edit extracted field values before saving (INTAKE-03 correction)'

    2. Create `src/features/school-profile/__tests__/intake-merge.test.ts`:
       - describe('intake merge (append-only)') with it.todo() for:
         - 'appends new modules without removing existing ones'
         - 'appends new contacts without overwriting existing contacts'
         - 'appends new actions without modifying existing actions'
         - 'preserves existing moduleSetups when merging new extraction'

    3. Create `src/hooks/__tests__/useSchoolPrices.test.ts`:
       - describe('useSchoolPrices hooks') with it.todo() for:
         - 'useSchoolPrices returns prices filtered by schoolId'
         - 'useCreateSchoolPrice inserts a new price entry'
         - 'useActivateSchoolPrice deactivates all then activates one'
         - 'useDeleteSchoolPrice removes a price entry'

    4. Create `api/__tests__/extract-document.test.ts`:
       - describe('extractTextFromFile') with it.todo() for:
         - 'extracts text from PDF buffer'
         - 'extracts CSV text from Excel buffer'
         - 'extracts text from Word docx buffer'
         - 'passes through plain text for CSV/TXT'
         - 'throws on unsupported file format'

    5. Create `src/features/school-profile/__tests__/document-extraction.test.ts`:
       - describe('DocumentExtractionPreview logic') with it.todo() for:
         - 'marks extracted price as new when no existing price for module/provider'
         - 'marks extracted price as conflict when existing price differs'
         - 'includes PriceDeviationWarning for prices with >50% deviation'
         - 'onConfirm returns only checked prices'

    6. Create `src/lib/__tests__/ai-intake-v2.test.ts`:
       - `import { describe, it, expect } from 'vitest';`
       - describe('streamIntakeFromNotes v2') with it.todo() for:
         - 'yields SSE text chunks from streaming response'
         - 'parseExtractionFromText strips markdown fences and parses JSON'
         - 'parseExtractionFromText validates against IntakeExtractionSchemaV2'
         - 'parseExtractionFromText throws descriptive Dutch error on invalid JSON'
       - Add a commented import line: `// import { parseExtractionFromText, streamIntakeFromNotes } from '@/lib/ai-intake';`

    All files use `import { describe, it, expect } from 'vitest';` and commented-out source imports.
  </action>
  <verify>
    <automated>npx vitest run src/features/school-profile/__tests__/ src/hooks/__tests__/useSchoolPrices.test.ts api/__tests__/extract-document.test.ts src/lib/__tests__/ai-intake-v2.test.ts --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <done>All 6 test stub files exist, vitest recognizes them, all tests show as todo/skipped with zero failures</done>
</task>

</tasks>

<verification>
- `npx vitest run --reporter=verbose` passes with all new stubs showing as todo/pending
- Each stub file is in the correct `__tests__/` directory adjacent to its source
- No import errors (all imports are commented out until source files exist)
</verification>

<success_criteria>
- 8 test stub files created across models, schemas, features, hooks, lib, and api directories
- All stubs use it.todo() pattern so vitest counts them but doesn't fail
- Running the full test suite passes without errors
- Subsequent plans can reference these specific test files in their verify blocks
</success_criteria>

<output>
After completion, create `.planning/phases/09-ai-intake-prijsbeheer/09-00-SUMMARY.md`
</output>

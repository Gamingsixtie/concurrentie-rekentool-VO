---
phase: 10.3-ux-overhaul-wizard-vergelijking
plan: 02
subsystem: ui
tags: [react, zustand, price-comparison, dynamic-columns, breakdown]

# Dependency graph
requires:
  - phase: 10.3-01
    provides: visibleProviders state slice in store.ts
provides:
  - Dynamic provider columns in ComparisonTable based on visibleProviders
  - Schoolgrootte-impact badges (JIJ tier, DIA package) in table headers
  - Price breakdown step rendering per provider in ModuleDetailPanel
  - Per-provider columnar differentiators layout
affects: [10.3-03, price-comparison-page]

# Tech tracking
tech-stack:
  added: []
  patterns: [visibleProviders-driven column rendering, breakdown step chain display]

key-files:
  created: []
  modified:
    - src/features/price-comparison/ComparisonTable.tsx
    - src/features/price-comparison/ModuleDetailPanel.tsx

key-decisions:
  - "visibleProviders from store replaces hard-coded PROVIDERS for dynamic column rendering"
  - "Prijsopbouw section replaces Berekeningsformule with step-by-step breakdown per provider"
  - "Differentiators reorganized to per-provider columnar layout matching table columns"
  - "Price override section respects visibleProviders (only shows overrides for visible providers)"

patterns-established:
  - "visibleProviders-driven rendering: all column-based UI reads from store.visibleProviders"
  - "Columnar grid layout: grid-template-columns repeat(N, 1fr) for provider-parallel sections"

requirements-completed: [PRIJS-01, PRIJS-03, PRIJS-05]

# Metrics
duration: 3min
completed: 2026-03-24
---

# Phase 10.3 Plan 02: Dynamic Provider Columns and Price Breakdown Summary

**ComparisonTable uses visibleProviders from store for dynamic columns with schoolgrootte-impact badges; ModuleDetailPanel shows step-by-step price breakdown per provider in columnar layout**

## Performance

- **Duration:** 3 min
- **Started:** 2026-03-24T20:36:13Z
- **Completed:** 2026-03-24T20:39:13Z
- **Tasks:** 2/2
- **Files modified:** 2

## Accomplishments

### Task 1: Refactor ComparisonTable for dynamic provider columns with schoolgrootte-impact badges
- Replaced `PROVIDERS` constant with `visibleProviders` from Zustand store for column derivation
- Added schoolgrootte-impact badges in column headers showing JIJ tier (Licentie N) or DIA package ID
- Column widths adapt dynamically via `Math.floor(70 / activeProviders.length)%`
- Removed unused `PROVIDERS` import (only `PROVIDER_LABELS` and `getTotalStudents` needed)
- **Commit:** `d361812`

### Task 2: Extend ModuleDetailPanel with price breakdown steps per provider
- Replaced old "Berekeningsformule" section with new "Prijsopbouw" columnar layout
- Each provider column shows `breakdown.map()` steps with label + amount, plus total
- DIA package savings annotation shown when applicable
- Reorganized "Onderscheidend vermogen" to per-provider columnar layout (Cito gets CheckmarkIcon, competitors get InfoCircleIcon)
- Price override section now only shows visible providers
- All Dutch labels: Prijsopbouw, Niet beschikbaar, Totaal, Onderscheidend vermogen, Geen differentiators, Niet aangeboden
- **Commit:** `572238b`

## Deviations from Plan

None - plan executed exactly as written. The store already contained visibleProviders (added by parallel Plan 01 agent).

## Known Stubs

None - all data flows are wired to real store/engine data.

## Verification

- `npm run build` passes (no new errors in modified files; pre-existing errors in unrelated files)
- ComparisonTable uses `usePriceComparisonStore(s => s.visibleProviders)` instead of `PROVIDERS`
- Column headers show schoolgrootte-impact badges via tierId/packageId from ProviderCost
- ModuleDetailPanel renders PriceBreakdownStep[] per provider with Dutch labels
- Differentiators displayed per provider column in grid layout

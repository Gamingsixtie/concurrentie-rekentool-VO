# Phase 10.2: Engine Refactoring — Provider-aware berekeningen - Context

**Gathered:** 2026-03-24
**Status:** Ready for planning
**Mode:** Infrastructure phase — discuss skipped (pure engine refactoring)

<domain>
## Phase Boundary

De rekenmotor begrijpt de werkelijke prijsmodellen per aanbieder en rekent correct op basis van schoolgrootte. Refactoring van calculateComparison() naar provider-specifieke calculators met prijsopbouw (breakdown) per module.

</domain>

<decisions>
## Implementation Decisions

### Claude's Discretion
All implementation choices are at Claude's discretion — pure infrastructure phase. Use ROADMAP phase goal, success criteria, and codebase conventions to guide decisions.

Key constraints from prior phases:
- [Phase 10.1] PricingStrategy discriminated union (flat/tiered-license/package-bundle/platform+module) defined in src/models/pricing.ts
- [Phase 10.1] Provider configs in src/data/providers/{cito,dia,jij,saqi}.ts with pricingStrategy and defaultPrices
- [Phase 10.1] PROVIDER_CONFIGS map in src/data/providers/index.ts
- [Phase 10] DIA package selection compares all qualifying packages by total cost and picks cheapest
- [Phase 10] Break-even returns null when Cito is already more expensive
- [Phase 9] useSchoolPrices for school-specific price overrides from Supabase
- Engine functions are pure TypeScript functions (src/engine/)
- Existing tests in src/engine/__tests__/price-comparison.test.ts

</decisions>

<code_context>
## Existing Code Insights

### Reusable Assets
- `src/engine/price-comparison.ts` — calculateComparison() current engine (to be refactored)
- `src/engine/types.ts` — CalculationInput, CalculationResult types
- `src/engine/current-vs-proposed.ts` — calculateCurrentVsProposed()
- `src/data/providers/` — New provider configs from Phase 10.1
- `src/data/dia-packages.ts` → re-exports from providers/dia.ts
- `src/data/jij-license-tiers.ts` → re-exports from providers/jij.ts with estimateJijCostPerStudent()
- `src/hooks/useSchoolPrices.ts` — School-specific price overrides

### Established Patterns
- Pure engine functions in src/engine/
- Zustand store for state management
- Provider keys: 'cito' | 'dia' | 'jij' | 'saqi'

### Integration Points
- `src/features/price-comparison/store.ts` — Zustand store consuming engine
- `src/features/price-comparison/ComparisonTable.tsx` — UI consuming results
- `src/hooks/useSchoolPrices.ts` — Supabase price overrides

</code_context>

<specifics>
## Specific Ideas

No specific requirements — infrastructure phase. Refer to ROADMAP phase description and success criteria.

</specifics>

<deferred>
## Deferred Ideas

None — discuss phase skipped.

</deferred>

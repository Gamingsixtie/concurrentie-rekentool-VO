---
phase: 10.2-engine-refactoring-provider-aware-berekeningen
verified: 2026-03-24T21:14:00Z
status: passed
score: 11/11 must-haves verified
re_verification: false
---

# Phase 10.2: Engine Refactoring — Provider-Aware Berekeningen Verification Report

**Phase Goal:** De rekenmotor begrijpt de werkelijke prijsmodellen per aanbieder en rekent correct op basis van schoolgrootte
**Verified:** 2026-03-24T21:14:00Z
**Status:** passed
**Re-verification:** No — initial verification

---

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | Each provider has a dedicated calculator implementing its actual pricing model | VERIFIED | `cito-calculator.ts`, `dia-calculator.ts`, `jij-calculator.ts`, `flat-calculator.ts` — all substantive, no stubs |
| 2 | `calculateComparison()` returns a breakdown per module per provider explaining HOW the price was computed | VERIFIED | `ProviderCost.breakdown: PriceBreakdownStep[]` field present and populated by calculators, confirmed by price-comparison tests |
| 3 | JIJ prices vary with school size (tier selection based on totalStudents * testsPerStudent) | VERIFIED | `jij-calculator.ts` lines 60–74: tier lookup by totalAdministrations, `tierId` set on result, 25 calculator tests including 100/800/2500 student cases pass |
| 4 | DIA package optimization is integrated inside the DIA calculator's `calculateAll()` | VERIFIED | `dia-calculator.ts` lines 111–115: calls `selectOptimalDiaPackage`; `isPackagePrice`/`packageId` set on covered modules |
| 5 | Override prices take precedence over calculator-computed prices | VERIFIED | All 4 calculators check override first; calculator tests cover override precedence |
| 6 | Store uses new `calculateComparison()` with options object — no pre-processing pipelines | VERIFIED | `store.ts` 213 lines; `applyDiaVolumeDiscount`, `applyDynamicJijPrices`, `applyDiaPackageToResult` are absent (grep confirms 0 matches) |
| 7 | DIA package result comes from `ComparisonResult.diaPackageResult` | VERIFIED | `store.ts` lines 145, 215: `diaPackageResult = annualResult.diaPackageResult ?? null` |
| 8 | `calculateComparisonLegacy` removed from `price-comparison.ts` | VERIFIED | grep 0 matches in `price-comparison.ts` |
| 9 | All engine tests pass with zero regressions | VERIFIED | 165 tests across 14 test files: all pass |
| 10 | School-specific overrides passed as `Map<string, number>` to engine | VERIFIED | `store.ts` `recalculate()` lines 198–201: builds `overridePrices` Map keyed by `moduleId:provider` |
| 11 | `applyContractPeriodToResult` stays as store post-processing | VERIFIED | `store.ts` line 142, 212: called after `calculateComparison` in both `initialize()` and `recalculate()` |

**Score:** 11/11 truths verified

---

### Required Artifacts

| Artifact | Status | Details |
|----------|--------|---------|
| `src/engine/calculators/types.ts` | VERIFIED | Exports `ProviderPriceCalculator`, `PriceBreakdownStep`, `ModulePriceResult` — 30 lines, substantive |
| `src/engine/calculators/cito-calculator.ts` | VERIFIED | Implements `ProviderPriceCalculator`, bundle logic present |
| `src/engine/calculators/dia-calculator.ts` | VERIFIED | Implements `ProviderPriceCalculator`, volume discount + package optimization, 202 lines |
| `src/engine/calculators/jij-calculator.ts` | VERIFIED | Implements `ProviderPriceCalculator`, tier selection, `tierId` on result, 121 lines |
| `src/engine/calculators/flat-calculator.ts` | VERIFIED | Implements `ProviderPriceCalculator` for SAQI flat pricing |
| `src/engine/calculators/index.ts` | VERIFIED | `createCalculator` factory, exhaustive switch on `pricingStrategy.type`, 39 lines |
| `src/engine/price-comparison.ts` | VERIFIED | Refactored `calculateComparison()` uses `createCalculator` per provider, 255 lines; `ComparisonResult.diaPackageResult` field present |
| `src/features/price-comparison/store.ts` | VERIFIED | 3-step pipeline, 213 lines (-47% from 405); removed functions confirmed absent |
| `src/engine/__tests__/calculators.test.ts` | VERIFIED | 25 tests, all pass |
| `src/engine/__tests__/calculator-parity.test.ts` | VERIFIED | Regression snapshot tests (30 snapshots for 4 school sizes), all pass |

---

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `calculators/index.ts` | `data/providers/index.ts` | `createCalculator` switches on `pricingStrategy.type` | VERIFIED | `switch (config.pricingStrategy.type)` at line 19 |
| `price-comparison.ts` | `calculators/index.ts` | `calculateComparison()` calls `createCalculator()` per provider | VERIFIED | Lines 106–112: iterates PROVIDERS, calls `createCalculator` |
| `dia-calculator.ts` | `engine/dia-packages.ts` | `DiaCalculator` calls `selectOptimalDiaPackage` | VERIFIED | Line 111: `selectOptimalDiaPackage(nonOverriddenModules, this.config.packages, perModulePrices)` |
| `jij-calculator.ts` | `data/providers/jij.ts` | `JijCalculator` uses `pricingStrategy.tiers` for tier selection | VERIFIED | Lines 60–69: `const { tiers, defaultTestsPerStudent } = this.config.pricingStrategy; tiers.find(...)` |
| `store.ts` | `engine/price-comparison.ts` | Store calls `calculateComparison` with options object | VERIFIED | Lines 135–137, 204–207: `calculateComparison(selectedModules, studentCounts, { citoBundleType[, overridePrices] })` |
| `store.ts` | `engine/cito-bundles.ts` | Store calls `applyContractPeriodToResult` | VERIFIED | Lines 142, 212: `applyContractPeriodToResult(annualResult, state.contractPeriod, citoFactor)` |

---

### Data-Flow Trace (Level 4)

The calculators are pure functions — data flows from `PROVIDER_CONFIGS` (static provider config) through calculators into `ComparisonResult`, which is rendered by the UI. No async/DB queries involved.

| Artifact | Data Variable | Source | Produces Real Data | Status |
|----------|---------------|--------|--------------------|--------|
| `price-comparison.ts` | `modules`, `totals` | `PROVIDER_CONFIGS[provider].defaultPrices` + calculators | Yes — real tier/discount/bundle logic | FLOWING |
| `store.ts` `result` | `ComparisonResult` | `calculateComparison()` | Yes — full calculator pipeline | FLOWING |
| `store.ts` `diaPackageResult` | `DiaPackageResult` | `annualResult.diaPackageResult` | Yes — set from DIA calculator's package optimization | FLOWING |

---

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| All engine + store tests pass | `npx vitest run src/engine/__tests__/ src/features/price-comparison/__tests__/store.test.ts` | 165 tests, 14 files, 0 failures | PASS |
| `createCalculator` factory exported | Module export check via test suite | `createCalculator` test cases in calculators.test.ts pass | PASS |
| JIJ tier varies per school size | `calculators.test.ts` (100/800/2500 students) | Different `tierId` and `pricePerStudent` per size | PASS |
| DIA volume discount 0%/5%/10% | `calculators.test.ts` | Discount applied per threshold | PASS |
| Legacy function absent | `grep calculateComparisonLegacy price-comparison.ts` | 0 matches | PASS |
| Store removed provider pipelines | `grep applyDiaVolumeDiscount store.ts` | 0 matches | PASS |

---

### Requirements Coverage

| Requirement | Description | Status | Evidence |
|-------------|-------------|--------|----------|
| PRIJS-01 | Gebruiker ziet per geselecteerde module de kosten per leerling en totaalkosten per aanbieder naast elkaar | SATISFIED | `calculateComparison()` returns `ModuleComparison[]` with `providers: Record<ProviderKey, ProviderCost \| null>` including `pricePerStudent` and `totalCost` per provider |
| PRIJS-06 | Engine berekent correcte DIA-pakketprijzen: als school 3+ DIA-modules afneemt wordt automatisch het voordeligste pakket berekend | SATISFIED | `DiaCalculator.calculateAll()` calls `selectOptimalDiaPackage`; covered modules get `isPackagePrice: true`; `ComparisonResult.diaPackageResult` carries the result |
| PRIJS-08 | Hybride scenario: engine berekent per module apart waar school wisselt van aanbieder en toont besparingen per module | SATISFIED | `store.ts` `computeExtendedResults()` calls `calculateHybridScenario(result, moduleSetups)` using `ComparisonResult` from the new calculator-based engine; hybrid-scenario tests pass (confirmed in 165 test run) |

All three requirement IDs declared in both PLAN frontmatter sections are accounted for. No orphaned requirements found — REQUIREMENTS.md maps PRIJS-01, PRIJS-06, PRIJS-08 to "Phase 10" (Complete).

---

### Anti-Patterns Found

None found. Checks performed:

- No `TODO`/`FIXME`/`PLACEHOLDER` comments in calculator files or store.ts
- No `return null` / `return {}` / `return []` stub implementations
- No empty handlers
- `calculateComparisonLegacy` fully removed
- `applyDiaVolumeDiscount`, `applyDynamicJijPrices`, `applyDiaPackageToResult` fully removed from store
- Dutch breakdown labels present in all 4 calculators (verified in calculator test assertions)

---

### Human Verification Required

None required. All behavioral aspects are verifiable through the test suite:

- JIJ tier selection correctness: covered by calculators.test.ts with exact numeric assertions
- DIA package optimization trigger logic: covered by calculators.test.ts
- Override precedence: covered per calculator
- Parity with old pipeline: covered by snapshot regression tests (30 snapshots)

---

### Gaps Summary

No gaps. All 11 observable truths verified. All 10 required artifacts exist, are substantive, and are correctly wired. All key links confirmed present in source. All 3 requirements (PRIJS-01, PRIJS-06, PRIJS-08) satisfied. Test suite passes with 165 tests, 0 failures, 0 regressions.

---

_Verified: 2026-03-24T21:14:00Z_
_Verifier: Claude (gsd-verifier)_

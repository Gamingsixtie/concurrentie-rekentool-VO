---
phase: 01-fundament
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - src/App.tsx
  - src/components/wizard/WizardShell.tsx
  - src/components/wizard/ProgressBar.tsx
  - src/components/wizard/StepContainer.tsx
  - src/components/wizard/NavigationButtons.tsx
  - src/features/school-profile/components/WizardStep1.tsx
  - src/features/school-profile/components/WizardStep2.tsx
  - src/features/school-profile/components/WizardStep3.tsx
  - src/features/school-profile/components/WizardStep4.tsx
  - src/features/school-profile/__tests__/step1.test.tsx
  - src/features/school-profile/__tests__/step2.test.tsx
  - src/features/school-profile/__tests__/step3.test.tsx
  - src/features/school-profile/__tests__/step4.test.tsx
  - src/features/school-profile/__tests__/wizard-navigation.test.tsx
autonomous: true
requirements:
  - PROF-01
  - PROF-02
  - PROF-03
  - PROF-04
  - UX-03
  - UX-04
must_haves:
  truths:
    - "Gebruiker kan schoolniveaus selecteren via checkboxes (vmbo-b, vmbo-k, vmbo-gt, havo, vwo)"
    - "Gebruiker kan leerlingaantallen invoeren per leerjaar per geselecteerd niveau in een matrix"
    - "Gebruiker kan een standaardprofiel toepassen (Klein/Middelgroot/Groot VO) dat de matrix vult"
    - "Gebruiker kan modules selecteren via togglekaarten in twee categorieen"
    - "Gebruiker kan scenario A of B kiezen via radiokaarten"
    - "Voortgangsbalk toont 4 stappen, voltooide stappen zijn klikbaar"
    - "Vooruit navigeren kan alleen als huidige stap valide is"
    - "Terug navigeren bewaart alle ingevulde data"
    - "Alle UI-tekst is in het Nederlands met formeel 'u'-vorm"
  artifacts:
    - path: "src/components/wizard/WizardShell.tsx"
      provides: "Wizard container met progress bar en step routing"
      min_lines: 30
    - path: "src/components/wizard/ProgressBar.tsx"
      provides: "4-stappen voortgangsbalk met klikbare voltooide stappen"
      min_lines: 40
    - path: "src/features/school-profile/components/WizardStep1.tsx"
      provides: "Schoolniveau-selectie met checkboxes"
      min_lines: 30
    - path: "src/features/school-profile/components/WizardStep2.tsx"
      provides: "Leerlingaantallen matrix met preset-knoppen"
      min_lines: 60
    - path: "src/features/school-profile/components/WizardStep3.tsx"
      provides: "Module-selectie met togglekaarten"
      min_lines: 40
    - path: "src/features/school-profile/components/WizardStep4.tsx"
      provides: "Scenario-selectie met radiokaarten"
      min_lines: 30
  key_links:
    - from: "src/components/wizard/WizardShell.tsx"
      to: "src/features/school-profile/store.ts"
      via: "reads currentStep from zustand store"
      pattern: "useSchoolProfileStore"
    - from: "src/features/school-profile/components/WizardStep1.tsx"
      to: "src/features/school-profile/schemas/step1-schema.ts"
      via: "zodResolver for form validation"
      pattern: "zodResolver.*schoolTypeSchema"
    - from: "src/features/school-profile/components/WizardStep2.tsx"
      to: "src/data/school-profiles.ts"
      via: "imports SCHOOL_SIZE_PRESETS for preset buttons"
      pattern: "SCHOOL_SIZE_PRESETS"
    - from: "src/App.tsx"
      to: "src/components/wizard/WizardShell.tsx"
      via: "renders WizardShell as main content"
      pattern: "<WizardShell"
---

<objective>
Complete 4-step wizard UI met voortgangsbalk, navigatie, formuliervalidatie en alle stap-componenten.

Purpose: Dit is de kern-UI van Phase 1: de gebruiker kan een volledig schoolprofiel invoeren via een stapsgewijze wizard. Alle locked decisions uit CONTEXT.md worden hier geimplementeerd.
Output: Werkende wizard met 4 stappen, klikbare voortgangsbalk, preset-ondersteuning, en volledige Nederlandstalige interface.
</objective>

<execution_context>
@C:/Users/pdebu/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/pdebu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-fundament/01-CONTEXT.md
@.planning/phases/01-fundament/01-RESEARCH.md
@.planning/phases/01-fundament/01-UI-SPEC.md
@.planning/phases/01-fundament/01-01-SUMMARY.md

<interfaces>
<!-- Types and contracts from Plan 01 that this plan depends on -->

From src/models/school.ts:
```typescript
export const SCHOOL_LEVELS = ['vmbo-b', 'vmbo-k', 'vmbo-gt', 'havo', 'vwo'] as const;
export type SchoolLevel = typeof SCHOOL_LEVELS[number];
export const SCHOOL_LEVEL_LABELS: Record<SchoolLevel, string>;
export const YEARS_PER_LEVEL: Record<SchoolLevel, number[]>;
export type Scenario = 'A' | 'B';
export const SCENARIO_LABELS: Record<Scenario, { title: string; description: string }>;
```

From src/models/modules.ts:
```typescript
export type ModuleCategory = 'leerlingvolgsysteem' | 'overige-instrumenten';
export interface ModuleDefinition {
  id: string; name: string; description: string;
  category: ModuleCategory; separateLicense: boolean; differentiator?: string;
}
export const MODULE_CATALOG: ModuleDefinition[];
export const MODULE_CATEGORIES: Record<ModuleCategory, string>;
```

From src/features/school-profile/store.ts:
```typescript
export const useSchoolProfileStore: UseBoundStore<StoreApi<SchoolProfileState>>;
// State: levels, studentCounts, selectedModules, scenario, currentStep
// Actions: setLevels, setStudentCounts, setSelectedModules, setScenario, setCurrentStep, applyPreset, reset
```

From src/data/school-profiles.ts:
```typescript
export interface SchoolSizePreset {
  id: 'klein' | 'midden' | 'groot'; label: string; description: string;
  totalStudents: string; studentCounts: Partial<Record<SchoolLevel, Record<number, number>>>;
}
export const SCHOOL_SIZE_PRESETS: SchoolSizePreset[];
```

From src/features/school-profile/schemas/:
```typescript
// step1-schema.ts: export const schoolTypeSchema (z.object with levels)
// step2-schema.ts: export const studentCountSchema
// step3-schema.ts: export const moduleSelectionSchema
// step4-schema.ts: export const scenarioSchema
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build wizard shell, progress bar, navigation buttons, and all 4 step components</name>
  <files>
    src/App.tsx, src/components/wizard/WizardShell.tsx,
    src/components/wizard/ProgressBar.tsx, src/components/wizard/StepContainer.tsx,
    src/components/wizard/NavigationButtons.tsx,
    src/features/school-profile/components/WizardStep1.tsx,
    src/features/school-profile/components/WizardStep2.tsx,
    src/features/school-profile/components/WizardStep3.tsx,
    src/features/school-profile/components/WizardStep4.tsx
  </files>
  <read_first>
    .planning/phases/01-fundament/01-UI-SPEC.md
    .planning/phases/01-fundament/01-CONTEXT.md
    .planning/phases/01-fundament/01-RESEARCH.md
    src/features/school-profile/store.ts
    src/features/school-profile/schemas/step1-schema.ts
    src/features/school-profile/schemas/step2-schema.ts
    src/features/school-profile/schemas/step3-schema.ts
    src/features/school-profile/schemas/step4-schema.ts
    src/models/school.ts
    src/models/modules.ts
    src/data/school-profiles.ts
    src/styles/index.css
  </read_first>
  <action>
Build all wizard components following the UI-SPEC exactly. All text in Dutch, formal "u" form. All interactive elements have minimum 44px touch targets.

**src/components/wizard/ProgressBar.tsx:**
- Horizontal bar with 4 numbered circles (36px diameter, 16px font) connected by 2px lines
- Step labels below circles: "Niveaus", "Leerlingen", "Modules", "Scenario"
- Completed steps: `bg-cito-primary` background, white text, checkmark replacing number. Clickable (cursor-pointer), onClick calls `setCurrentStep`
- Current step: `bg-cito-accent` (#FF6600) background, white text. NOT clickable
- Future steps: `bg-neutral-200` background, `text-neutral-500`. NOT clickable
- Connector lines between circles: same color scheme as step status
- Props: `currentStep: number`, `completedSteps: number[]`, `onStepClick: (step: number) => void`

**src/components/wizard/NavigationButtons.tsx:**
- Flex row with `justify-between`, 32px margin-top
- "Vorige stap" (secondary button): transparent bg, `text-cito-primary`, 1px `border-cito-primary`, hover `bg-[#f0f4ff]`. Hidden on step 0.
- "Volgende stap" / "Bekijk resultaten" (primary button): `bg-cito-accent` (#FF6600), white text, 16px font-semibold, 12px vertical / 24px horizontal padding, rounded-lg, min-h-[44px]. Hover: `bg-cito-accent-hover` (#e55b00). Active: `bg-cito-accent-active` (#cc5200). Disabled: `bg-neutral-200` + `text-neutral-400` + cursor-not-allowed. Focus: ring-2 ring-cito-primary ring-offset-2.
- Step 4 button text: "Bekijk resultaten" instead of "Volgende stap"
- Props: `currentStep: number`, `onBack: () => void`, `onNext: () => void`, `nextDisabled: boolean`, `isLastStep: boolean`

**src/components/wizard/StepContainer.tsx:**
- Simple wrapper div with heading (20px semibold, `text-cito-primary`) and content slot
- Props: `title: string`, `children: React.ReactNode`

**src/components/wizard/WizardShell.tsx:**
- White card container on `bg-cito-bg`: `bg-white rounded-xl shadow-sm max-w-[720px] mx-auto py-12 px-8`
- Renders ProgressBar at top
- Renders current step component based on `currentStep` from zustand store (0-indexed: 0=Step1, 1=Step2, 2=Step3, 3=Step4)
- Renders NavigationButtons at bottom
- No animation on step transition; instant swap. Scroll to top of wizard on step change.
- Handles onNext: validates current step form, if valid saves to store and increments currentStep
- Handles onBack: decrements currentStep (no validation needed), data preserved in store
- Tracks which steps are completed (validated and saved) for progress bar clickability

**src/features/school-profile/components/WizardStep1.tsx:**
- StepContainer with title "Welke niveaus biedt uw school aan?"
- 5 checkbox items, one per SCHOOL_LEVEL, using SCHOOL_LEVEL_LABELS for display text
- Each item: full-width row, 48px height, 16px padding, checkbox (20px square, `border-neutral-200`, checked: `bg-cito-primary`) on left, label on right
- Uses react-hook-form with zodResolver(schoolTypeSchema)
- defaultValues loaded from useSchoolProfileStore
- Validation error below list: "Selecteer minimaal een niveau om door te gaan" in `text-red-600` at 14px
- On valid submit: calls setLevels on store

**src/features/school-profile/components/WizardStep2.tsx:**
- StepContainer with title "Hoeveel leerlingen per leerjaar?"
- Preset button row above matrix: label "Vul standaard in:" then 3 buttons ("Klein VO", "Middelgroot VO", "Groot VO")
  - Outlined style: `border-cito-accent text-cito-accent` bg transparent, 14px semibold, rounded-md, 8px horizontal padding, 40px height
  - Active preset (last applied): `bg-cito-accent text-white`
  - onClick: calls `applyPreset` on zustand store, which fills studentCounts AND updates levels
- Matrix grid: columns = leerjaren (Leerjaar 1, 2, 3...), rows = only selected levels from store
  - Row header: SCHOOL_LEVEL_LABELS value, 14px semibold, left-aligned, min-w-[120px]
  - Column header: "Leerjaar 1", "Leerjaar 2", etc., 14px semibold, center-aligned
  - Input cell: `<input type="number" min="0">`, 64px width, 40px height, center-aligned, white bg, 1px `border-neutral-200`, rounded-md
  - Focus: 2px `border-cito-primary`, no outline
  - Invalid: 1px `border-red-600`
  - Uses YEARS_PER_LEVEL to determine which columns to show per level
- Horizontal scroll on matrix container if overflow
- Uses react-hook-form with zodResolver for step 2 schema
- defaultValues loaded from store
- On valid submit: calls setStudentCounts on store
- Validation error: "Vul voor elk leerjaar een geldig leerlingaantal in (0 of hoger)"

**src/features/school-profile/components/WizardStep3.tsx:**
- StepContainer with title "Welke toetsen en instrumenten gebruikt uw school?"
- Two groups, each with category heading: MODULE_CATEGORIES values in 16px semibold `text-cito-primary`
- Second group: 24px margin-top
- Cards in responsive grid: 1 column default, 2 columns at sm (480px+): `grid grid-cols-1 sm:grid-cols-2 gap-4`
- Each module card:
  - White bg, 1px `border-neutral-200`, rounded-lg, 16px padding, `shadow-sm`
  - Module name: 16px semibold. Description: 14px regular `text-neutral-500`.
  - Toggle on right side, vertically centered: 44px wide, 24px tall pill. Off: `bg-neutral-200`. On: `bg-cito-primary`. White 20px circle knob.
  - Selected state: 2px `border-cito-accent` replacing 1px gray border, `bg-[#fff7ed]` tint
  - For cognitieve-capaciteiten: 14px italic "Losse licentie" in `text-neutral-500` below description
  - For modules with differentiator: 14px `text-cito-primary` below description, prefixed with bullet
- Note below cards when nothing selected: "U kunt altijd later modules toevoegen of verwijderen" in 14px `text-neutral-500`
- All modules default to off (deselected)
- No minimum validation -- 0 modules is valid
- Uses react-hook-form
- On submit: calls setSelectedModules on store

**src/features/school-profile/components/WizardStep4.tsx:**
- StepContainer with title "Wat wilt u vergelijken?"
- Two radio-style cards stacked vertically, 16px gap
- Each card: white bg, 1px `border-neutral-200`, rounded-lg, 24px padding, full width, cursor-pointer
- Selected card: 2px `border-cito-primary`, left edge 4px solid `border-cito-accent`, `bg-[#f0f4ff]`
- Radio indicator: 20px circle on left. Unselected: 2px `border-neutral-200`, white fill. Selected: `bg-cito-primary` fill with 6px white inner dot
- Card title: 20px semibold. Use SCENARIO_LABELS[scenario].title
- Card description: 16px regular `text-neutral-500`, 4px gap below title. Use SCENARIO_LABELS[scenario].description
- Uses react-hook-form with zodResolver(scenarioSchema)
- On valid submit: calls setScenario on store

**src/App.tsx:**
- Update to render WizardShell as main content within the existing layout (keep the `bg-cito-bg` and max-width container)
- Keep the "Rekentool VO" title and "Vergelijk toetsaanbieders voor uw school" subtitle above the wizard
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -5 && npx vitest run --reporter=verbose 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - src/components/wizard/WizardShell.tsx contains "useSchoolProfileStore"
    - src/components/wizard/ProgressBar.tsx contains "Niveaus"
    - src/components/wizard/ProgressBar.tsx contains "Leerlingen"
    - src/components/wizard/ProgressBar.tsx contains "Modules"
    - src/components/wizard/ProgressBar.tsx contains "Scenario"
    - src/components/wizard/NavigationButtons.tsx contains "Volgende stap"
    - src/components/wizard/NavigationButtons.tsx contains "Vorige stap"
    - src/components/wizard/NavigationButtons.tsx contains "Bekijk resultaten"
    - src/features/school-profile/components/WizardStep1.tsx contains "Welke niveaus biedt uw school aan?"
    - src/features/school-profile/components/WizardStep1.tsx contains "zodResolver"
    - src/features/school-profile/components/WizardStep1.tsx contains "SCHOOL_LEVEL_LABELS"
    - src/features/school-profile/components/WizardStep2.tsx contains "Hoeveel leerlingen per leerjaar?"
    - src/features/school-profile/components/WizardStep2.tsx contains "Vul standaard in"
    - src/features/school-profile/components/WizardStep2.tsx contains "Klein VO"
    - src/features/school-profile/components/WizardStep2.tsx contains "Middelgroot VO"
    - src/features/school-profile/components/WizardStep2.tsx contains "Groot VO"
    - src/features/school-profile/components/WizardStep2.tsx contains "SCHOOL_SIZE_PRESETS"
    - src/features/school-profile/components/WizardStep3.tsx contains "Welke toetsen en instrumenten gebruikt uw school?"
    - src/features/school-profile/components/WizardStep3.tsx contains "MODULE_CATALOG"
    - src/features/school-profile/components/WizardStep3.tsx contains "Losse licentie"
    - src/features/school-profile/components/WizardStep3.tsx contains "MODULE_CATEGORIES"
    - src/features/school-profile/components/WizardStep4.tsx contains "Wat wilt u vergelijken?"
    - src/features/school-profile/components/WizardStep4.tsx contains "SCENARIO_LABELS"
    - src/App.tsx contains "WizardShell"
    - npm run build exits with code 0
  </acceptance_criteria>
  <done>Alle 4 wizard stappen renderen correct met Nederlandse tekst, formuliervalidatie, en navigatie. Voortgangsbalk is klikbaar voor voltooide stappen. Preset-knoppen vullen de leerlingaantallen-matrix. Module-selectie toont categoriegroe pen en differentiators. Scenario-selectie werkt met radiokaarten.</done>
</task>

<task type="auto">
  <name>Task 2: Write component tests for all 4 wizard steps and navigation</name>
  <files>
    src/features/school-profile/__tests__/step1.test.tsx,
    src/features/school-profile/__tests__/step2.test.tsx,
    src/features/school-profile/__tests__/step3.test.tsx,
    src/features/school-profile/__tests__/step4.test.tsx,
    src/features/school-profile/__tests__/wizard-navigation.test.tsx
  </files>
  <read_first>
    src/features/school-profile/components/WizardStep1.tsx
    src/features/school-profile/components/WizardStep2.tsx
    src/features/school-profile/components/WizardStep3.tsx
    src/features/school-profile/components/WizardStep4.tsx
    src/components/wizard/WizardShell.tsx
    src/components/wizard/ProgressBar.tsx
    src/features/school-profile/store.ts
    src/features/school-profile/schemas/step1-schema.ts
  </read_first>
  <action>
Write tests using React Testing Library + vitest for each wizard step and the navigation flow.

**src/features/school-profile/__tests__/step1.test.tsx:**
- Test: renders all 5 school level checkboxes with correct Dutch labels (VMBO Basis, VMBO Kader, VMBO GT, HAVO, VWO)
- Test: selecting at least one checkbox allows form submission (no error shown)
- Test: submitting without any selection shows error "Selecteer minimaal een niveau om door te gaan"
- Test: selected levels are persisted to zustand store on submit
- Reset zustand store in beforeEach

**src/features/school-profile/__tests__/step2.test.tsx:**
- Test: renders matrix rows only for levels selected in store (set store to {levels: ['havo', 'vwo']} in beforeEach)
- Test: renders correct number of columns per level (5 for havo, 6 for vwo)
- Test: preset button "Klein VO" fills the matrix with SCHOOL_SIZE_PRESETS values
- Test: entering a valid number in a cell does not show error
- Test: submitting with all valid numbers persists studentCounts to store

**src/features/school-profile/__tests__/step3.test.tsx:**
- Test: renders all 6 module cards with correct names
- Test: renders category headings "Leerlingvolgsysteem" and "Overige instrumenten"
- Test: toggling a module card updates its visual state (selected border)
- Test: "Losse licentie" text appears only for Cognitieve capaciteitentoets
- Test: submitting with 0 modules selected does NOT show error (valid)
- Test: selected modules are persisted to store on submit

**src/features/school-profile/__tests__/step4.test.tsx:**
- Test: renders both scenario cards with correct titles ("Cito vs. concurrentie" and "Huidig naar nieuw Cito-platform")
- Test: clicking a scenario card selects it (visual indicator changes)
- Test: submitting without selection shows error
- Test: selected scenario is persisted to store on submit

**src/features/school-profile/__tests__/wizard-navigation.test.tsx:**
- Test: progress bar shows 4 steps with correct labels
- Test: clicking a completed step in the progress bar navigates back
- Test: clicking a future step in the progress bar does NOT navigate
- Test: "Vorige stap" button is hidden on step 1
- Test: "Volgende stap" is disabled when current step is invalid
- Test: navigating back preserves previously entered data

Each test file must:
- Import from `@testing-library/react` (render, screen, fireEvent or userEvent)
- Import the component under test
- Reset the zustand store before each test using `useSchoolProfileStore.getState().reset()`
- Use `waitFor` for async form validation assertions
  </action>
  <verify>
    <automated>npx vitest run --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - src/features/school-profile/__tests__/step1.test.tsx contains "VMBO Basis"
    - src/features/school-profile/__tests__/step1.test.tsx contains "Selecteer minimaal een niveau"
    - src/features/school-profile/__tests__/step2.test.tsx contains "Klein VO"
    - src/features/school-profile/__tests__/step2.test.tsx contains "SCHOOL_SIZE_PRESETS"
    - src/features/school-profile/__tests__/step3.test.tsx contains "Leerlingvolgsysteem"
    - src/features/school-profile/__tests__/step3.test.tsx contains "Losse licentie"
    - src/features/school-profile/__tests__/step4.test.tsx contains "Cito vs. concurrentie"
    - src/features/school-profile/__tests__/wizard-navigation.test.tsx contains "Volgende stap"
    - src/features/school-profile/__tests__/wizard-navigation.test.tsx contains "Vorige stap"
    - npx vitest run exits with code 0 (all tests pass)
  </acceptance_criteria>
  <done>Alle wizard step tests en navigation tests slagen. Test coverage bevestigt: niveau-selectie valideert minimaal 1, matrix toont juiste kolommen per niveau, presets werken, modules hebben categorieen en toggles, scenario-selectie persists naar store, en navigatie bewaart data.</done>
</task>

</tasks>

<verification>
1. `npm run build` exits with code 0
2. `npx vitest run --reporter=verbose` exits with code 0 (all tests pass including step tests)
3. Wizard renders in browser: 4-step progress bar visible, all steps navigable
4. All Dutch copy matches UI-SPEC copywriting contract exactly
5. Cito brand colors visible throughout wizard (primary blue, accent orange)
</verification>

<success_criteria>
- Working 4-step wizard: Niveaus -> Leerlingen -> Modules -> Scenario
- Clickable progress bar for completed steps, non-clickable for future steps
- Per-step zod validation with Dutch error messages
- Matrix input for student counts with preset buttons (Klein/Midden/Groot VO)
- Module toggle cards grouped by category with differentiators shown
- Scenario radio cards with visual selection state
- All data persisted in zustand store across step navigation
- Back navigation preserves all entered data
- All component tests passing
</success_criteria>

<output>
After completion, create `.planning/phases/01-fundament/01-02-SUMMARY.md`
</output>

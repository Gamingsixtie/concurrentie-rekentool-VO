---
phase: 24-ux-audit-vergelijkingsoverzicht-stakeholder-ready
plan: 02
type: execute
wave: 2
depends_on: [24-01]
files_modified:
  - src/features/price-comparison/ai-advies/AiAdviesSection.tsx
  - src/features/price-comparison/AnalysisPanel.tsx
autonomous: false
requirements: [D-01, D-09, D-10, D-11, D-14]

must_haves:
  truths:
    - "AI Advies hero is the first section on the page, collapsed by default with a 2-3 line summary visible"
    - "SchoolplanBanner content is integrated inside AiAdviesSection, no separate banner"
    - "Clicking 'Lees volledig advies' expands the full AI analysis content"
    - "When no analysis exists, a wizard CTA is shown instead of an empty summary"
    - "Self-explanatory tooltips on key terms help stakeholders understand without consultant explanation"
  artifacts:
    - path: "src/features/price-comparison/ai-advies/AiAdviesSection.tsx"
      provides: "AI hero with integrated schoolplan context and collapse/expand"
      exports: ["AiAdviesSection"]
  key_links:
    - from: "src/features/price-comparison/ai-advies/AiAdviesSection.tsx"
      to: "useWizardStore"
      via: "hasCompletedOnce for conditional summary display"
      pattern: "hasCompletedOnce"
    - from: "src/features/price-comparison/ai-advies/AiAdviesSection.tsx"
      to: "SchoolplanBanner"
      via: "SchoolplanBanner rendered inside AiAdviesSection"
      pattern: "SchoolplanBanner"
---

<objective>
Restructure AiAdviesSection into a collapsed AI hero with integrated SchoolplanBanner, progressive disclosure summary, and self-explanatory tooltips.

Purpose: The AI hero becomes the first visual anchor on the page (per D-01, D-09). Stakeholders see a 2-3 line conclusion immediately. Consultants can expand for the full analysis. SchoolplanBanner merges into this section to eliminate the separate banner.
Output: Restructured AiAdviesSection with collapse/expand, integrated schoolplan context, and tooltips.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-ux-audit-vergelijkingsoverzicht-stakeholder-ready/24-CONTEXT.md
@.planning/phases/24-ux-audit-vergelijkingsoverzicht-stakeholder-ready/24-RESEARCH.md
@.planning/phases/24-ux-audit-vergelijkingsoverzicht-stakeholder-ready/24-UI-SPEC.md
@.planning/phases/24-ux-audit-vergelijkingsoverzicht-stakeholder-ready/24-01-SUMMARY.md

<interfaces>
<!-- Key types from AI analysis that provide the summary field -->

From src/lib/ai-analysis.ts:
```typescript
export interface AnalysisResult {
  samenvatting: string;          // <-- This is the 2-3 line summary for D-09
  prijsanalyse: PrijsAnalyseItem[];
  citoSterkePunten: Array<{ module: string; argumenten: string[] }>;
  concurrentieVergelijking: ConcurrentDetail[];
  schoolplanKoppeling: SchoolplanKoppeling[] | null;
  gespreksargumenten: string[];
}
```

From src/features/price-comparison/wizard/wizard-store.ts:
```typescript
// Relevant selectors for AI hero state
hasCompletedOnce: boolean;      // Whether wizard has been completed at least once
wizardNarrativeContext: WizardNarrativeContext | null;  // Output from wizard completion
```

From src/features/price-comparison/AnalysisPanel.tsx:
```typescript
// AnalysisPanel manages its own local state for analysis results
// It calls generateAnalysis() and stores result in useState<AnalysisResult | null>
// The samenvatting field from AnalysisResult is what we need for the collapsed hero
```

From src/features/price-comparison/SchoolplanBanner.tsx:
```typescript
// SchoolplanBanner renders:
// - No schoolplan: purple banner suggesting upload with Link to /scholen/$slug/schoolplan
// - Schoolplan complete: compact purple summary with opportunity count + themes
// - Other states: null
// Uses: useSchoolProfileStore (activeSchoolId), useSchoolplanAnalysis hook, useParams + Link from @tanstack/react-router
```

From src/features/price-comparison/ai-advies/SchoolplanContextCard.tsx:
```typescript
// Already rendered inside AiAdviesSection as Step 1
// Shows schoolplan context in the AI narrative flow
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restructure AiAdviesSection as collapsible AI hero with integrated SchoolplanBanner</name>
  <files>
    src/features/price-comparison/ai-advies/AiAdviesSection.tsx
    src/features/price-comparison/AnalysisPanel.tsx
  </files>
  <read_first>
    src/features/price-comparison/ai-advies/AiAdviesSection.tsx
    src/features/price-comparison/SchoolplanBanner.tsx
    src/features/price-comparison/ai-advies/SchoolplanContextCard.tsx
    src/features/price-comparison/AnalysisPanel.tsx
    src/features/price-comparison/wizard/wizard-store.ts
    src/lib/ai-analysis.ts
  </read_first>
  <action>
    Restructure AiAdviesSection to be a collapsible AI hero per D-01 and D-09.

    **1. Add SchoolplanBanner integration (per D-01):**
    - Import `SchoolplanBanner` from `'../SchoolplanBanner'`
    - Render `<SchoolplanBanner />` as the FIRST element inside AiAdviesSection (before the section header), replacing the current `SchoolplanContextCard` in step 1. SchoolplanContextCard stays for the narrative flow but SchoolplanBanner provides the top-level context.
    - Actually: SchoolplanBanner IS the top-level schoolplan context. SchoolplanContextCard is the detailed step 1 inside the wizard narrative. Keep both but move SchoolplanBanner to be ABOVE the section header in the hero.

    **2. Add collapse/expand state (per D-09):**
    - Add `const [expanded, setExpanded] = useState(false)` for the AI content
    - The hero has TWO visual states:

    **Collapsed state (default):**
    - SchoolplanBanner (always visible at top of hero)
    - Section header with icon + title "AI Advies" + subtitle "Schoolplan-context, vergelijkingsadvies en concurrentieanalyse"
    - If analysis exists (AnalysisPanel has been run): show the `samenvatting` text from the analysis result as 2-3 line summary preview
    - "Lees volledig advies" button: `className="inline-flex items-center gap-1.5 mt-3 text-sm font-semibold text-cito-accent hover:underline"` — onClick sets `expanded(true)`
    - If no analysis exists: show "Start de analyse om een advies op maat te ontvangen" in neutral text + the ComparisonWizard (always visible so user can start)

    **Expanded state:**
    - SchoolplanBanner (always visible)
    - Full existing content: section header, complete banner (if wizard completed), SchoolplanContextCard + NarrativeConnector + ComparisonWizard + NarrativeConnector + AnalysisPanel
    - "Samenvatting tonen" button to collapse back: `className="inline-flex items-center gap-1.5 mt-3 text-sm font-semibold text-cito-accent hover:underline"` — onClick sets `expanded(false)`

    **3. Analysis summary extraction challenge:**
    The AnalysisPanel currently manages its own `analysis` state internally via `useState`. The `samenvatting` field is NOT exposed to AiAdviesSection. Two approaches:

    **Approach A (recommended — lift summary state up):**
    - Add `analysisResult` and `setAnalysisResult` as props passed from AiAdviesSection to AnalysisPanel
    - In AiAdviesSection: `const [analysisResult, setAnalysisResult] = useState<{ samenvatting: string } | null>(null)`
    - In AnalysisPanel: accept optional `onAnalysisComplete?: (result: AnalysisResult) => void` prop. Call it when analysis finishes (in the `handleGenerate` success path after `setAnalysis(analysisResult)`).
    - AiAdviesSection reads `analysisResult?.samenvatting` for the collapsed summary text.

    **Implementation for AnalysisPanel change:**
    - Add `onAnalysisComplete` optional prop to AnalysisPanelProps interface
    - After `setAnalysis(analysisResult)` on line 165 of AnalysisPanel.tsx, add: `onAnalysisComplete?.(analysisResult)`
    - This is a backward-compatible addition — other AnalysisPanel consumers don't pass the prop

    **4. Remove outer card styling:**
    - Remove the `bg-white rounded-xl border border-neutral-200 p-6 mb-10` wrapper div. The SectionBand from Plan 01 handles background/spacing. The hero content renders directly inside.

    **5. Auto-expand when wizard completes:**
    - When the wizard fires `shouldAutoTriggerAnalysis`, auto-expand the hero so the user sees the analysis generating.

    All UI text in Dutch. Code/variable names in English.
  </action>
  <verify>
    <automated>cd "C:\Users\pdebu\Projects VS code\Agentic OS\agentic-os\projects\Concurrentieenrekentool VO" && npm run build 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - AiAdviesSection.tsx contains `import { SchoolplanBanner }` or `import { SchoolplanBanner } from`
    - AiAdviesSection.tsx contains `useState(false)` (expanded state, starts collapsed)
    - AiAdviesSection.tsx contains `Lees volledig advies`
    - AiAdviesSection.tsx contains `Samenvatting tonen`
    - AiAdviesSection.tsx contains `samenvatting` (reading the summary field)
    - AiAdviesSection.tsx contains `Start de analyse om een advies op maat te ontvangen`
    - AiAdviesSection.tsx does NOT contain `bg-white rounded-xl border border-neutral-200 p-6 mb-10`
    - AiAdviesSection.tsx contains `<SchoolplanBanner`
    - AnalysisPanel.tsx contains `onAnalysisComplete`
    - `npm run build` exits with code 0
  </acceptance_criteria>
  <done>AiAdviesSection renders as a collapsible AI hero with SchoolplanBanner integrated. Default collapsed state shows 2-3 line samenvatting or wizard CTA. Expand shows full analysis. Build passes.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of complete UX restructure</name>
  <files>
    src/features/price-comparison/PriceComparisonPage.tsx
    src/features/price-comparison/ai-advies/AiAdviesSection.tsx
    src/features/price-comparison/MeerwaardePanel.tsx
    src/features/price-comparison/components/ProviderToolbar.tsx
  </files>
  <action>
    Run `npm run dev` to start the dev server. Navigate to an existing school's comparison page (Vergelijking tab). Perform the 10-step visual verification described in how-to-verify. No code changes in this task — purely user verification of the complete Phase 24 restructure across Plan 01 and Plan 02.
  </action>
  <verify>
    <automated>cd "C:\Users\pdebu\Projects VS code\Agentic OS\agentic-os\projects\Concurrentieenrekentool VO" && npx vitest run 2>&1 | tail -10</automated>
  </verify>
  <what-built>
    Complete PriceComparisonPage UX restructure:
    - New section order per D-05 (AI hero, bediening, totalen, toolbar+tabel, grafiek, meerwaarde, disclaimer)
    - Alternating color bands (bg-neutral-50 / bg-white)
    - AI hero collapsed with summary text or wizard CTA
    - SchoolplanBanner integrated in AI hero
    - Provider toolbar with info popovers
    - Chart collapsed by default
    - MeerwaardePanel collapsed by default (only time savings, no differentiators)
    - ComparisonSummary without differentiators list
    - Self-explanatory tooltips on key terms (bundel, contractperiode, per leerling/jaar)
  </what-built>
  <how-to-verify>
    1. Run `npm run dev` and navigate to an existing school's comparison page
    2. Verify section order from top to bottom: AI hero (bg-neutral-50) -> title + bundel/periode (bg-white) -> totalen (bg-neutral-50) -> toolbar + tabel (bg-white) -> grafiek collapsed (bg-neutral-50) -> meerwaarde collapsed (bg-white) -> disclaimer (bg-neutral-50)
    3. Verify AI hero: if analysis was run before, 2-3 line summary visible. Click "Lees volledig advies" to expand. Click "Samenvatting tonen" to collapse.
    4. If no analysis: verify wizard CTA is shown with "Start de analyse om een advies op maat te ontvangen"
    5. Verify SchoolplanBanner appears inside the AI hero section (not as separate banner above)
    6. Verify provider toolbar: checkboxes with color dots + info icon buttons. Click an info icon — popover shows pricing model description. Click outside to dismiss.
    7. Verify chart section: collapsed by default, click heading to expand, see bar chart
    8. Verify meerwaarde section: collapsed by default, expand to see time savings only (no "Cito voordelen per module" list)
    9. Verify ComparisonSummary: shows only Cito/DIA/JIJ total cards with difference amounts. No "Unieke Cito voordelen" bullet list.
    10. Verify D-14 tooltips: hover over "Bundel" label near CitoBundleSelector — tooltip should say "Een bundel combineert meerdere modules met volumekorting". Hover over PeriodToggle label — tooltip about contractperiode. Check for "per leerling/jaar" tooltip in table/summary area.
    11. Verify mobile (resize to 375px): toolbar wraps to 2 rows, bundel selectors stack, totaal-kaarten single column
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
  <done>User has visually verified all D-01 through D-17 decisions are correctly implemented. All sections render in correct order with proper visual hierarchy, progressive disclosure, tooltips, and responsive behavior.</done>
</task>

</tasks>

<verification>
- `npm run build` passes
- `npx vitest run` — full test suite passes
- Visual: all D-01 through D-17 decisions implemented
- Visual: page is self-explanatory for stakeholders (D-14)
- Visual: progressive disclosure works for AI hero, chart, meerwaarde (D-09, D-10, D-11)
</verification>

<success_criteria>
- AI hero is first section, collapsed by default with samenvatting or wizard CTA
- SchoolplanBanner integrated inside AI hero (no separate banner)
- Full page renders in D-05 order with alternating color bands
- All collapse/expand interactions work (AI hero, chart, meerwaarde)
- User approves visual checkpoint
- Build and tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/24-ux-audit-vergelijkingsoverzicht-stakeholder-ready/24-02-SUMMARY.md`
</output>

---
phase: 07-school-intelligence
plan: 04
type: execute
wave: 3
depends_on: ["07-02", "07-03"]
files_modified:
  - src/features/school-overview/SchoolOverviewPage.tsx
  - src/features/school-overview/SchoolCard.tsx
  - src/features/school-overview/FilterBar.tsx
  - src/features/school-overview/ViewToggle.tsx
  - src/features/school-overview/CardModeToggle.tsx
  - src/features/school-overview/PipelineKanbanView.tsx
  - src/features/school-overview/__tests__/filter.test.ts
  - src/features/price-comparison/__tests__/store.test.ts
autonomous: false
requirements:
  - SCHOOL-05
  - SCHOOL-06
  - PRIJS-07
must_haves:
  truths:
    - "Gebruiker ziet schooloverzicht met pipeline-status badges op elke kaart"
    - "Gebruiker kan schooloverzicht filteren op pipeline-status via filterbar"
    - "Gebruiker kan wisselen tussen lijst-view en kanban pipeline-view"
    - "Gebruiker kan wisselen tussen compacte en uitgebreide kaartweergave"
    - "Kanban pipeline-view toont scholen gegroepeerd per status met drag & drop"
    - "Schoolspecifieke prijsoverschrijvingen zijn apart opgeslagen en niet verward met publicatieprijzen"
  artifacts:
    - path: "src/features/school-overview/SchoolOverviewPage.tsx"
      provides: "Extended overview with FilterBar, ViewToggle, CardModeToggle"
      contains: "FilterBar"
    - path: "src/features/school-overview/FilterBar.tsx"
      provides: "Pipeline status filter buttons"
      contains: "Alle"
    - path: "src/features/school-overview/PipelineKanbanView.tsx"
      provides: "Kanban columns by pipeline status with drag & drop"
      contains: "DndContext"
    - path: "src/features/school-overview/SchoolCard.tsx"
      provides: "Extended card with pipeline badge, primary contact, compact/extended modes"
      contains: "PipelineBadge"
    - path: "src/features/school-overview/ViewToggle.tsx"
      provides: "List / Pipeline view switch"
      contains: "Pipeline"
    - path: "src/features/school-overview/CardModeToggle.tsx"
      provides: "Compact / Uitgebreid toggle"
      contains: "Compact"
  key_links:
    - from: "src/features/school-overview/SchoolOverviewPage.tsx"
      to: "src/features/school-overview/FilterBar.tsx"
      via: "component import"
      pattern: "import.*FilterBar"
    - from: "src/features/school-overview/PipelineKanbanView.tsx"
      to: "@dnd-kit/core"
      via: "DndContext for pipeline drag & drop"
      pattern: "import.*DndContext"
    - from: "src/features/school-overview/SchoolCard.tsx"
      to: "src/components/ui/PipelineBadge.tsx"
      via: "PipelineBadge import"
      pattern: "import.*PipelineBadge"
---

<objective>
Extend the school overview page with pipeline-status filtering, list/kanban view toggle, compact/extended card modes, and a kanban pipeline view with drag & drop status changes.

Purpose: The accountmanager needs to quickly find and manage schools by pipeline status. The kanban view provides a visual pipeline overview, while filters and card modes support both small and large school portfolios.
Output: Feature-complete school overview with filtering, dual view modes, and kanban pipeline management.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-school-intelligence/07-CONTEXT.md
@.planning/phases/07-school-intelligence/07-UI-SPEC.md
@.planning/phases/07-school-intelligence/07-01-SUMMARY.md
@.planning/phases/07-school-intelligence/07-02-SUMMARY.md
@.planning/phases/07-school-intelligence/07-03-SUMMARY.md

<interfaces>
From src/features/school-overview/SchoolOverviewPage.tsx (current):
```typescript
// Uses useLiveQuery to get all schools from Dexie
// Filters by name query
// Renders SchoolCard grid + AddSchoolButton + DeleteSchoolDialog
// Has MigrationWizard for v1 data
```

From src/features/school-overview/SchoolCard.tsx (current):
```typescript
interface SchoolCardProps { school: SchoolRecord; onDelete: (school: SchoolRecord) => void; }
// Renders: school name, level labels, student count, module count, last edited date
// Links to /scholen/$slug/wizard/1
```

From src/db/types.ts (extended in Plan 01):
```typescript
// SchoolRecord now includes:
// pipelineStatus: PipelineStatus
// contacts: Contact[] (with isPrimary)
// conversations: Conversation[]
// viewPreference: 'compact' | 'extended'
```

From src/db/operations.ts:
```typescript
export async function setPipelineStatus(schoolId: number, newStatus: PipelineStatus, reason?: string, lostDealInfo?: LostDealInfo): Promise<void>
export function validatePipelineTransition(from: PipelineStatus, to: PipelineStatus): { allowed: boolean; requiresReason: boolean; requiresLostDeal: boolean }
```

From src/components/ui/PipelineBadge.tsx (from Plan 02):
```typescript
export default function PipelineBadge({ status, size }: { status: PipelineStatus; size?: 'sm' | 'md' }): JSX.Element
```

From src/models/school.ts:
```typescript
export const PIPELINE_STATUSES = ['prospect', 'contact-gelegd', 'demo-presentatie', 'offerte', 'gewonnen', 'verloren'] as const;
export const PIPELINE_STATUS_LABELS: Record<PipelineStatus, string>;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: FilterBar, ViewToggle, CardModeToggle and extended SchoolCard</name>
  <files>src/features/school-overview/FilterBar.tsx, src/features/school-overview/ViewToggle.tsx, src/features/school-overview/CardModeToggle.tsx, src/features/school-overview/SchoolCard.tsx, src/features/school-overview/__tests__/filter.test.ts</files>
  <read_first>
    - src/features/school-overview/SchoolCard.tsx
    - src/features/school-overview/SchoolOverviewPage.tsx
    - src/components/ui/PipelineBadge.tsx
    - .planning/phases/07-school-intelligence/07-UI-SPEC.md (School Overview sections — FilterBar, ViewToggle, CardModeToggle, Extended SchoolCard)
    - .planning/phases/07-school-intelligence/07-CONTEXT.md (D-06, D-08, D-22, D-25, D-26)
  </read_first>
  <action>
1. Create `src/features/school-overview/FilterBar.tsx` per D-25:
   - Props: `{ activeFilter: PipelineStatus | 'all'; onFilterChange: (filter: PipelineStatus | 'all') => void; counts: Record<PipelineStatus | 'all', number> }`
   - Row of buttons: "Alle" + one per PIPELINE_STATUSES
   - Button labels: "Alle", then PIPELINE_STATUS_LABELS values
   - Each button shows count in parentheses: "Prospect (3)"
   - Active filter: bg-cito-primary text-white
   - Inactive: bg-white text-neutral-700 border-neutral-200 hover:bg-neutral-50
   - All buttons: 44px height (touch-friendly per MODE-03), rounded-lg, px-4
   - Horizontal scroll on mobile (overflow-x-auto, flex-nowrap, 8px gap)

2. Create `src/features/school-overview/ViewToggle.tsx` per D-22:
   - Props: `{ activeView: 'list' | 'pipeline'; onViewChange: (view: 'list' | 'pipeline') => void }`
   - Two icon-buttons: "Lijst" (grid icon) and "Pipeline" (kanban icon)
   - Active: bg-cito-primary text-white
   - Inactive: bg-white text-neutral-600 border-neutral-200
   - 44px height, rounded-lg, inline SVG icons

3. Create `src/features/school-overview/CardModeToggle.tsx` per D-06:
   - Props: `{ mode: 'compact' | 'extended'; onModeChange: (mode: 'compact' | 'extended') => void }`
   - Two options: "Compact" and "Uitgebreid"
   - Segmented control style: bg-neutral-100 rounded-lg, active segment bg-white shadow-sm
   - 14px text, 36px height

4. Update `src/features/school-overview/SchoolCard.tsx`:
   - Add new prop: `mode: 'compact' | 'extended'`
   - **Both modes:**
     - Add PipelineBadge next to school name (import from @/components/ui/PipelineBadge, size 'sm')
     - Change Link target from `/scholen/$slug/wizard/1` to `/scholen/$slug` (dashboard, not wizard)
   - **Compact mode (D-06):**
     - Shows: name + PipelineBadge + "Laatst bewerkt" date
     - Minimal card: reduced padding (p-4), smaller min-height
   - **Extended mode (D-06):**
     - Shows everything compact shows PLUS:
     - Primary contact name (find contact where isPrimary === true, show "Contact: {name}" or "Geen primair contact")
     - Modules summary (e.g., "4 modules, 2 bij DIA")
     - Latest conversation date if any conversations exist
     - Next action title if any actions with status 'todo' exist
   - Keep existing delete button, level labels, student count (in extended mode only)

5. Create `src/features/school-overview/__tests__/filter.test.ts`:
   - Test filter logic: 'all' returns all schools, specific status returns only matching
   - Test count calculation per status
   - Test compact vs extended mode renders correct fields
  </action>
  <verify>
    <automated>npx vitest run src/features/school-overview/__tests__/filter.test.ts --reporter=verbose && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - src/features/school-overview/FilterBar.tsx contains `Alle` and `onFilterChange` and PIPELINE_STATUS_LABELS reference
    - src/features/school-overview/FilterBar.tsx buttons have `min-h-[44px]` or `h-11` (44px touch target)
    - src/features/school-overview/ViewToggle.tsx contains `Lijst` and `Pipeline`
    - src/features/school-overview/CardModeToggle.tsx contains `Compact` and `Uitgebreid`
    - src/features/school-overview/SchoolCard.tsx contains `PipelineBadge` import
    - src/features/school-overview/SchoolCard.tsx Link target contains `/scholen/$slug` (not `/wizard/`)
    - src/features/school-overview/SchoolCard.tsx contains `mode` prop handling with `compact` and `extended`
    - src/features/school-overview/__tests__/filter.test.ts contains at least 3 test cases
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>FilterBar, ViewToggle, CardModeToggle and extended SchoolCard with pipeline badges render correctly. Compact shows name+status+date, extended adds contact, modules and actions. Card links to dashboard. Tests pass. Build passes.</done>
</task>

<task type="auto">
  <name>Task 2: PipelineKanbanView and SchoolOverviewPage integration</name>
  <files>src/features/school-overview/PipelineKanbanView.tsx, src/features/school-overview/SchoolOverviewPage.tsx, src/features/price-comparison/__tests__/store.test.ts</files>
  <read_first>
    - src/features/school-overview/SchoolOverviewPage.tsx
    - src/features/school-overview/FilterBar.tsx (from Task 1)
    - src/features/school-overview/ViewToggle.tsx (from Task 1)
    - src/features/school-overview/CardModeToggle.tsx (from Task 1)
    - src/features/school-overview/SchoolCard.tsx (from Task 1)
    - src/db/operations.ts (setPipelineStatus, validatePipelineTransition)
    - src/features/school-profile/components/LostDealDialog.tsx (from Plan 02)
    - src/features/school-profile/components/PipelineReasonDialog.tsx (from Plan 02)
    - .planning/phases/07-school-intelligence/07-UI-SPEC.md (Pipeline Kanban View section)
    - .planning/phases/07-school-intelligence/07-CONTEXT.md (D-20 through D-27)
    - src/features/price-comparison/__tests__/store.test.ts (verify existing tests still pass)
  </read_first>
  <action>
1. Create `src/features/school-overview/PipelineKanbanView.tsx` per D-21, D-22:
   - Props: `{ schools: SchoolRecord[]; cardMode: 'compact' | 'extended'; onDeleteSchool: (school: SchoolRecord) => void }`
   - 6 columns, one per pipeline status in order (PIPELINE_STATUSES)
   - Column headers: status label + count, colored per D-26 pipeline badge colors:
     - prospect column header: bg-neutral-100 text-neutral-600
     - contact-gelegd: bg-blue-50 text-blue-700
     - demo-presentatie: bg-purple-50 text-purple-700
     - offerte: bg-orange-50 text-orange-700
     - gewonnen: bg-green-50 text-green-700
     - verloren: bg-red-50 text-red-700
   - Uses @dnd-kit/core DndContext for drag & drop between columns
   - Each column is a droppable area
   - Each school card is a draggable item (wrap SchoolCard in useDraggable or use overlay)
   - On drag end:
     - Determine newStatus from target column
     - Call validatePipelineTransition(oldStatus, newStatus)
     - If requiresLostDeal: show LostDealDialog (manage via state), commit only on confirm
     - If requiresReason: show PipelineReasonDialog, commit only on confirm
     - Otherwise: call setPipelineStatus directly
   - Layout: horizontal scroll container on mobile, flex-nowrap, each column min-w-[250px]
   - Empty columns show: "Geen scholen" in 14px text-neutral-400, centered
   - Columns have subtle border-neutral-200, rounded-lg, p-4 padding, bg-white

2. Update `src/features/school-overview/SchoolOverviewPage.tsx`:
   - Add state: `pipelineFilter: PipelineStatus | 'all'` (default 'all'), `viewMode: 'list' | 'pipeline'` (default 'list'), `cardMode: 'compact' | 'extended'` (default 'extended')
   - Calculate filter counts: iterate schools, count per status + total for 'all'
   - Apply filter: if pipelineFilter !== 'all', filter schools by pipelineStatus
   - Combine with existing text search: schools filtered by both query AND pipelineFilter
   - Add controls row below search bar:
     - Left side: FilterBar
     - Right side: ViewToggle + CardModeToggle (inline, 8px gap)
   - Conditional rendering:
     - If viewMode === 'list': existing grid with SchoolCard (pass mode={cardMode})
     - If viewMode === 'pipeline': PipelineKanbanView (pass filtered schools, cardMode)
   - Keep existing: AddSchoolButton, DeleteSchoolDialog, MigrationWizard, EmptySchoolState, NotFoundRedirect, SchoolSearchBar
   - Persist viewMode and cardMode in localStorage (simple: `localStorage.getItem/setItem('school-overview-viewMode')`) so preferences survive page refresh

3. Verify PRIJS-07 in existing tests:
   - Read `src/features/price-comparison/__tests__/store.test.ts` and confirm that appliedOverrides in SchoolRecord are tested as school-specific (they are already separate from DEFAULT_PRICES per existing architecture)
   - If no explicit test exists for "overrides are per-school", add one that verifies: two different schools can have different appliedOverrides arrays, and these don't cross-contaminate
  </action>
  <verify>
    <automated>npx vitest run src/features/school-overview/__tests__/ src/features/price-comparison/__tests__/ --reporter=verbose && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - src/features/school-overview/PipelineKanbanView.tsx contains `DndContext` and all 6 pipeline status references
    - src/features/school-overview/PipelineKanbanView.tsx contains `LostDealDialog` and `PipelineReasonDialog` (for drag transitions)
    - src/features/school-overview/PipelineKanbanView.tsx contains `validatePipelineTransition`
    - src/features/school-overview/SchoolOverviewPage.tsx contains `FilterBar` and `ViewToggle` and `CardModeToggle`
    - src/features/school-overview/SchoolOverviewPage.tsx contains `pipelineFilter` state variable
    - src/features/school-overview/SchoolOverviewPage.tsx contains `viewMode` state with 'list' and 'pipeline' options
    - src/features/school-overview/SchoolOverviewPage.tsx contains `PipelineKanbanView` conditional render
    - src/features/school-overview/SchoolOverviewPage.tsx contains `localStorage` for view preferences
    - `npm run build` exits 0
    - `npx vitest run` exits 0 (all tests pass including existing ones)
  </acceptance_criteria>
  <done>SchoolOverviewPage has FilterBar, ViewToggle (list/pipeline), CardModeToggle (compact/extended), and PipelineKanbanView with drag & drop pipeline management including dialog guards for backward transitions and lost deals. All tests pass. Build passes.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify complete School Intelligence phase</name>
  <what-built>Complete CRM-lite school profile system with contacts, conversations, pipeline management, kanban views, and enriched school overview</what-built>
  <how-to-verify>
    1. Start dev server: `npm run dev`
    2. Go to /scholen — verify you see the school overview with FilterBar, ViewToggle, CardModeToggle
    3. Click a school card — verify it opens the dashboard tab (not wizard)
    4. Verify ProfileHeader shows school name, pipeline dropdown, and context-smart CTA
    5. Verify tab navigation: Overzicht | Vergelijking | Producten | Contacten | Gesprekken
    6. Dashboard tab: verify summary block (pipeline, modules, contacts, last contact), smart actions change per pipeline status, school data is inline-editable
    7. Contacts tab: add a contact with all fields, set as primary, verify DMU badge and primary indicator
    8. Try to delete a contact — should succeed if no conversations linked
    9. Conversations tab: add a conversation (select contact, add tags), verify it appears in timeline
    10. Verify system events appear in timeline (e.g., "School aangemaakt")
    11. Add an action from a conversation, verify it appears in the kanban board
    12. Drag an action between kanban columns (Te doen -> In uitvoering)
    13. Change pipeline status via dropdown — verify forward is free, backward shows reason dialog
    14. Set status to "Verloren" — verify LostDealDialog appears requiring competitor selection
    15. Go back to /scholen — verify pipeline badge on school card, filter by status, try pipeline kanban view
    16. Drag a school card between pipeline columns in kanban view
    17. Toggle compact/extended card mode — verify extended shows primary contact and more details
    18. Vergelijking tab: verify existing comparison pages render correctly within the tab
    19. Producten tab: verify module usage with provider labels and price distinction (publication vs school-specific)
    20. Run `npm run build` — verify it succeeds
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
1. `npx vitest run --reporter=verbose` — all tests pass
2. `npm run build` — no errors
3. Full manual walkthrough of all CRM-lite features
</verification>

<success_criteria>
- School overview has working filter bar, list/pipeline view toggle, and compact/extended card modes
- Pipeline kanban view groups schools by status with drag & drop transitions
- Pipeline transitions respect D-24 (backward needs reason) and D-27 (verloren needs competitor)
- School cards show pipeline badge, primary contact name (extended mode), and link to dashboard
- PRIJS-07: school-specific price overrides clearly labeled separately from publication prices
- All existing functionality preserved (wizard, comparison engine, migration)
- `npm run build` and `npx vitest run` both succeed
</success_criteria>

<output>
After completion, create `.planning/phases/07-school-intelligence/07-04-SUMMARY.md`
</output>

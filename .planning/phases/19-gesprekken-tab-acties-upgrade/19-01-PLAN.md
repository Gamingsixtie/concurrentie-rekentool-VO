---
phase: "19"
plan: "01"
type: full-implementation
autonomous: true
wave: 1
depends_on: []
requirements: []
---

# Plan 19-01: Gesprekken-tab & Acties Upgrade

## Objective

Upgrade het gesprekken-tab (AI-extractie verbergen, spraak-naar-tekst toevoegen, contactpersoon-dropdown met DMU+engagement) en verfijn het acties kanban-bord (always-visible inline invoer, inline titel bewerking, type labels, deadline veld, modale verwijder-bevestiging, visuele verbeteringen).

## Context

- @.planning/phases/19-gesprekken-tab-acties-upgrade/19-CONTEXT.md
- @.planning/phases/19-gesprekken-tab-acties-upgrade/19-UI-SPEC.md
- @.planning/phases/19-gesprekken-tab-acties-upgrade/19-RESEARCH.md

## Tasks

### Task 1: Data layer extension (ActionItem type + dueDate, schema, operations, hooks)
type="auto"

Extend ActionItem with `type` and `dueDate` fields. Update schema, operations.ts mappers, useActions hooks.

**Changes:**
- `src/db/types.ts`: Add `type: string | null` and `dueDate: string | null` to ActionItem
- `src/features/school-profile/schemas/action.schema.ts`: Add type and dueDate fields
- `src/db/operations.ts`: Update mapActionRow, addAction, updateAction for new fields
- `src/hooks/useConversations.ts`: Add useDeleteConversation hook
- `src/db/operations.ts`: Add deleteConversation function

**Done criteria:**
- ActionItem interface has type and dueDate
- mapActionRow maps new fields
- addAction/updateAction handle new fields
- deleteConversation exists and works

### Task 2: ConfirmDialog + useSpeechRecognition
type="auto"

Create reusable ConfirmDialog component and useSpeechRecognition hook.

**Changes:**
- `src/components/ui/ConfirmDialog.tsx`: Modal confirmation dialog per IC-07
- `src/hooks/useSpeechRecognition.ts`: Web Speech API wrapper per IC-01

**Done criteria:**
- ConfirmDialog renders modal overlay with configurable title, body, cancel/confirm labels
- useSpeechRecognition returns isListening, isSupported, transcript, start, stop

### Task 3: ConversationForm upgrade (hide AI, add speech, contact dropdown with DMU)
type="auto"

Simplify ConversationForm: hide AI intake toggle, add microphone button for speech-to-text, upgrade contact dropdown to show DMU position and engagement status.

**Changes:**
- `src/features/school-profile/components/ConversationForm.tsx`: Hide IntakeModeToggle, add mic button, upgrade contact dropdown

**Done criteria:**
- AI mode toggle no longer visible
- Microphone button next to Inhoud label
- Contact dropdown shows "Name -- DMU-positie -- Engagement-status"

### Task 4: ActionItemCard upgrade (inline edit, type label, deadline, modal delete)
type="auto"

Upgrade ActionItemCard with inline title editing, type label chips, deadline field with overdue styling, and modal delete confirmation via ConfirmDialog.

**Changes:**
- `src/features/school-profile/components/ActionItem.tsx`: Full rewrite with inline edit, type chips, deadline, ConfirmDialog

**Done criteria:**
- Click on title enters edit mode, blur/Enter saves
- Type label shows as colored chip
- Deadline with overdue visual (red border)
- Delete uses ConfirmDialog modal

### Task 5: ActionKanban upgrade (always-visible input, column icons, empty states, visual refinements)
type="auto"

Replace toggle-based add form with always-visible inline input. Add column header icons. Improve empty states per-column. Visual refinements per IC-09.

**Changes:**
- `src/features/school-profile/components/ActionKanban.tsx`: Always-visible input, column icons, per-column empty states, card hover effects

**Done criteria:**
- Input always visible at bottom of Te doen column
- Column headers have icons
- Per-column empty state messages
- Card hover effects

### Task 6: ConversationsTab integration (delete conversation with ConfirmDialog, remove top-level add action button)
type="auto"

Integrate ConfirmDialog for conversation deletion in Timeline. Remove redundant "+ Actie toevoegen" top-level button (replaced by always-visible input in kanban).

**Changes:**
- `src/features/school-profile/tabs/ConversationsTab.tsx`: Remove top-level action button, add delete conversation flow
- `src/features/school-profile/components/Timeline.tsx`: Add delete button with ConfirmDialog

**Done criteria:**
- Conversations can be deleted via modal confirmation
- Top-level "+ Actie toevoegen" button removed
- All features work together

### Task 7: Build verification
type="auto"

Run build and fix any TypeScript errors.

**Done criteria:**
- `npm run build` passes without errors

## Verification

```bash
npm run build
```

## Success Criteria

1. AI intake mode toggle is hidden in ConversationForm
2. Microphone button enables speech-to-text in Dutch
3. Contact dropdown shows DMU position and engagement status
4. Always-visible inline input in kanban Te doen column
5. Action cards have inline edit, type labels, deadline, modal delete
6. Conversations can be deleted with confirmation dialog

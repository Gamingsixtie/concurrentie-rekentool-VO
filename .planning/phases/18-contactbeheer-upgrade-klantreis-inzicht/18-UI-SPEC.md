---
phase: 18
slug: contactbeheer-upgrade-klantreis-inzicht
status: draft
shadcn_initialized: false
preset: none
created: 2026-03-25
---

# Phase 18 — UI Design Contract

> Visual and interaction contract for Contactbeheer Upgrade & Klantreis-inzicht. Generated by gsd-ui-researcher, verified by gsd-ui-checker.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | none (custom Tailwind v4 tokens) |
| Preset | not applicable |
| Component library | none (custom components) |
| Icon library | inline SVG (established pattern) |
| Font | System default (inherited from app) |

---

## Spacing Scale

Declared values (must be multiples of 4):

| Token | Value | Usage |
|-------|-------|-------|
| xs | 4px | Icon gaps, badge internal padding, inline spacing |
| sm | 8px | Compact element spacing, badge gaps |
| md | 16px | Default element spacing, card padding-x, table cell padding |
| lg | 24px | Card padding (p-6), section padding |
| xl | 32px | Section gaps between timeline entries |
| 2xl | 48px | Major section breaks |
| 3xl | 64px | Not used in this phase |

Exceptions: Touch targets remain 44px minimum height (h-[44px]) for all interactive elements per MODE-03.

---

## Typography

| Role | Size | Weight | Line Height |
|------|------|--------|-------------|
| Small | 12px | 400 (regular) | 1.5 |
| Body | 14px | 400 (regular) | 1.5 |
| Heading | 20px | 600 (semibold) | 1.3 |
| Display | 28px | 600 (semibold) | 1.2 |

Weights: 400 (regular) and 600 (semibold). Use weight to distinguish hierarchy within the same size -- contact names, stat values, and labels use 14px at weight 600, body text uses 14px at weight 400. Compact indicators (DmuProgressIndicator, stagnation badges) and stat labels use 12px.

Source: Established pattern from DmuMatrix.tsx, ContactsTab.tsx, ContactCard.tsx.

---

## Color

| Role | Value | Usage |
|------|-------|-------|
| Dominant (60%) | #F8F9FA (`cito-bg`) | Page background, surfaces |
| Secondary (30%) | #FFFFFF with `border-neutral-200` | Cards, table rows, contact cards, timeline entries |
| Accent (10%) | #FF6600 (`cito-accent`) | Primary CTA buttons only ("+ Contact toevoegen", "Blokkade registreren") |
| Destructive | #DC2626 (red-600) | Delete confirmation text, "Afgehaakt" status |
| Brand primary | #003082 (`cito-primary`) | Initials avatar circle, headings, "Primair" badge |

Accent reserved for: Primary action buttons (add contact, register blokkade), never for informational badges or passive elements.

### DMU Role Colors (Phase 18 new roles)

Each new DMU role gets a unique bg/text color pair following the existing DMUBadge pattern (`bg-{color}-100 text-{color}-800`):

| DMU Role | Background | Text | Rationale |
|----------|-----------|------|-----------|
| beslisser | `bg-purple-100` | `text-purple-800` | Highest authority, migrated from `mt` |
| inkoper | `bg-green-100` | `text-green-800` | Budget role, migrated from `finance` |
| adviseur | `bg-sky-100` | `text-sky-800` | Advisory role, new distinct color |
| gebruiker | `bg-blue-100` | `text-blue-800` | Daily user, migrated from `coordinator` |
| beinvloeder | `bg-amber-100` | `text-amber-800` | Influencer role, new distinct color |
| overig | `bg-neutral-100` | `text-neutral-700` | Unchanged from current |

### Engagement Status Colors (unchanged)

Retained from EngagementBadge.tsx:

| Status | Background | Text | Border |
|--------|-----------|------|--------|
| nog-niet-benaderd | `bg-neutral-100` | `text-neutral-600` | `border-neutral-300` |
| in-gesprek | `bg-blue-50` | `text-blue-700` | `border-blue-200` |
| positief | `bg-emerald-50` | `text-emerald-700` | `border-emerald-200` |
| wacht-op-intern | `bg-amber-50` | `text-amber-700` | `border-amber-200` |
| akkoord | `bg-green-50` | `text-green-700` | `border-green-200` |
| afgehaakt | `bg-red-50` | `text-red-700` | `border-red-200` |

### Timeline Entry Type Indicators

| Type | Left-border color | Icon |
|------|-------------------|------|
| Gesprek (conversation) | `border-l-blue-400` | Chat bubble SVG |
| Blokkade (blockade) | `border-l-red-400` | Warning triangle SVG |
| Statuswijziging (system event) | `border-l-neutral-300` | Arrow right SVG |
| Eerste contact (first contact) | `border-l-green-400` | Star SVG |

---

## Focal Points

| Screen | Focal Element | Rationale |
|--------|---------------|-----------|
| ContactsTab (DMU-overzicht) | The first non-empty DMU role group header (beslisser group) | The highest-authority decision maker group is the primary reason users visit this tab -- it answers "who decides?" at first glance |
| ContactsTab (Klantreis) | The topmost timeline entry (most recent event) | Chronological recency drives action -- the latest interaction or blokkade is what the user needs to act on |
| DashboardTab | The CustomerJourneySummary card (specifically the "Huidige blokkade" cell) | The active blokkade is the single most actionable insight -- it tells the user what is currently blocking progress |

---

## Component Inventory

### New Components

| Component | Location | Purpose |
|-----------|----------|---------|
| `CustomerJourneyTimeline` | `src/features/school-profile/components/CustomerJourneyTimeline.tsx` | Chronological timeline of interactions, blokkades, status changes per school |
| `TimelineEntry` | `src/features/school-profile/components/TimelineEntry.tsx` | Single timeline row: date, contact (with DMU badge), type indicator, description |
| `BlockadeForm` | `src/features/school-profile/components/BlockadeForm.tsx` | Inline expandable form for registering a blokkade with description and optional contact reference |
| `CustomerJourneySummary` | `src/features/school-profile/components/CustomerJourneySummary.tsx` | Compact dashboard card: first contact, decision maker, current blokkade, DMU progress |
| `ContactGroupHeader` | `src/features/school-profile/components/ContactGroupHeader.tsx` | Section header for DMU role groups in contacts tab (role label, count, collapse toggle) |

### Modified Components

| Component | Changes |
|-----------|---------|
| `DMUBadge.tsx` | Update `DMU_BADGE_STYLES` map for 6 new roles instead of 4 |
| `ContactsTab.tsx` | Add view toggle (DMU-groepering / Tijdlijn), group contacts by DMU role, integrate timeline |
| `ContactCard.tsx` | No structural changes, DMUBadge automatically picks up new roles |
| `ContactForm.tsx` | Update DMU position dropdown options to new 6-role model |
| `DmuMatrix.tsx` | Add "Contactvolgorde" column (1e/2e/3e), update role rendering for new roles |
| `DmuProgressIndicator.tsx` | Update to work with new DMU role types (logic unchanged, types change) |
| `DmuMismatchBanner.tsx` | No logic changes needed, works on engagement status (unchanged) |
| `DashboardTab.tsx` | Add CustomerJourneySummary card above or beside DmuMatrix |

### Reused Components (no changes)

| Component | Reused In |
|-----------|-----------|
| `EngagementBadge.tsx` | Timeline entries, DMU matrix rows |
| `WaitingForSelect.tsx` | BlockadeForm contact reference dropdown |

---

## Layout Specifications

### ContactsTab View Toggle

Position: Between header ("Contactpersonen (N)") and content. Two-button toggle group.

```
[DMU-overzicht] [Klantreis]
```

| Property | Value |
|----------|-------|
| Toggle container | `inline-flex rounded-lg border border-neutral-200 overflow-hidden` |
| Active button | `bg-cito-primary text-white px-4 py-2 text-[14px] font-semibold` |
| Inactive button | `bg-white text-neutral-700 px-4 py-2 text-[14px] hover:bg-neutral-50` |
| Min touch target | 44px height |
| Placement | Left-aligned, mb-6 below header row |

### DMU-overzicht View (default)

Contacts grouped by DMU role in hierarchical order: beslisser, inkoper, adviseur, gebruiker, beinvloeder, overig.

Each group:
- Group header: DMU role badge (large) + count + collapse/expand chevron
- Spacing between groups: 24px (gap-6)
- Contact cards within group: existing ContactCard layout, 16px gap (gap-4)
- Empty groups: hidden entirely (do not show "Geen beslisser" empty sections)

### Klantreis View (timeline)

Vertical timeline with left-aligned date markers.

| Property | Value |
|----------|-------|
| Timeline line | `w-0.5 bg-neutral-200` vertical line, left offset 20px from container |
| Entry dot | `w-3 h-3 rounded-full` on timeline line, colored by entry type |
| Entry card | `ml-10 bg-white border border-neutral-200 rounded-lg p-4` |
| Entry spacing | 16px between entries (space-y-4) |
| Date label | `text-[12px] text-neutral-400` above entry card, formatted "d MMM yyyy" |
| Contact ref | Name + DMUBadge inline, `text-[14px] font-semibold` |
| Description | `text-[14px] text-neutral-700` |
| Blokkade highlight | `bg-red-50 border-red-200` instead of white/neutral |

### BlockadeForm (inline expandable)

Triggered by "+ Blokkade registreren" text button below the timeline.

| Property | Value |
|----------|-------|
| Container | `bg-neutral-50 border border-neutral-200 rounded-lg p-4 mt-4` |
| Description textarea | Full width, 3 rows, `text-[14px]`, placeholder: "Bijv. Wacht op budget-goedkeuring van [contactpersoon]" |
| Contact dropdown | Optional, reuses WaitingForSelect pattern, label: "Gerelateerd aan" |
| Save button | `h-[44px] px-4 bg-cito-accent text-white rounded-lg text-[14px] font-semibold`, label: "Blokkade opslaan" |
| Cancel link | `text-[14px] text-neutral-500 hover:text-neutral-700` |

### CustomerJourneySummary (dashboard card)

Placed above DmuMatrix on DashboardTab.

| Property | Value |
|----------|-------|
| Container | `bg-white border border-neutral-200 rounded-lg p-6 mb-6` |
| Title | "Klantreis-overzicht", `text-[20px] font-semibold text-neutral-900` |
| Layout | 2x2 grid on desktop (`grid grid-cols-2 gap-4`), stacked on mobile |
| Stat label | `text-[12px] text-neutral-500 uppercase tracking-wide` |
| Stat value | `text-[14px] font-semibold text-neutral-900` |

Grid cells:
1. **Eerste aanspreekpunt** — Contact name + DMUBadge
2. **Beslisser** — Contact name + EngagementBadge (or "Niet vastgelegd" in neutral-400)
3. **DMU-bereik** — "X van Y bereikt" with inline progress bar (h-2, rounded-full, bg-cito-primary fill)
4. **Huidige blokkade** — Latest blokkade description truncated to 1 line, or "Geen blokkades" in neutral-400

### DmuMatrix Contactvolgorde Column

New column added after "Naam", before "DMU-rol".

| Property | Value |
|----------|-------|
| Column header | "Nr." (short for contactvolgorde) |
| Column width | `w-[48px]` |
| Cell content | Ordinal number based on first conversation date: "1e", "2e", "3e" etc. |
| No conversation | `text-neutral-400` showing "--" |
| Font | `text-[14px] text-neutral-700` |

---

## Interaction Patterns

### View Toggle State

- Persisted via `localStorage` key `contacts-view-mode` (values: `dmu` or `timeline`)
- Default: `dmu`
- Toggle does NOT trigger API calls, just re-renders existing contact + conversation data

### Blokkade Registration Flow

1. User clicks "+ Blokkade registreren" below timeline
2. Inline form expands with slide-down animation (no modal)
3. User types description, optionally selects related contact
4. User clicks "Blokkade opslaan" -- saves as system event in conversations table
5. Form collapses, new entry appears at top of timeline
6. Cancel closes form without saving

### Contact Order Calculation

- Ordinal determined by earliest conversation date per contact
- Contacts with no conversations show "--"
- Order recalculates reactively when conversations are added/deleted

### DMU Role Migration UX

- No user-facing migration UI; migration runs automatically on first load
- After migration, user sees new role labels in existing views
- User can manually update any contact's DMU role via ContactForm

---

## Copywriting Contract

| Element | Copy |
|---------|------|
| Primary CTA (contacts) | "+ Contact toevoegen" |
| Primary CTA (blokkade) | "+ Blokkade registreren" |
| Save button (blokkade form) | "Blokkade opslaan" |
| View toggle option 1 | "DMU-overzicht" |
| View toggle option 2 | "Klantreis" |
| Empty state heading (contacts) | "Geen contactpersonen" |
| Empty state body (contacts) | "Voeg contactpersonen toe via de Contacten-tab om het DMU-beslissingsoverzicht te gebruiken." |
| Empty state heading (timeline) | "Geen klantreis-historie" |
| Empty state body (timeline) | "De klantreis wordt opgebouwd uit gesprekken en statuswijzigingen. Voeg een gesprek toe of wijzig een engagement-status om te beginnen." |
| Empty state (blokkades) | "Geen blokkades" |
| Empty state (eerste aanspreekpunt) | "Niet vastgelegd" |
| Empty state (beslisser) | "Niet vastgelegd" |
| Blokkade placeholder | "Bijv. Wacht op budget-goedkeuring van [contactpersoon]" |
| Blokkade label | "Blokkade of wachtpunt" |
| Related contact label | "Gerelateerd aan" |
| DMU progress format | "X van Y bereikt" |
| Contact order column header | "Nr." |
| Contact order cell | "1e", "2e", "3e" etc. |
| Delete contact (linked) | "Ontkoppel eerst de N gekoppelde gesprek(ken)" (existing, unchanged) |
| Dashboard summary title | "Klantreis-overzicht" |
| DmuMatrix title | "DMU-beslissingsoverzicht" (existing, unchanged) |
| Huidige blokkade label | "Huidige blokkade" |

### DMU Role Labels

| Key | Label |
|-----|-------|
| beslisser | "Beslisser" |
| inkoper | "Inkoper" |
| adviseur | "Adviseur" |
| gebruiker | "Gebruiker" |
| beinvloeder | "Beinvloeder" |
| overig | "Overig" |

---

## Responsive Behavior

### Desktop (md and up, >= 768px)

- DmuMatrix: table view with all columns including new "Nr." column
- CustomerJourneySummary: 2x2 grid layout
- ContactsTab: DMU-grouped view shows cards in single column
- Timeline: full width with date labels and entry cards

### Mobile (below md, < 768px)

- DmuMatrix: card view (existing pattern), include contactvolgorde as inline field
- CustomerJourneySummary: single column stack
- ContactsTab: same card layout, groups collapse by default
- Timeline: narrower margins, `ml-8` instead of `ml-10`
- Touch targets: all interactive elements minimum 44px height
- View toggle: full width buttons

---

## Accessibility

| Concern | Implementation |
|---------|----------------|
| Timeline semantics | `<ol>` with `role="list"`, each entry is `<li>` |
| View toggle | `role="tablist"` with `role="tab"` on each button, `aria-selected` |
| DMU badges | Badge text is visible, no additional aria needed |
| Blokkade form | `aria-expanded` on trigger button, `aria-label` on textarea |
| Progress indicator | `aria-label="DMU bereik: X van Y"` on progress bar |
| Stagnation dot | Existing `aria-label="Stagnatie gedetecteerd"` pattern maintained |
| Color contrast | All badge color pairs meet WCAG AA (verified: purple-800 on purple-100 = 7.3:1) |

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| No registries | n/a | n/a |

This phase uses only custom Tailwind v4 components. No shadcn or third-party registries.

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending

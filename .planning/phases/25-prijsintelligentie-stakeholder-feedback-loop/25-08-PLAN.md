---
phase: 25-prijsintelligentie-stakeholder-feedback-loop
plan: "08"
type: execute
wave: 5
depends_on: ["25-04", "25-05", "25-06", "25-07"]
files_modified:
  - .claude/skills/ops-competitor-intel/SKILL.md
  - .claude/skills/ops-competitor-intel/references/pricing-workflow.md
  - src/lib/supabase/types.ts
autonomous: false
requirements: ["PI-09"]

must_haves:
  truths:
    - "ops-competitor-intel skill serves as single entry point for all competitor intelligence"
    - "Skill orchestrates manual input, document upload, and AI extraction into review queue"
    - "Skill covers prices, features, and differentiators scope"
    - "Updated Supabase types reflect all new tables"
  artifacts:
    - path: ".claude/skills/ops-competitor-intel/SKILL.md"
      provides: "Skill definition with methodology and triggers"
    - path: ".claude/skills/ops-competitor-intel/references/pricing-workflow.md"
      provides: "Detailed workflow for pricing data management"
  key_links:
    - from: ".claude/skills/ops-competitor-intel/SKILL.md"
      to: "src/db/pricing-operations.ts"
      via: "references pricing CRUD operations"
      pattern: "pricing-operations"
    - from: ".claude/skills/ops-competitor-intel/SKILL.md"
      to: "api/normalize-price.ts"
      via: "references AI normalization endpoint"
      pattern: "normalize-price"
---

<objective>
Build the ops-competitor-intel skill as the single orchestrator for all competitor intelligence (prices, features, differentiators). Update Supabase generated types to include all new tables. Final integration checkpoint.

Purpose: Per D-15/D-16, the skill is the user's single entry point for managing competitor data. It orchestrates existing infrastructure (document upload parser, AI extraction, review queue) without duplicating code.
Output: Skill definition, reference documentation, updated Supabase types, final verification
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/25-prijsintelligentie-stakeholder-feedback-loop/25-CONTEXT.md
@.planning/phases/25-prijsintelligentie-stakeholder-feedback-loop/25-04-SUMMARY.md
@.planning/phases/25-prijsintelligentie-stakeholder-feedback-loop/25-07-SUMMARY.md

<interfaces>
Existing skill pattern (from .claude/skills/ops-competitor-intel/SKILL.md -- already exists as stub):
```yaml
name: ops-competitor-intel
description: ...
triggers: ["competitor intel", "update competitor data", ...]
```

From CLAUDE.md skill structure:
```
.claude/skills/{category}-{skill-name}/
  SKILL.md
  references/
  assets/
```

Infrastructure available (from earlier plans):
- api/normalize-price.ts -- AI price normalization
- src/db/pricing-operations.ts -- CRUD for all pricing tables
- src/features/review/PriceProposalModal.tsx -- UI for proposal submission
- ReviewQueuePage -- Manager review workflow
- detectDiscountPatterns -- Pattern detection engine
- document-parser.ts -- Existing document upload + AI extraction
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build ops-competitor-intel skill with methodology and references</name>
  <files>
    .claude/skills/ops-competitor-intel/SKILL.md,
    .claude/skills/ops-competitor-intel/references/pricing-workflow.md
  </files>
  <read_first>
    .claude/skills/ops-competitor-intel/SKILL.md,
    .claude/skills/ops-price-intake/SKILL.md,
    src/db/pricing-operations.ts,
    api/normalize-price.ts,
    src/lib/ai-price-normalization.ts
  </read_first>
  <action>
    Update .claude/skills/ops-competitor-intel/SKILL.md with full skill definition per D-15/D-16.

    YAML frontmatter with name ops-competitor-intel, description covering single entry point for all competitor intelligence management, triggers including competitor intel, update competitor data, check Dia pricing, JIJ! features, prijzen bijwerken, concurrentieonderzoek, nieuwe prijs invoeren, prijsvoorstel indienen, marktkorting, kortingspatroon. Negative triggers: school profile, wizard, export.

    Methodology sections (max 200 lines total):
    1. Identify Intent -- categories: update price, upload price document, check discount patterns, update features/differentiators, review proposals
    2. Route to Correct Channel: manual price update to Klopt niet in Products tab; document upload to existing document upload; free-form text to normalizePrice from src/lib/ai-price-normalization.ts then createPriceProposal; discount patterns to detectDiscountPatterns engine; feature updates to admin editor at /admin
    3. Validate and Confirm -- AI normalizes all input, show structured result in diff-view before confirming
    4. Submit to Review Queue -- all changes go through review queue per D-05/D-06, managers can self-approve
    5. Report Patterns -- after submission check if new data creates discount patterns

    Context Needs: None from brand_context (internal tool). Learnings: ops-competitor-intel section.

    Dependencies table: ops-price-intake (Optional, provides free-form text intake during calls, without it use direct modal entry).

    Create references/pricing-workflow.md with detailed workflow documentation: step-by-step for each channel, which Supabase tables are involved, which API endpoints to call, how review queue status transitions work, how discount pattern detection triggers, staleness alerts and how to resolve them. Reference src/db/pricing-operations.ts, api/normalize-price.ts, src/engine/discount-patterns.ts.
  </action>
  <verify>
    <automated>test -f ".claude/skills/ops-competitor-intel/SKILL.md" && test -f ".claude/skills/ops-competitor-intel/references/pricing-workflow.md" && wc -l < ".claude/skills/ops-competitor-intel/SKILL.md" | awk '{if ($1 <= 200) print "PASS: "$1" lines"; else print "FAIL: "$1" lines (max 200)"}' && grep -c "triggers:" ".claude/skills/ops-competitor-intel/SKILL.md" && grep -c "pricing-operations\|normalize-price\|discount-patterns" ".claude/skills/ops-competitor-intel/references/pricing-workflow.md"</automated>
  </verify>
  <acceptance_criteria>
    - .claude/skills/ops-competitor-intel/SKILL.md has YAML frontmatter with name ops-competitor-intel
    - SKILL.md contains triggers including prijsvoorstel indienen and marktkorting
    - SKILL.md methodology references PriceProposalModal, normalizePrice, detectDiscountPatterns
    - SKILL.md is under 200 lines
    - SKILL.md contains required trigger phrases from CLAUDE.md Skill Registry
    - references/pricing-workflow.md exists with step-by-step workflow
    - references/pricing-workflow.md references pricing-operations.ts, normalize-price.ts, discount-patterns.ts
  </acceptance_criteria>
  <done>ops-competitor-intel skill serves as single orchestrator for all competitor intelligence channels</done>
</task>

<task type="auto">
  <name>Task 2: Regenerate Supabase types and run full test suite</name>
  <files>
    src/lib/supabase/types.ts
  </files>
  <read_first>
    src/lib/supabase/types.ts
  </read_first>
  <action>
    Update Supabase generated types. Run npx supabase gen types typescript --local to regenerate src/lib/supabase/types.ts if Supabase CLI is available. If CLI not available, manually add type definitions for the 4 new tables (publication_prices, pricing_configs, price_proposals, price_audit_log) to the existing types file following the exact pattern of existing table types. Ensure all columns from migrations 009-012 are represented. Add the new tables to the Database interface under public.Tables.

    Run full verification: npm run build must succeed with no type errors, npx vitest run full test suite must pass. Check that no existing functionality is broken.
  </action>
  <verify>
    <automated>npm run build && npx vitest run --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - src/lib/supabase/types.ts contains publication_prices table definition
    - src/lib/supabase/types.ts contains pricing_configs table definition
    - src/lib/supabase/types.ts contains price_proposals table definition
    - src/lib/supabase/types.ts contains price_audit_log table definition
    - npm run build succeeds with exit code 0
    - npx vitest run all tests pass
  </acceptance_criteria>
  <done>Supabase types updated, full build and test suite green</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Final Phase 25 end-to-end verification</name>
  <action>Human verifies the complete Phase 25 implementation including pricing data loading, offline fallback, proposal flow, review queue, admin editor, discount patterns, and navigation badge.</action>
  <what-built>Complete Phase 25: Database-driven pricing with review workflow, discount pattern detection, admin config editor, AI normalization, ops-competitor-intel skill, offline fallback</what-built>
  <how-to-verify>
    1. Start dev server: npm run dev
    2. Pricing data loading: Open app, check browser DevTools Network tab for Supabase calls to publication_prices and pricing_configs tables
    3. Offline fallback: Disable network in DevTools, refresh -- app should show OfflinePriceBanner with cached data
    4. Proposal flow: Navigate to a school Products tab, click Klopt niet next to a price, fill in and submit proposal
    5. Review queue: Log in as manager, navigate to /review, see submitted proposal, approve it
    6. Auto-recalculation: After approval, check that comparison view reflects the new price
    7. Admin editor: Navigate to /admin, view Cito pricing config, verify form renders with current values
    8. Discount patterns: In comparison view, check for MarktKortingToggle (may be disabled if insufficient data)
    9. Badge: Check navigation shows review badge with open proposal count
    10. Build: Run npm run build -- must succeed
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- Full build passes: npm run build
- Full test suite passes: npx vitest run
- ops-competitor-intel skill registered in CLAUDE.md
- Supabase types include all 4 new tables
- All 10 success criteria from ROADMAP can be verified
</verification>

<success_criteria>
- ops-competitor-intel skill is the single entry point for competitor data (D-15, D-16)
- Supabase types match all migration schemas
- Full build and test suite green
- End-to-end flow verified by user
</success_criteria>

<output>
After completion, create `.planning/phases/25-prijsintelligentie-stakeholder-feedback-loop/25-08-SUMMARY.md`
</output>

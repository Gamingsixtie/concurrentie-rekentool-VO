---
phase: 25-prijsintelligentie-stakeholder-feedback-loop
plan: 04
type: execute
wave: 3
depends_on: [25-02, 25-03]
files_modified:
  - src/features/review/ReviewQueuePage.tsx
  - src/features/review/ReviewQueueItem.tsx
  - src/features/review/ReviewFilterBar.tsx
  - src/components/ui/ReviewBadgeCounter.tsx
  - src/router/routes.ts
  - src/router/__tests__/routes.test.ts
  - src/features/auth/NavigationBar.tsx
  - src/features/pricing/__tests__/review-queue.test.tsx
  - src/engine/__tests__/recalculation.test.ts
  - src/db/__tests__/audit-trail.test.ts
autonomous: false
requirements: [PI-05]

must_haves:
  truths:
    - "Manager can see all open price proposals in a filterable list at /review"
    - "Manager can approve a proposal with one click and the publication price updates immediately"
    - "Manager can reject a proposal with a required reason"
    - "Navigation shows badge with open proposal count for managers only"
    - "Non-managers cannot access the /review page"
    - "Approved price triggers React Query invalidation for automatic recalculation"
  artifacts:
    - path: "src/features/review/ReviewQueuePage.tsx"
      provides: "Manager-only review queue page"
    - path: "src/features/review/ReviewQueueItem.tsx"
      provides: "Expandable proposal row with approve/reject actions"
    - path: "src/components/ui/ReviewBadgeCounter.tsx"
      provides: "Red circle badge showing open proposal count"
    - path: "src/router/routes.ts"
      provides: "New /review route for managers"
  key_links:
    - from: "src/features/review/ReviewQueuePage.tsx"
      to: "src/hooks/usePriceProposals.ts"
      via: "usePriceProposals() hook with filters"
      pattern: "usePriceProposals"
    - from: "src/features/review/ReviewQueueItem.tsx"
      to: "src/db/pricing-operations.ts"
      via: "approveProposal() and rejectProposal()"
      pattern: "approveProposal|rejectProposal"
    - from: "src/router/routes.ts"
      to: "src/features/review/ReviewQueuePage.tsx"
      via: "TanStack Router route definition"
      pattern: "reviewRoute"
    - from: "src/components/ui/ReviewBadgeCounter.tsx"
      to: "src/hooks/usePriceProposals.ts"
      via: "useOpenProposalCount()"
      pattern: "useOpenProposalCount"
---

<objective>
Build the review queue page for managers: filterable list of price proposals, approve/reject workflow with audit trail, navigation badge showing open count, and route registration.

Purpose: Per D-06/D-07/D-08/D-09, managers need a central place to review and act on price proposals. Approval immediately activates the new price and triggers recalculation.
Output: Review queue page, filter bar, expandable items with actions, navigation badge, route, tests
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/25-prijsintelligentie-stakeholder-feedback-loop/25-CONTEXT.md
@.planning/phases/25-prijsintelligentie-stakeholder-feedback-loop/25-UI-SPEC.md
@.planning/phases/25-prijsintelligentie-stakeholder-feedback-loop/25-01-SUMMARY.md
@.planning/phases/25-prijsintelligentie-stakeholder-feedback-loop/25-03-SUMMARY.md

<interfaces>
From src/hooks/usePriceProposals.ts (Plan 03):
```typescript
export function usePriceProposals(filters?: { status?: string; provider?: string; moduleId?: string });
export function useOpenProposalCount();
export function useCreateProposal();
```

From src/db/pricing-operations.ts (Plan 01):
```typescript
export function approveProposal(proposalId: string): Promise<void>;
export function rejectProposal(proposalId: string, reason: string): Promise<void>;
export function fetchAuditLog(entityType: string, entityId: string): Promise<PriceAuditEntry[]>;
```

From src/components/ui/ProposalBadge.tsx (Plan 03):
```typescript
// Props: status: 'open' | 'approved' | 'rejected'
```

From src/components/ui/PriceDiffDisplay.tsx (Plan 03):
```typescript
// Props: oldPrice: number, newPrice: number
```

From src/features/auth/AuthProvider.tsx:
```typescript
export function useAuth(): { user, userProfile: { role: 'accountmanager' | 'manager' | 'viewer' }, loading, signOut }
```

From src/router/routes.ts:
```typescript
export const routeTree = rootRoute.addChildren([
  loginRoute, indexRoute, scholenRoute,
  schoolRoute.addChildren([...])
]);
// New reviewRoute must be added at top level (not under schoolRoute)
```

From src/features/school-profile/components/ConfirmDialog.tsx:
```typescript
// Reusable confirmation dialog with configurable cancel labels
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create ReviewQueuePage with filter bar, expandable items, and approve/reject flow</name>
  <files>
    src/features/review/ReviewQueuePage.tsx,
    src/features/review/ReviewQueueItem.tsx,
    src/features/review/ReviewFilterBar.tsx,
    src/features/pricing/__tests__/review-queue.test.tsx,
    src/engine/__tests__/recalculation.test.ts,
    src/db/__tests__/audit-trail.test.ts
  </files>
  <read_first>
    src/features/school-overview/SchoolOverviewPage.tsx,
    src/features/school-profile/components/PriceHistoryList.tsx,
    src/features/school-profile/components/ConfirmDialog.tsx,
    src/hooks/usePriceProposals.ts,
    src/db/pricing-operations.ts,
    src/db/pricing-types.ts,
    src/features/auth/AuthProvider.tsx,
    src/features/pricing/__tests__/review-queue.test.tsx,
    src/engine/__tests__/recalculation.test.ts,
    src/db/__tests__/audit-trail.test.ts
  </read_first>
  <behavior>
    - Test 1 (review-queue): ReviewQueuePage shows "Geen toegang" for non-manager role
    - Test 2 (review-queue): ReviewQueuePage renders proposal list with ProposalBadge and PriceDiffDisplay
    - Test 3 (review-queue): Filter bar filters proposals by status and provider
    - Test 4 (review-queue): Approve button calls approveProposal and invalidates queries
    - Test 5 (review-queue): Reject button requires reason text before submission
    - Test 6 (recalculation): approveProposal triggers usePricingDataStore.loadFromSupabase() and query invalidation
    - Test 7 (audit-trail): approveProposal creates audit log entry with old_value and new_value
  </behavior>
  <action>
    **src/features/review/ReviewFilterBar.tsx:**
    - Filter bar with pill-based filters per UI-SPEC
    - Filter options: Provider (cito/dia/jij/saqi), Status (open/goedgekeurd/afgewezen)
    - Active pill uses `bg-[#003082] text-white` (cito-primary), inactive uses `border border-neutral-200`
    - Multiple filters combine with AND
    - Props: `filters: { status?: string; provider?: string }`, `onFilterChange: (filters) => void`

    **src/features/review/ReviewQueueItem.tsx:**
    - Expandable row showing: provider pill, module name, PriceDiffDisplay (current -> proposed), submitter name, date, ProposalBadge
    - Click row to expand: shows full toelichting, evidence link (if present), audit trail entries via `fetchAuditLog('price_proposal', proposal.id)`
    - Action buttons (only for status === 'open'):
      - "Goedkeuren" button (bg-[#003082] text-white) -- calls `approveProposal(proposal.id)`. No confirmation dialog per UI-SPEC (low-risk, reversible).
      - "Afwijzen" button (bg-red-600 text-white) -- opens inline textarea for rejection reason (required, min 10 chars). Confirm with "Afwijzen" button.
    - On approve success: toast "Prijs bijgewerkt -- vergelijkingen worden automatisch herberekend", invalidate queries `['publication-prices']` and `['price-proposals']`, trigger `usePricingDataStore.getState().loadFromSupabase()` for store refresh
    - On reject success: toast "Voorstel afgewezen -- de indiener wordt geinformeerd"
    - Use React Query `useMutation` for approve/reject with `onSuccess` invalidation

    **src/features/review/ReviewQueuePage.tsx:**
    - Manager-only page per D-07
    - Check `useAuth().userProfile?.role === 'manager'` -- if not manager, show "Geen toegang" message
    - Uses `usePriceProposals(filters)` with filters from ReviewFilterBar
    - Default sort: newest first (created_at DESC)
    - Layout per UI-SPEC wireframe: filter bar at top, table with columns
    - Empty state (no proposals): "Geen openstaande voorstellen" heading + body text from UI-SPEC copywriting contract
    - Empty state (no filter results): "Geen resultaten" heading + body text from UI-SPEC
    - Error state: "Prijsvoorstellen konden niet worden geladen. Controleer uw internetverbinding en probeer het opnieuw."
    - Loading state: skeleton rows

    **Update test stubs** -- replace todo stubs in review-queue.test.tsx, recalculation.test.ts, and audit-trail.test.ts with actual tests. Use `@testing-library/react` for component tests, mock `useAuth`, `usePriceProposals`, `approveProposal`, `rejectProposal`.
  </action>
  <verify>
    <automated>npx vitest run src/features/pricing/__tests__/review-queue.test.tsx src/engine/__tests__/recalculation.test.ts src/db/__tests__/audit-trail.test.ts -x</automated>
  </verify>
  <acceptance_criteria>
    - src/features/review/ReviewQueuePage.tsx contains `role === 'manager'` check
    - src/features/review/ReviewQueuePage.tsx contains "Geen openstaande voorstellen" (empty state)
    - src/features/review/ReviewQueueItem.tsx contains "Goedkeuren" and "Afwijzen" buttons
    - src/features/review/ReviewQueueItem.tsx contains "Prijs bijgewerkt" toast text
    - src/features/review/ReviewQueueItem.tsx imports `approveProposal` and `rejectProposal`
    - src/features/review/ReviewQueueItem.tsx calls `loadFromSupabase()` on approve success
    - src/features/review/ReviewFilterBar.tsx contains `bg-[#003082]` for active filter pills
    - review-queue, recalculation, and audit-trail tests pass
  </acceptance_criteria>
  <done>Review queue page shows filterable list of proposals, managers can approve (one click) or reject (with reason), and approval triggers automatic price update and recalculation. Tests verify behavior.</done>
</task>

<task type="auto">
  <name>Task 2: Add /review route and ReviewBadgeCounter to navigation</name>
  <files>
    src/router/routes.ts,
    src/router/__tests__/routes.test.ts,
    src/components/ui/ReviewBadgeCounter.tsx,
    src/features/auth/NavigationBar.tsx
  </files>
  <read_first>
    src/router/routes.ts,
    src/router/__tests__/routes.test.ts,
    src/components/ui/ReviewBadgeCounter.tsx,
    src/features/auth/AuthProvider.tsx
  </read_first>
  <action>
    **src/router/routes.ts:**
    - Add new route:
      ```typescript
      export const reviewRoute = createRoute({
        getParentRoute: () => rootRoute,
        path: '/review',
        component: lazy(() => import('@/features/review/ReviewQueuePage')),
      });
      ```
    - Add to routeTree: `rootRoute.addChildren([loginRoute, indexRoute, scholenRoute, reviewRoute, schoolRoute.addChildren([...])])`
    - Add to ROUTE_PATHS: `review: '/review'`

    **src/router/__tests__/routes.test.ts:**
    - Add assertion: `expect(paths).toContain('/review')`

    **src/components/ui/ReviewBadgeCounter.tsx:**
    - Per UI-SPEC: red circle badge with white text, 20px diameter
    - Uses `useOpenProposalCount()` hook
    - Shows count only when > 0
    - Positioned inline next to "Review" nav item text
    - Styling: `bg-red-500 text-white text-xs font-bold rounded-full w-5 h-5 flex items-center justify-center`
    - Tooltip: "[N] openstaande voorstellen" (from UI-SPEC copywriting contract)

    **Navigation bar** (find the actual nav component -- likely in `src/features/auth/` or `src/components/layout/`):
    - Add "Review" nav item with ReviewBadgeCounter
    - Only visible when `useAuth().userProfile?.role === 'manager'`
    - Links to `/review` route
    - Position: after existing nav items, before user menu
  </action>
  <verify>
    <automated>npx vitest run src/router/__tests__/routes.test.ts -x</automated>
  </verify>
  <acceptance_criteria>
    - src/router/routes.ts contains `path: '/review'`
    - src/router/routes.ts contains `reviewRoute` in routeTree
    - src/router/__tests__/routes.test.ts contains `expect(paths).toContain('/review')`
    - src/components/ui/ReviewBadgeCounter.tsx imports `useOpenProposalCount`
    - src/components/ui/ReviewBadgeCounter.tsx contains `bg-red-500` for badge styling
    - src/components/ui/ReviewBadgeCounter.tsx contains "openstaande voorstellen" tooltip text
    - Navigation component shows "Review" link only for managers
    - Route test passes
  </acceptance_criteria>
  <done>/review route registered, navigation shows Review link with badge counter for managers only</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify review queue workflow end-to-end</name>
  <action>Human verifies the complete review queue workflow including proposal submission, manager review, and navigation badge.</action>
  <what-built>Complete review queue workflow: proposal submission modal, manager review page with approve/reject, navigation badge, /review route</what-built>
  <how-to-verify>
    1. Start dev server: `npm run dev`
    2. Log in as accountmanager role
    3. Navigate to a school's Products tab -- look for "Klopt niet?" link next to a publication price (this link will be wired in a later plan if not yet present -- skip if not visible)
    4. Log in as manager role
    5. Check navigation bar -- should show "Review" link with badge counter
    6. Navigate to /review -- should see the review queue page
    7. Filter by provider and status -- filters should work
    8. If proposals exist: expand one, verify approve/reject buttons work
    9. As non-manager: navigate to /review -- should see "Geen toegang" message
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- `npx vitest run src/features/pricing/__tests__/review-queue.test.tsx src/engine/__tests__/recalculation.test.ts src/db/__tests__/audit-trail.test.ts src/router/__tests__/routes.test.ts -x` passes
- `/review` route accessible by managers, blocked for other roles
- Review queue shows proposals with correct status badges
- Approve triggers publication price update + React Query invalidation
- Reject requires reason text
- Badge shows open proposal count in navigation
- `npm run build` passes
</verification>

<success_criteria>
- Managers have a dedicated review page at /review (D-07)
- Managers can approve (immediate activation per D-08) or reject proposals (D-06)
- Navigation badge shows open proposal count (D-09)
- Non-managers cannot access review page
</success_criteria>

<output>
After completion, create `.planning/phases/25-prijsintelligentie-stakeholder-feedback-loop/25-04-SUMMARY.md`
</output>

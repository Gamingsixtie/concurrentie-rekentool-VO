---
phase: 22-architectuur-testen-productie-readiness
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - vitest.config.ts
  - src/engine/__tests__/analyze-schoolplan.test.ts
  - src/features/price-comparison/__tests__/ComparisonChart.test.tsx
autonomous: true
requirements:
  - SC-01
  - SC-05
must_haves:
  truths:
    - "Vitest excludes .claude/worktrees/ from test discovery"
    - "npx vitest run reports 0 failing tests"
    - "Coverage reporting works with v8 provider"
    - "No todo/placeholder tests remain in the codebase — all are implemented or deleted"
    - "All 42 skipped test suites reviewed — valid ones activated, obsolete ones deleted (per D-08)"
  artifacts:
    - path: "vitest.config.ts"
      provides: "Worktree exclusion + coverage config"
      contains: ".claude/worktrees"
    - path: "vitest.config.ts"
      provides: "Coverage v8 provider configuration"
      contains: "provider: 'v8'"
  key_links:
    - from: "vitest.config.ts"
      to: "test runs"
      via: "exclude pattern"
      pattern: "exclude.*worktrees"
---

<objective>
Fix the test environment: exclude worktree duplicates, add coverage reporting, fix the 2 genuine failing tests, triage all 158 todo/placeholder tests, and review all 42 skipped test suites per D-08.

Purpose: A clean, zero-failure test baseline is required before expanding coverage or adding E2E tests. Per D-07, D-02, and D-08.
Output: Clean vitest config with coverage, 0 failing tests, 0 dead todo tests, 0 unjustified skipped suites.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-architectuur-testen-productie-readiness/22-CONTEXT.md
@.planning/phases/22-architectuur-testen-productie-readiness/22-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix vitest config — worktree exclusion + coverage reporting</name>
  <files>vitest.config.ts, package.json</files>
  <read_first>
    - vitest.config.ts (current config state)
    - package.json (current devDependencies)
    - .planning/phases/22-architectuur-testen-productie-readiness/22-RESEARCH.md (Pattern 1: coverage config)
  </read_first>
  <action>
1. Install @vitest/coverage-v8 as devDependency: `npm install -D @vitest/coverage-v8`

2. Update vitest.config.ts to add worktree exclusion and coverage config:
   - Add `exclude: ['**/node_modules/**', '**/.claude/worktrees/**']` to the `test` block
   - Add coverage configuration block inside `test`:
     ```
     coverage: {
       provider: 'v8',
       reporter: ['text', 'text-summary', 'lcov'],
       include: ['src/**/*.{ts,tsx}'],
       exclude: [
         'src/**/*.test.{ts,tsx}',
         'src/**/__tests__/**',
         'src/**/*.d.ts',
         'src/test/**',
         'src/vite-env.d.ts',
       ],
       thresholds: {
         statements: 60,
         branches: 50,
         functions: 55,
         lines: 60,
       },
     },
     ```
   Note: thresholds are conservative initially (60/50/55/60) — will be raised after coverage expansion in plans 03/04.

3. Run `npx vitest run` to verify worktree exclusion works (should drop from ~305 to ~79 test files discovered).
  </action>
  <verify>
    <automated>npx vitest run --reporter=verbose 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - vitest.config.ts contains `exclude: ['**/node_modules/**', '**/.claude/worktrees/**']`
    - vitest.config.ts contains `provider: 'v8'`
    - vitest.config.ts contains `thresholds`
    - package.json devDependencies contains `@vitest/coverage-v8`
    - `npx vitest run` discovers ~79 test files (not ~305)
    - `npx vitest run` shows 0 skipped suites from worktree paths
  </acceptance_criteria>
  <done>Vitest excludes worktrees, coverage v8 is configured, test discovery is clean</done>
</task>

<task type="auto">
  <name>Task 2: Fix genuine failing tests (2 remaining after worktree exclusion)</name>
  <files>api/__tests__/analyze-schoolplan.test.ts, src/features/price-comparison/__tests__/ComparisonChart.test.tsx</files>
  <read_first>
    - api/__tests__/analyze-schoolplan.test.ts (see why it fails — likely VERCEL_ENV/SKIP_AUTH guard outdated after Phase 13)
    - src/features/price-comparison/__tests__/ComparisonChart.test.tsx (see responsive container height assertion)
    - api/analyze-schoolplan.ts (current implementation for auth guard behavior)
    - src/features/price-comparison/components/ComparisonChart.tsx (current component for height behavior)
  </read_first>
  <action>
**Context:** Per RESEARCH.md (Pitfall 2), 8 of the 11 originally failing tests come from worktree paths. Task 1 excludes worktrees from vitest, which eliminates those 8 failures. Only 2 genuine failures remain in the main source after Task 1 is applied. This task fixes those 2.

**Step 0 — Discovery:** After Task 1 is complete, run `npx vitest run 2>&1 | grep FAIL` to confirm exactly which tests still fail. Expected: only the 2 files listed below. If additional failures appear, diagnose and fix them too.

**Step 1 — Fix `api/__tests__/analyze-schoolplan.test.ts`:**
   - Read the current implementation of `api/analyze-schoolplan.ts` to understand the actual auth guard behavior after Phase 13 changes
   - The test likely expects a VERCEL_ENV guard that was modified — update the test assertions to match the current implementation
   - If the auth guard behavior changed (e.g., SKIP_AUTH check instead of VERCEL_ENV), update the test to reflect that

**Step 2 — Fix `src/features/price-comparison/__tests__/ComparisonChart.test.tsx`:**
   - Read the current ComparisonChart component to find the actual container height value/behavior
   - The test likely asserts a specific height (e.g., `height: 400`) that was changed in a UI update
   - Update the test assertion to match the current rendered height

**Step 3 — Verify:** Run `npx vitest run` — must report 0 failures. All 11 original failures are now resolved: 8 eliminated by worktree exclusion (Task 1), 2 fixed here.
  </action>
  <verify>
    <automated>npx vitest run 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run` exits with code 0
    - Output contains "0 failed" or no "FAIL" lines
    - api/__tests__/analyze-schoolplan.test.ts passes
    - src/features/price-comparison/__tests__/ComparisonChart.test.tsx passes
    - All 11 original failures accounted for: 8 via worktree exclusion, 2 fixed here, verified by 0-failure run
  </acceptance_criteria>
  <done>All tests pass with 0 failures — clean baseline established (per D-07)</done>
</task>

<task type="auto">
  <name>Task 3: Triage todo/placeholder tests (D-02) and skipped test suites (D-08)</name>
  <files>Multiple test files across src/</files>
  <read_first>
    - Run `npx vitest run 2>&1 | grep -i "todo"` to identify which files contain todo tests
    - .planning/phases/22-architectuur-testen-productie-readiness/22-CONTEXT.md (D-02: implement critical paths, delete the rest; D-08: review 42 skipped suites)
  </read_first>
  <action>
**Part A — Per D-02: triage the 158 todo/placeholder tests.**

1. Find all todo tests: search for `it.todo(`, `test.todo(`, `xit(`, `xtest(` across `src/` (exclude `.claude/worktrees/`)

2. For each todo test, classify as IMPLEMENT or DELETE:

   IMPLEMENT (critical paths — per D-02):
   - Engine calculation edge cases (price-comparison, current-vs-proposed, migration, sensitivity engines)
   - Store actions that modify state (useSchoolProfileStore, usePriceComparisonStore)
   - Wizard flow step transitions
   - Any todo that tests a behavior currently untested and critical for correctness

   DELETE (non-critical or superseded):
   - Duplicate todos (same behavior tested elsewhere)
   - Todos for features that changed significantly since the todo was written
   - UI-only todos (will be covered by RTL tests in Plan 04)
   - Todos with vague descriptions that don't map to specific behavior

3. Implement the IMPLEMENT todos: write the actual test body with meaningful assertions. Follow existing test patterns in `src/engine/__tests__/` for pure function tests.

4. Delete the DELETE todos: remove the `it.todo()` / `test.todo()` lines entirely.

Target: implement ~30-50 critical todos, delete ~100-120 non-critical ones. A clean suite is more valuable than 158 hollow placeholders.

**Part B — Per D-08: triage all 42 skipped test suites.**

5. After worktree exclusion is applied (Task 1), find all remaining skipped tests in `src/`: search for `test.skip(`, `it.skip(`, `describe.skip(`, `xit(`, `xtest(` across `src/` (exclude `.claude/worktrees/`).

6. For each skipped test or suite, assess WHY it was skipped:
   - Was it skipped due to a bug that has since been fixed? -> ACTIVATE (remove `.skip`)
   - Was it skipped because the feature changed and the test is now obsolete? -> DELETE the test entirely
   - Was it skipped because it needs a dependency not available in test env (e.g., real Supabase)? -> Keep skipped but add a comment explaining why, or convert to a proper integration test if feasible

7. After activating tests, run `npx vitest run` to confirm they pass. If an activated test fails, fix it or re-assess whether it should be deleted.

8. After cleanup, run `npx vitest run` — must report 0 todo tests and 0 unjustified skipped tests remaining.
  </action>
  <verify>
    <automated>bash -c "npx vitest run 2>&1 | grep -i 'todo\|skipped' | head -5; echo '---'; grep -r 'test\.skip\|it\.skip\|describe\.skip\|it\.todo\|test\.todo' src/ --include='*.ts' --include='*.tsx' -l 2>/dev/null | grep -v '.claude/worktrees' | head -10"</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run` output shows "0 todo" (or no todo line at all)
    - No `it.todo(` or `test.todo(` calls remain in `src/` (outside `.claude/worktrees/`)
    - No unjustified `test.skip(` / `it.skip(` / `describe.skip(` calls remain — each remaining skip has a comment explaining why
    - `npx vitest run` still passes with 0 failures
    - Total passing test count is higher than 2232 (new tests from implemented todos + activated skips)
  </acceptance_criteria>
  <done>All 158 todo tests triaged (D-02) and all 42 skipped suites reviewed (D-08) — critical paths implemented, obsolete tests deleted, justified skips documented.</done>
</task>

</tasks>

<verification>
- `npx vitest run` exits 0 with 0 failures, 0 todos, 0 unjustified skipped suites, 0 worktree pollution
- `npx vitest run --coverage` produces coverage report with v8 provider
- Test file count is ~79 (main source only, no worktree duplicates)
- No `test.skip`/`it.skip`/`describe.skip` without justification comment
</verification>

<success_criteria>
- Zero failing tests
- Zero todo/placeholder tests
- All skipped suites reviewed — activated, deleted, or justified (per D-08)
- Coverage reporting functional with v8 provider
- Worktree pollution eliminated from test discovery
</success_criteria>

<output>
After completion, create `.planning/phases/22-architectuur-testen-productie-readiness/22-01-SUMMARY.md`
</output>

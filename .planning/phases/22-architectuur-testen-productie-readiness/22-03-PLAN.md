---
phase: 22-architectuur-testen-productie-readiness
plan: 03
type: execute
wave: 2
depends_on: ["22-01"]
files_modified:
  - src/hooks/__tests__/useSchool.test.ts
  - src/hooks/__tests__/useSchoolPrices.test.ts
  - src/hooks/__tests__/useContacts.test.ts
  - src/hooks/__tests__/useConversations.test.ts
  - src/hooks/__tests__/useActions.test.ts
  - src/hooks/__tests__/useSchoolplanAnalysis.test.ts
  - src/hooks/__tests__/useSwitchingCosts.test.ts
  - src/hooks/__tests__/useTeam.test.ts
  - src/stores/__tests__/school-profile-store.test.ts
  - src/stores/__tests__/price-comparison-store.test.ts
  - src/lib/__tests__/formatting.test.ts
autonomous: true
requirements:
  - SC-01
must_haves:
  truths:
    - "All React hooks in src/hooks/ have unit tests"
    - "Both Zustand stores have comprehensive tests for key actions"
    - "Untested utility functions in src/lib/ are covered"
  artifacts:
    - path: "src/hooks/__tests__/"
      provides: "Hook test files"
      min_lines: 20
    - path: "src/stores/__tests__/"
      provides: "Store test files"
      min_lines: 30
  key_links:
    - from: "src/hooks/__tests__/"
      to: "src/hooks/"
      via: "test imports"
      pattern: "import.*from.*hooks/"
---

<objective>
Add unit tests for untested hooks, stores, and utility functions — closing the coverage gaps identified in research.

Purpose: Per D-01, priority is coverage gaps in stores, hooks, and utils. Engine tests are already strong. This plan covers the non-UI code that lacks tests.
Output: Test files for all 8 untested hooks, 2 store test expansions, and utility coverage.

Note: This plan depends on Plan 01 (wave 2) because Plan 01 applies the vitest.config.ts worktree exclusion. Without it, `npx vitest run src/hooks/` may discover and run stale worktree copies causing false failures.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/22-architectuur-testen-productie-readiness/22-CONTEXT.md
@.planning/phases/22-architectuur-testen-productie-readiness/22-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Unit tests for all untested React hooks</name>
  <files>
    src/hooks/__tests__/useSchool.test.ts,
    src/hooks/__tests__/useSchoolPrices.test.ts,
    src/hooks/__tests__/useContacts.test.ts,
    src/hooks/__tests__/useConversations.test.ts,
    src/hooks/__tests__/useActions.test.ts,
    src/hooks/__tests__/useSchoolplanAnalysis.test.ts,
    src/hooks/__tests__/useSwitchingCosts.test.ts,
    src/hooks/__tests__/useTeam.test.ts
  </files>
  <read_first>
    - src/hooks/ (list all hook files to identify which are untested)
    - src/hooks/__tests__/ (see which tests already exist — 3 files per research)
    - src/hooks/useSchool.ts (understand hook signature and return type)
    - src/hooks/useSchoolPrices.ts (understand hook behavior)
    - src/lib/supabase/operations.ts (hooks likely wrap these operations)
    - src/test/setup.ts (test setup patterns)
  </read_first>
  <action>
For each untested hook in `src/hooks/`, create a corresponding test file in `src/hooks/__tests__/`.

General pattern for hooks that wrap React Query + Supabase:
- Mock `@/lib/supabase/operations` functions with `vi.mock()`
- Use `renderHook` from `@testing-library/react` with a QueryClientProvider wrapper
- Test: initial loading state, successful data return, error handling, refetch behavior

For each hook, test at minimum:
1. Returns loading=true initially
2. Returns data on success
3. Returns error on failure
4. Handles empty/null responses gracefully

Mock Supabase operations at the module level — do NOT mock the Supabase client directly. Follow the pattern of existing hook tests.

If a hook has mutations (create, update, delete), also test:
5. Mutation calls the correct operation function
6. Mutation invalidates the correct query key

Run `npx vitest run src/hooks/` after creating each test file to verify it passes.
  </action>
  <behavior>
    - useSchool(id): returns school record or undefined, loading state, error on missing school
    - useSchoolPrices(schoolId): returns price records array, empty array for new school
    - useContacts(schoolId): returns contacts array, handles school with no contacts
    - useConversations(schoolId): returns conversations sorted by date
    - useActions(schoolId): returns action items
    - useSchoolplanAnalysis(schoolId): returns analysis data or null
    - useSwitchingCosts(schoolId): returns switching cost config
    - useTeam(): returns team members array
  </behavior>
  <verify>
    <automated>npx vitest run src/hooks/ 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - At least 8 new test files exist in src/hooks/__tests__/
    - Each test file has at least 3 test cases (loading, success, error)
    - `npx vitest run src/hooks/` reports 0 failures
    - All new tests import from the actual hook file they test
  </acceptance_criteria>
  <done>All hooks in src/hooks/ have unit tests covering loading, success, and error states</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Expand store tests + cover untested utils</name>
  <files>
    src/stores/__tests__/school-profile-store.test.ts,
    src/stores/__tests__/price-comparison-store.test.ts,
    src/lib/__tests__/formatting.test.ts
  </files>
  <read_first>
    - src/stores/ (list all store files)
    - src/stores/__tests__/ (existing store tests — identify gaps)
    - src/stores/school-profile-store.ts (or similar — store actions to test)
    - src/stores/price-comparison-store.ts (or similar)
    - src/lib/ (list all utility files)
    - src/lib/__tests__/ (existing util tests — identify gaps)
  </read_first>
  <action>
1. **Store tests expansion**: For each Zustand store:
   - Read the store file to identify all actions/selectors
   - Check existing test coverage — add tests for uncovered actions
   - Key patterns to test:
     - State initialization (default values)
     - Each action modifies state correctly
     - Derived selectors compute correctly
     - Persist middleware round-trips correctly (serialize -> deserialize)
     - Edge cases: empty arrays, null values, boundary conditions

   For `useSchoolProfileStore`:
   - setSelectedModules syncs moduleSetups automatically
   - Level/student count changes
   - Scenario selection (A, B, C)
   - Module setup configuration

   For `usePriceComparisonStore`:
   - initialize() from school profile
   - Draft vs applied overrides
   - recalculate() with overrides
   - Price override CRUD

2. **Utility tests**: scan `src/lib/` for untested utility files. For each:
   - Create or expand test file in `src/lib/__tests__/`
   - Test edge cases: empty input, null, undefined, boundary values
   - Formatting utils: test Dutch locale formatting, edge numbers

3. Run full test suite to verify no regressions.
  </action>
  <behavior>
    - Store init: default state matches expected shape
    - setSelectedModules: updates modules AND syncs moduleSetups
    - recalculate: produces correct totals after override
    - Formatting: Dutch locale number formatting, currency, percentage
  </behavior>
  <verify>
    <automated>npx vitest run src/stores/ src/lib/ 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - Store test files exist in src/stores/__tests__/ for both main stores
    - Each store test covers at minimum: initialization, 3+ actions, 1+ edge case
    - No untested utility files remain in src/lib/ (each .ts file has a matching test)
    - `npx vitest run src/stores/ src/lib/` reports 0 failures
  </acceptance_criteria>
  <done>Both Zustand stores have comprehensive tests, all utility functions are covered</done>
</task>

</tasks>

<verification>
- `npx vitest run src/hooks/ src/stores/ src/lib/` passes with 0 failures
- Total test count increased significantly from baseline (~2232)
- All hooks, stores, and utils have test coverage
</verification>

<success_criteria>
- Every hook in src/hooks/ has a test file with loading/success/error cases
- Both main Zustand stores have comprehensive action tests
- All utility functions in src/lib/ have edge case coverage
</success_criteria>

<output>
After completion, create `.planning/phases/22-architectuur-testen-productie-readiness/22-03-SUMMARY.md`
</output>

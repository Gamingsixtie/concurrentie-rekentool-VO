---
phase: 22-architectuur-testen-productie-readiness
plan: 04
type: execute
wave: 2
depends_on: ["22-01"]
files_modified:
  - src/features/school-profile/components/__tests__/WizardShell.test.tsx
  - src/features/school-profile/components/__tests__/WizardStep1.test.tsx
  - src/features/school-profile/components/__tests__/WizardStep2.test.tsx
  - src/features/school-profile/components/__tests__/WizardStep3.test.tsx
  - src/features/school-profile/components/__tests__/WizardStep4.test.tsx
  - src/features/school-profile/components/__tests__/WizardStep5.test.tsx
  - src/features/school-overview/components/__tests__/SchoolCard.test.tsx
  - src/features/school-overview/components/__tests__/SchoolPickerDialog.test.tsx
  - src/features/auth/components/__tests__/LoginForm.test.tsx
  - src/features/intake/__tests__/IntakePanel.test.tsx
  - src/components/ui/__tests__/ConfirmDialog.test.tsx
  - src/components/ui/__tests__/EditableField.test.tsx
autonomous: true
requirements:
  - SC-01
  - SC-02
must_haves:
  truths:
    - "All interactive UI components have React Testing Library tests"
    - "Wizard step components are tested for form validation and navigation"
    - "Integration tests verify wizard -> store data flow (inputs persisted correctly)"
  artifacts:
    - path: "src/features/school-profile/components/__tests__/"
      provides: "Wizard component tests"
      min_lines: 30
    - path: "src/features/school-overview/components/__tests__/"
      provides: "School overview component tests"
      min_lines: 20
  key_links:
    - from: "src/features/school-profile/components/__tests__/"
      to: "src/stores/"
      via: "store interaction in tests"
      pattern: "useSchoolProfileStore"
---

<objective>
Add React Testing Library component tests for all interactive UI components — forms, modals, tabs, wizard steps, navigation.

Purpose: Per D-03, broad UI coverage with RTL for all interactive components. This gives confidence when refactoring.
Output: Component test files for wizard, school overview, auth, intake, and reusable UI components.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/22-architectuur-testen-productie-readiness/22-CONTEXT.md
@.planning/phases/22-architectuur-testen-productie-readiness/22-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wizard step component tests + integration with store</name>
  <files>
    src/features/school-profile/components/__tests__/WizardShell.test.tsx,
    src/features/school-profile/components/__tests__/WizardStep1.test.tsx,
    src/features/school-profile/components/__tests__/WizardStep2.test.tsx,
    src/features/school-profile/components/__tests__/WizardStep3.test.tsx,
    src/features/school-profile/components/__tests__/WizardStep4.test.tsx,
    src/features/school-profile/components/__tests__/WizardStep5.test.tsx
  </files>
  <read_first>
    - src/features/school-profile/components/WizardShell.tsx (shell component with step navigation)
    - src/features/school-profile/components/WizardStep1.tsx (Niveaus step)
    - src/features/school-profile/components/WizardStep2.tsx (Leerlingen step)
    - src/features/school-profile/components/WizardStep3.tsx (Modules step)
    - src/features/school-profile/components/WizardStep4.tsx (Situatie step)
    - src/features/school-profile/components/WizardStep5.tsx (Doel step)
    - src/features/school-profile/schemas/ (Zod schemas for form validation)
    - src/stores/school-profile-store.ts (store actions the wizard calls)
    - src/features/school-profile/components/__tests__/ (existing tests to follow patterns)
  </read_first>
  <action>
Per D-03 (broad UI coverage with RTL):

For each wizard step component, create a test file testing:

1. **WizardShell.test.tsx**:
   - Renders the correct step based on current step index
   - "Volgende" button advances to next step
   - "Vorige" button goes back
   - Progress bar shows correct step labels
   - Step 5 shows "Voltooien" instead of "Volgende"

2. **WizardStep1.test.tsx** (Niveaus):
   - Renders all education level checkboxes
   - Selecting a level updates the form state
   - At least one level must be selected to proceed (validation)

3. **WizardStep2.test.tsx** (Leerlingen):
   - Renders input fields for each selected level
   - Validates that student counts are positive numbers
   - Empty/zero count shows validation error

4. **WizardStep3.test.tsx** (Modules):
   - Renders module selection grid
   - Selecting modules updates form state
   - At least one module required

5. **WizardStep4.test.tsx** (Situatie):
   - Renders current provider selection per module
   - Provider dropdown contains cito, dia, jij, overig, geen options
   - Changing provider updates moduleSetups

6. **WizardStep5.test.tsx** (Doel):
   - Renders scenario selection (A, B, C)
   - Selection persists in store

**Integration test in WizardShell.test.tsx**:
   - Complete wizard flow: select levels -> enter counts -> select modules -> set providers -> choose scenario
   - Verify store state matches wizard inputs after completion (wizard -> store data flow)
   - Note: the engine data flow (store -> engine -> UI) is tested implicitly via E2E in Plan 05

Mock approach:
- Mock Supabase/API calls with vi.mock()
- Render with real Zustand store (use store.setState for setup)
- Use userEvent for form interactions
- Use screen.getByRole, getByLabelText for queries (accessible selectors)

All labels, buttons, and error messages are in Dutch — use Dutch text in assertions.
  </action>
  <behavior>
    - WizardShell: step navigation works forward and backward
    - Step1: level selection toggles update state
    - Step2: student count validation rejects negative/zero
    - Step3: module selection updates selectedModules
    - Step4: provider dropdown changes update moduleSetups
    - Step5: scenario radio selection persists
    - Integration: full wizard flow produces correct store state
  </behavior>
  <verify>
    <automated>npx vitest run src/features/school-profile/ 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - 6 test files exist in src/features/school-profile/components/__tests__/
    - WizardShell test includes at least 4 test cases (render, forward, backward, completion)
    - Each WizardStep test includes at least 2 test cases (render, validation)
    - At least 1 integration test verifies wizard -> store data flow
    - `npx vitest run src/features/school-profile/` reports 0 failures
  </acceptance_criteria>
  <done>All wizard components have RTL tests including form validation and store integration</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Component tests for school overview, auth, intake, and shared UI</name>
  <files>
    src/features/school-overview/components/__tests__/SchoolCard.test.tsx,
    src/features/school-overview/components/__tests__/SchoolPickerDialog.test.tsx,
    src/features/auth/components/__tests__/LoginForm.test.tsx,
    src/features/intake/__tests__/IntakePanel.test.tsx,
    src/components/ui/__tests__/ConfirmDialog.test.tsx,
    src/components/ui/__tests__/EditableField.test.tsx
  </files>
  <read_first>
    - src/features/school-overview/components/ (list components to identify interactive ones)
    - src/features/school-overview/components/SchoolCard.tsx (card component)
    - src/features/auth/components/ (auth UI components)
    - src/features/intake/ (intake panel component)
    - src/components/ui/ (shared UI components)
    - src/components/ui/ConfirmDialog.tsx (reusable confirm dialog)
    - src/components/ui/EditableField.tsx (inline edit component)
  </read_first>
  <action>
Per D-03: test all remaining interactive components beyond the wizard.

1. **SchoolCard.test.tsx**:
   - Renders school name and pipeline status badge
   - Incomplete school shows "Profiel voltooien" link
   - Click navigates to school dashboard (or wizard for incomplete)

2. **SchoolPickerDialog.test.tsx**:
   - Opens when triggered
   - Shows list of schools
   - Search filters school list
   - Selecting a school calls onSelect callback
   - "Annuleren" closes dialog

3. **LoginForm.test.tsx**:
   - Renders email and password fields
   - Shows validation errors for empty fields
   - Submit calls auth handler
   - Shows loading state during submission
   - Shows error message on auth failure

4. **IntakePanel.test.tsx**:
   - Renders text input area
   - Submit sends text for AI processing
   - Shows loading/streaming state
   - Displays extracted data for confirmation
   Note: Mock the AI API call, do NOT call Claude in tests.

5. **ConfirmDialog.test.tsx**:
   - Opens with title and message
   - "Bevestigen" calls onConfirm
   - "Annuleren" calls onCancel
   - Configurable cancel label works

6. **EditableField.test.tsx**:
   - Renders value in display mode
   - Click/focus enters edit mode
   - Enter/blur saves new value
   - Escape cancels edit
   - a11y: focus management works

Mock approach:
- Mock navigation hooks (useNavigate, router links)
- Mock Supabase auth for LoginForm
- Mock AI SDK for IntakePanel
- Use userEvent for all interactions
  </action>
  <behavior>
    - SchoolCard: renders correct status, navigates on click
    - SchoolPickerDialog: open/close/search/select flow
    - LoginForm: validation, submit, error display
    - IntakePanel: input, submit, streaming display, confirmation
    - ConfirmDialog: confirm/cancel callbacks
    - EditableField: display/edit mode toggle, save/cancel
  </behavior>
  <verify>
    <automated>npx vitest run src/features/school-overview/ src/features/auth/ src/features/intake/ src/components/ui/ 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - SchoolCard.test.tsx has at least 3 test cases
    - LoginForm.test.tsx has at least 4 test cases (render, validation, submit, error)
    - IntakePanel.test.tsx has at least 3 test cases
    - ConfirmDialog.test.tsx has at least 3 test cases
    - EditableField.test.tsx has at least 4 test cases (display, edit, save, cancel)
    - All new test files pass with 0 failures
  </acceptance_criteria>
  <done>All interactive UI components have RTL tests covering user interactions, form validation, and error states</done>
</task>

</tasks>

<verification>
- `npx vitest run src/features/ src/components/` passes with 0 failures
- Component test count significantly increased
- All forms tested for validation behavior
- All dialogs/modals tested for open/close/callback behavior
</verification>

<success_criteria>
- Every interactive component (forms, modals, wizard steps, dialogs) has at least one test file
- Tests use accessible selectors (getByRole, getByLabelText) not implementation details
- Integration test proves wizard -> store data flow works (inputs persisted correctly)
</success_criteria>

<output>
After completion, create `.planning/phases/22-architectuur-testen-productie-readiness/22-04-SUMMARY.md`
</output>

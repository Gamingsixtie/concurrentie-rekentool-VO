---
phase: 16-ai-wizard-verbetering-prijsvergelijking-harmonisatie
plan: 02
type: execute
wave: 2
depends_on: ["16-01"]
files_modified:
  - src/features/price-comparison/wizard/ComparisonWizard.tsx
  - src/features/price-comparison/wizard/ComparisonWizardProgress.tsx
  - src/features/price-comparison/wizard/WizardStep1Notes.tsx
  - src/features/price-comparison/wizard/WizardStep2Variants.tsx
  - src/features/price-comparison/wizard/VariantCard.tsx
  - src/features/price-comparison/wizard/ScenarioDetector.tsx
autonomous: true
requirements:
  - PRIJS-01
  - PRIJS-03
  - PRIJS-06

must_haves:
  truths:
    - "User sees a 3-step progress bar and can navigate between completed steps"
    - "User can enter conversation notes or skip with 'Niet bekend' to proceed to variant selection"
    - "User sees AI-extracted variant info pre-filled when notes are provided, with confidence indicators showing what was inferred vs unknown"
    - "User can select per module which competitor variant (DIA package or JIJ tier) the school uses, with 'Aanbevolen' badge on the engine-suggested option"
    - "User sees appropriate scenario banners for schools using only old-Cito or only new-Cito"
    - "Variant selections default from moduleSetups first (D-07), with AI extraction overlaid on top for higher-confidence matches"
  artifacts:
    - path: "src/features/price-comparison/wizard/ComparisonWizard.tsx"
      provides: "Main wizard container with step navigation and collapsed state"
      min_lines: 80
    - path: "src/features/price-comparison/wizard/ComparisonWizardProgress.tsx"
      provides: "3-step horizontal progress bar"
      min_lines: 30
    - path: "src/features/price-comparison/wizard/WizardStep1Notes.tsx"
      provides: "Gespreksnotities textarea with AI extract trigger"
      min_lines: 60
    - path: "src/features/price-comparison/wizard/WizardStep2Variants.tsx"
      provides: "Per-module variant selection grid"
      min_lines: 80
    - path: "src/features/price-comparison/wizard/VariantCard.tsx"
      provides: "Selectable card for DIA package or JIJ tier"
      min_lines: 40
    - path: "src/features/price-comparison/wizard/ScenarioDetector.tsx"
      provides: "Scenario detection banner for D-26"
      min_lines: 25
  key_links:
    - from: "src/features/price-comparison/wizard/ComparisonWizard.tsx"
      to: "src/features/price-comparison/wizard/wizard-store.ts"
      via: "useWizardStore hook for step navigation and state"
      pattern: "useWizardStore"
    - from: "src/features/price-comparison/wizard/WizardStep1Notes.tsx"
      to: "src/lib/ai-wizard.ts"
      via: "extractVariantsFromNotes call on submit"
      pattern: "extractVariantsFromNotes"
    - from: "src/features/price-comparison/wizard/WizardStep2Variants.tsx"
      to: "src/features/price-comparison/wizard/variant-suggestions.ts"
      via: "suggestDiaPackage and suggestJijTier for smart defaults"
      pattern: "suggest(Dia|Jij)"
---

<objective>
Build the wizard UI shell and steps 1-2: the progress bar, conversation notes input with AI extraction, and per-module variant selection with DIA package/JIJ tier cards and smart suggestions.

Purpose: Steps 1-2 collect the data needed for the AI advice generation in step 3. The variant selection is the core of fair comparison -- users explicitly choose which competitor variant is being compared.

Output: 6 new React components forming the wizard shell, progress indicator, notes input, variant selection grid, variant cards, and scenario detection banner.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-ai-wizard-verbetering-prijsvergelijking-harmonisatie/16-CONTEXT.md
@.planning/phases/16-ai-wizard-verbetering-prijsvergelijking-harmonisatie/16-RESEARCH.md
@.planning/phases/16-ai-wizard-verbetering-prijsvergelijking-harmonisatie/16-UI-SPEC.md
@.planning/phases/16-ai-wizard-verbetering-prijsvergelijking-harmonisatie/16-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 output -->

From src/features/price-comparison/wizard/types.ts:
```typescript
export type WizardScenario = 'deels-concurrent' | 'alles-oud-cito' | 'alles-nieuw-cito';
export type VariantConfidence = 'high' | 'low' | 'unknown';
export interface ModuleVariantSelection { moduleId: string; provider: 'dia' | 'jij' | 'geen'; variantId: string | null; confidence: VariantConfidence; }
export interface ExtractedVariantResult { selections: ModuleVariantSelection[]; uitleg: string; }
export interface ExtraContextInput { korting: string; dmuFocus: string; bijzonderheden: string; }
```

From src/features/price-comparison/wizard/wizard-store.ts:
```typescript
export const useWizardStore: UseBoundStore<...>;
// Key selectors: currentStep, isCollapsed, hasCompletedOnce, conversationNotes, extractionResult, isExtracting, variantSelections, scenario
// Key actions: setStep, setConversationNotes, setExtractionResult, setIsExtracting, setVariantSelections, updateVariantSelection, setScenario, collapse, expand, resetWizard
```

From src/features/price-comparison/wizard/scenario-detection.ts:
```typescript
export function detectScenario(moduleSetups: ModuleCurrentSetup[]): WizardScenario;
```

From src/features/price-comparison/wizard/variant-suggestions.ts:
```typescript
export function suggestDiaPackage(selectedModuleIds: string[]): DiaPackage | null;
export function suggestJijTier(totalStudents: number, testsPerStudent?: number): JijLicenseTier;
```

From src/lib/ai-wizard.ts:
```typescript
export async function extractVariantsFromNotes(notes: string, selectedModules: string[]): Promise<ExtractedVariantResult>;
```

From src/data/providers/dia.ts:
```typescript
export const DIA_PACKAGES: DiaPackage[];
```

From src/data/providers/jij.ts:
```typescript
export const JIJ_LICENSE_TIERS: JijLicenseTier[];
```

From src/components/wizard/WizardShell.tsx (existing pattern):
```typescript
// Step navigation pattern: currentStep state, handleNext/handleBack callbacks
// ProgressBar receives labels array and currentStep number
```

From src/features/school-profile/store.ts:
```typescript
// useSchoolProfileStore selectors: selectedModules, moduleSetups, studentCounts, levels
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wizard shell, progress bar, and step 1 notes component</name>
  <files>
    src/features/price-comparison/wizard/ComparisonWizard.tsx,
    src/features/price-comparison/wizard/ComparisonWizardProgress.tsx,
    src/features/price-comparison/wizard/WizardStep1Notes.tsx,
    src/features/price-comparison/wizard/ScenarioDetector.tsx
  </files>
  <read_first>
    src/features/price-comparison/wizard/types.ts,
    src/features/price-comparison/wizard/wizard-store.ts,
    src/features/price-comparison/wizard/scenario-detection.ts,
    src/lib/ai-wizard.ts,
    src/components/wizard/WizardShell.tsx,
    src/components/wizard/ProgressBar.tsx,
    src/features/school-profile/store.ts,
    src/features/price-comparison/AdvicePanel.tsx,
    .planning/phases/16-ai-wizard-verbetering-prijsvergelijking-harmonisatie/16-UI-SPEC.md
  </read_first>
  <action>
    **ComparisonWizardProgress.tsx** -- 3-step horizontal progress bar (per D-03, UI-SPEC):
    - Three steps with labels: "Gespreksnotities", "Variant-selectie", "Advies & Resultaat"
    - Active step: circle with `bg-cito-accent text-white` + bold label
    - Completed step: circle with `bg-cito-primary text-white` + checkmark SVG, clickable (onClick -> setStep)
    - Future step: circle with `bg-neutral-200 text-neutral-500` + number, not clickable
    - Connecting lines between circles: `bg-cito-primary` for completed, `bg-neutral-200` for future
    - Props: `currentStep: 0 | 1 | 2`, `onStepClick: (step: 0 | 1 | 2) => void`
    - Responsive: On `< md` screens, hide labels and show only step numbers (per UI-SPEC)

    **ScenarioDetector.tsx** -- Banner component per D-26 and UI-SPEC:
    - Props: `scenario: WizardScenario`, `onProceed?: () => void`
    - 'deels-concurrent': return null (no banner, wizard proceeds)
    - 'alles-oud-cito': blue info banner (`bg-blue-50 border-blue-200`), text: "Deze school gebruikt uitsluitend het huidige Cito-platform. Ga naar de migratie-pagina voor een overstap-businesscase." (exact copy from UI-SPEC)
    - 'alles-nieuw-cito': green info banner (`bg-emerald-50 border-emerald-200`), text: "Deze school gebruikt al het nieuwe Cito-platform. Wilt u alsnog een marktvergelijking uitvoeren?" + "Doorgaan" button calling onProceed

    **WizardStep1Notes.tsx** -- Gespreksnotities step per D-09, D-10, D-11, UI-SPEC:
    - Full-width textarea, min 4 rows, placeholder: "Beschrijf het gesprek met de school: welke toetsaanbieders worden gebruikt, welke pakketten, bijzonderheden..."
    - Heading: "Wat weet u over het huidige gebruik van deze school?"
    - Textarea value bound to `useWizardStore.conversationNotes` via `setConversationNotes`
    - "Niet bekend -- handmatig selecteren" ghost button below textarea (per D-10, UI-SPEC copy)
    - On "Niet bekend" click: set extractionResult to `{ selections: [], uitleg: '' }`, advance to step 1
    - "Volgende stap" button (primary, `bg-cito-accent`, `min-h-[44px]` touch target):
      - If notes empty: disabled
      - If notes present: on click, set isExtracting true, call `extractVariantsFromNotes(notes, selectedModules)`, store result in wizard store, advance to step 1
      - During extraction: button shows spinner + "Analyseren..." text, textarea disabled, pulse animation on button
    - Error handling: if extraction fails, show inline error "De notities konden niet worden geanalyseerd. U kunt de varianten handmatig selecteren in de volgende stap." (UI-SPEC copy), allow advancing to step 2 with empty selections

    **ComparisonWizard.tsx** -- Main wizard container per D-01, D-03, D-04, UI-SPEC:
    - Title: "AI Vergelijkingsadvies" (display size `text-[22px] font-semibold`)
    - Subtitle: "Eerlijke concurrentievergelijking in 3 stappen" (body `text-sm text-neutral-500`)
    - White card container: `bg-white rounded-xl border border-neutral-200 p-6`
    - Max width: `max-w-[720px] mx-auto` on md+, full width with `px-4` on small screens
    - Reads `currentStep`, `isCollapsed`, `hasCompletedOnce` from useWizardStore
    - Reads `selectedModules`, `moduleSetups` from useSchoolProfileStore
    - On mount: call `detectScenario(moduleSetups)` and store in wizard store via setScenario
    - Collapsed state (per UI-SPEC): summary bar with `bg-cito-primary/5 border-cito-primary/20 rounded-lg p-4`, text "AI vergelijkingsadvies actief", "Opnieuw doorlopen" secondary button calling expand()
    - Expanded state: render ComparisonWizardProgress + ScenarioDetector + active step component (Step1Notes | Step2Variants | Step3Advice)
    - Navigation: "Vorige stap" text button (left) and "Volgende stap" primary button (right) in a flex row at the bottom. Step 0: no back button. Step 2: next button text changes based on step 3 logic (handled by Step3).
    - Empty state guard: if selectedModules.length === 0, show "Selecteer eerst modules in de wizard om een vergelijking te starten." (UI-SPEC copy)
  </action>
  <verify>
    <automated>npx vitest run src/features/price-comparison/wizard/__tests__/variant-suggestions.test.ts src/engine/__tests__/dia-packages.test.ts --reporter=verbose && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - ComparisonWizard.tsx imports and renders ComparisonWizardProgress, ScenarioDetector, WizardStep1Notes
    - ComparisonWizard.tsx contains useWizardStore import
    - ComparisonWizard.tsx contains detectScenario import
    - ComparisonWizard.tsx contains "AI Vergelijkingsadvies" string literal
    - ComparisonWizard.tsx contains "Opnieuw doorlopen" string literal for collapsed state
    - ComparisonWizardProgress.tsx renders 3 step circles with labels "Gespreksnotities", "Variant-selectie", "Advies & Resultaat"
    - WizardStep1Notes.tsx contains extractVariantsFromNotes import
    - WizardStep1Notes.tsx contains "Niet bekend" string literal
    - WizardStep1Notes.tsx contains "Analyseren..." string literal for loading state
    - ScenarioDetector.tsx contains "alles-oud-cito" and "alles-nieuw-cito" case handling
    - All components use min-h-[44px] on interactive buttons
    - npm run build passes
  </acceptance_criteria>
  <done>Wizard shell renders with progress bar, scenario detection, and step 1 notes input with AI extraction trigger</done>
</task>

<task type="auto">
  <name>Task 2: Step 2 variant selection with DIA/JIJ cards and smart suggestions</name>
  <files>
    src/features/price-comparison/wizard/WizardStep2Variants.tsx,
    src/features/price-comparison/wizard/VariantCard.tsx
  </files>
  <read_first>
    src/features/price-comparison/wizard/types.ts,
    src/features/price-comparison/wizard/wizard-store.ts,
    src/features/price-comparison/wizard/variant-suggestions.ts,
    src/data/providers/dia.ts,
    src/data/providers/jij.ts,
    src/data/providers/cito.ts,
    src/models/modules.ts,
    src/features/school-profile/store.ts,
    src/features/price-comparison/wizard/ComparisonWizard.tsx,
    .planning/phases/16-ai-wizard-verbetering-prijsvergelijking-harmonisatie/16-UI-SPEC.md
  </read_first>
  <action>
    **VariantCard.tsx** -- Selectable card for DIA package or JIJ tier per D-13, UI-SPEC:
    - Props: `{ type: 'dia-package' | 'jij-tier'; id: string; name: string; priceLabel: string; description: string; includedModules?: string[]; isSelected: boolean; isRecommended: boolean; onClick: () => void; }`
    - White card with border: `bg-white rounded-lg border p-4 cursor-pointer min-h-[48px]`
    - Selected state: `border-cito-accent ring-2 ring-cito-accent/20`
    - Default state: `border-neutral-200 hover:border-neutral-300`
    - Top-right "Aanbevolen" pill badge when `isRecommended`: `bg-cito-accent/10 text-cito-accent border border-cito-accent/30 text-[11px] font-semibold px-2 py-0.5 rounded-full`
    - Card content: name (semibold), priceLabel below (text-sm text-neutral-600), description below, optional module list as small tags
    - For DIA packages: priceLabel = `€{pricePerStudent}/leerling/jaar`, show included module names as tags
    - For JIJ tiers: priceLabel = `€{annualFee}/jaar + €{pricePerTest}/afname`, show administration range

    **WizardStep2Variants.tsx** -- Per-module variant selection per D-06, D-07, D-12, D-13, D-14, UI-SPEC:
    - Heading: "Bevestig de variant per module"
    - Reads `selectedModules` from useSchoolProfileStore
    - Reads `variantSelections`, `extractionResult` from useWizardStore
    - Reads `studentCounts` from useSchoolProfileStore for smart suggestions

    For each selected module, render a section:
    1. Module name (from MODULE_CATALOG lookup by moduleId) with confidence indicator:
       - Green dot + "Uit notities afgeleid" for confidence 'high' (colors: `text-green-800 bg-green-50`)
       - Orange dot + "Onzeker -- controleer" for confidence 'low' (colors: `text-orange-800 bg-orange-50`)
       - Gray dot + "Niet bekend" for confidence 'unknown' (colors: `text-neutral-500 bg-neutral-50`)
    2. Provider radio group: "DIA", "JIJ", "Geen" as horizontal radio buttons. Pre-filled from variantSelections[moduleId].provider or 'geen' if absent.
    3. When provider is 'dia': show grid of VariantCard components for each DIA_PACKAGES entry that includes this moduleId in `includedModuleIds`. Mark recommended via `suggestDiaPackage([moduleId])`. Selected = variantSelections[moduleId].variantId.
    4. When provider is 'jij': show grid of VariantCard components for each JIJ_LICENSE_TIERS entry. Mark recommended via `suggestJijTier(totalStudents)`. Selected = variantSelections[moduleId].variantId.
    5. When provider is 'geen': show "Selecteer welk aanbod de concurrent gebruikt voor deze module." (UI-SPEC copy)

    On provider radio change: call `updateVariantSelection(moduleId, { provider: newProvider, variantId: null, confidence: existingConfidence })`.
    On variant card click: call `updateVariantSelection(moduleId, { variantId: cardId })`.

    Pre-fill logic on mount (per D-07 -- moduleSetups is the startpunt):
    For each selectedModule, build the initial variantSelections as follows:
    1. **Base layer: moduleSetups** -- Check moduleSetups for currentProvider. If currentProvider is 'dia' or 'jij', use that as the base provider with variantId null and confidence 'unknown'. This is the starting point per D-07.
    2. **Overlay: AI extraction** -- If extractionResult.selections has a match for this module with confidence 'high' or 'low', overlay it on top of the base. AI extraction takes priority when it has information, because it provides specific variant IDs and confidence levels.
    3. **Fallback** -- If neither moduleSetups nor extraction has info, default to provider 'geen', variantId null, confidence 'unknown'.

    This ensures moduleSetups is the startpunt (D-07) while AI extraction enriches with specific variant details when available.

    Variant cards in 2-column grid on md+, single column on small screens: `grid grid-cols-1 md:grid-cols-2 gap-3`.
  </action>
  <verify>
    <automated>npx vitest run src/features/price-comparison/wizard/__tests__/variant-suggestions.test.ts src/engine/__tests__/dia-packages.test.ts --reporter=verbose && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - WizardStep2Variants.tsx imports DIA_PACKAGES from '@/data/providers/dia'
    - WizardStep2Variants.tsx imports JIJ_LICENSE_TIERS from '@/data/providers/jij'
    - WizardStep2Variants.tsx imports suggestDiaPackage and suggestJijTier from './variant-suggestions'
    - WizardStep2Variants.tsx imports MODULE_CATALOG from '@/models/modules'
    - WizardStep2Variants.tsx contains "Bevestig de variant per module" string literal
    - WizardStep2Variants.tsx contains confidence indicator strings: "Uit notities afgeleid", "Onzeker", "Niet bekend"
    - WizardStep2Variants.tsx pre-fill logic checks moduleSetups first as base layer, then overlays extractionResult
    - VariantCard.tsx contains "Aanbevolen" string literal
    - VariantCard.tsx has isRecommended prop
    - VariantCard.tsx has isSelected prop with ring-2 ring-cito-accent visual
    - Both components use min-h-[44px] or min-h-[48px] on interactive elements
    - npm run build passes
  </acceptance_criteria>
  <done>Step 2 renders per-module variant selection with DIA packages and JIJ tiers as selectable cards, with smart suggestions, confidence indicators, and moduleSetups-first pre-fill per D-07</done>
</task>

</tasks>

<verification>
- `npm run build` -- production build succeeds with all new components
- `npx vitest run` -- all existing + new tests pass
- All UI text in Dutch (Nederlands) per CLAUDE.md
- All components follow Tailwind CSS 4 + Cito theme tokens
- Touch targets minimum 44px height on all buttons
</verification>

<success_criteria>
- Wizard shell renders with 3-step progress bar, collapsed/expanded states, and step navigation
- Step 1 accepts conversation notes and triggers AI extraction or allows skip
- Step 2 shows per-module variant selection with DIA packages and JIJ tiers as cards
- Step 2 pre-fills from moduleSetups as base (D-07), overlays AI extraction for specifics
- Step 2 marks engine-suggested variants as "Aanbevolen"
- Scenario detection shows appropriate banners
- Build passes, no TypeScript errors
</success_criteria>

<output>
After completion, create `.planning/phases/16-ai-wizard-verbetering-prijsvergelijking-harmonisatie/16-02-SUMMARY.md`
</output>

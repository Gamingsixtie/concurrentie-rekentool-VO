---
phase: 16-ai-wizard-verbetering-prijsvergelijking-harmonisatie
plan: 03
type: execute
wave: 3
depends_on: ["16-01", "16-02"]
files_modified:
  - src/features/price-comparison/wizard/WizardStep3Advice.tsx
  - src/features/price-comparison/wizard/ExtraContextField.tsx
  - src/features/price-comparison/PriceComparisonPage.tsx
autonomous: false
requirements:
  - PRIJS-01
  - PRIJS-03
  - PRIJS-05
  - PRIJS-06

must_haves:
  truths:
    - "Step 3 accepts optional extra context (korting, DMU-focus, bijzonderheden) and triggers AI advice generation"
    - "AI advice streams in real-time with visible text accumulation and blinking cursor"
    - "Advice result shows typed cards (prijs, meerwaarde, bezwaar, kans, strategie) with Cito bundle recommendation"
    - "User can adjust module matching in step 3 before confirming"
    - "Pas tabel aan button writes wizard results to comparison store via applyToTable()"
    - "PriceComparisonPage renders ComparisonWizard in place of removed AdvicePanel"
    - "Table reflects wizard selections after apply -- visible providers and Cito bundle type match wizard output"
  artifacts:
    - path: "src/features/price-comparison/wizard/WizardStep3Advice.tsx"
      provides: "AI advice streaming, result display, editable matching, apply-to-table confirmation"
      min_lines: 120
    - path: "src/features/price-comparison/wizard/ExtraContextField.tsx"
      provides: "3 labeled input fields for extra context (D-21)"
      min_lines: 40
    - path: "src/features/price-comparison/PriceComparisonPage.tsx"
      provides: "Updated page with ComparisonWizard replacing AdvicePanel"
  key_links:
    - from: "src/features/price-comparison/wizard/WizardStep3Advice.tsx"
      to: "src/lib/ai-wizard.ts"
      via: "streamWizardAdvice and parseAdviceFromText"
      pattern: "streamWizardAdvice|parseAdviceFromText"
    - from: "src/features/price-comparison/wizard/WizardStep3Advice.tsx"
      to: "src/features/price-comparison/wizard/wizard-store.ts"
      via: "applyToTable() on confirmation"
      pattern: "applyToTable"
    - from: "src/features/price-comparison/PriceComparisonPage.tsx"
      to: "src/features/price-comparison/wizard/ComparisonWizard.tsx"
      via: "import and render in place of AdvicePanel"
      pattern: "ComparisonWizard"
---

<objective>
Build wizard step 3 (AI advice generation with streaming, result display, and editable matching), the extra context field, and integrate the complete wizard into PriceComparisonPage by replacing AdvicePanel.

Purpose: Step 3 is the core of fair comparison -- AI generates a matching recommendation with Cito bundle selection and strategic advice. The "Pas tabel aan" button harmonizes wizard output with the comparison table (single source of truth per D-23).

Output: 2 new components (WizardStep3Advice, ExtraContextField) + 1 modified page (PriceComparisonPage with wizard replacing AdvicePanel)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-ai-wizard-verbetering-prijsvergelijking-harmonisatie/16-CONTEXT.md
@.planning/phases/16-ai-wizard-verbetering-prijsvergelijking-harmonisatie/16-RESEARCH.md
@.planning/phases/16-ai-wizard-verbetering-prijsvergelijking-harmonisatie/16-UI-SPEC.md
@.planning/phases/16-ai-wizard-verbetering-prijsvergelijking-harmonisatie/16-01-SUMMARY.md
@.planning/phases/16-ai-wizard-verbetering-prijsvergelijking-harmonisatie/16-02-SUMMARY.md

<interfaces>
<!-- From Plan 01 output -->

From src/features/price-comparison/wizard/types.ts:
```typescript
export interface WizardAdviceResult {
  samenvatting: string;
  matchingUitleg: string;
  aanbevolenCitoBundel: 'individual' | 'basis' | 'plus';
  adviezen: Array<{ titel: string; tekst: string; type: 'prijs' | 'meerwaarde' | 'bezwaar' | 'kans' | 'strategie'; }>;
  dmuStrategie?: Record<string, string>;
}
export interface ExtraContextInput { korting: string; dmuFocus: string; bijzonderheden: string; }
export interface ModuleVariantSelection { moduleId: string; provider: 'dia' | 'jij' | 'geen'; variantId: string | null; confidence: VariantConfidence; }
```

From src/features/price-comparison/wizard/wizard-store.ts:
```typescript
// Key selectors: variantSelections, adjustedSelections, aiAdvice, extraContext, isGeneratingAdvice, streamingText
// Key actions: setAiAdvice, setAdjustedSelections, setExtraContext, setIsGeneratingAdvice, setStreamingText, appendStreamingText, applyToTable, collapse
```

From src/lib/ai-wizard.ts:
```typescript
export async function* streamWizardAdvice(...): AsyncGenerator<string>;
export function parseAdviceFromText(text: string): WizardAdviceResult;
```

From src/features/price-comparison/AdvicePanel.tsx (TYPE_CONFIG pattern to extend):
```typescript
const TYPE_CONFIG: Record<AdviceItem['type'], { label: string; color: string; bgColor: string }> = {
  prijs: { label: 'Prijs', color: 'text-blue-700', bgColor: 'bg-blue-50 border-blue-200' },
  meerwaarde: { label: 'Meerwaarde', color: 'text-emerald-700', bgColor: 'bg-emerald-50 border-emerald-200' },
  bezwaar: { label: 'Bezwaar', color: 'text-amber-700', bgColor: 'bg-amber-50 border-amber-200' },
  kans: { label: 'Kans', color: 'text-purple-700', bgColor: 'bg-purple-50 border-purple-200' },
};
```

From src/features/price-comparison/PriceComparisonPage.tsx:
```typescript
// Currently imports: AdvicePanel from './AdvicePanel'
// Renders: <AdvicePanel /> between ComparisonSummary and ComparisonTable sections
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Step 3 advice component with streaming, result display, and extra context field</name>
  <files>
    src/features/price-comparison/wizard/WizardStep3Advice.tsx,
    src/features/price-comparison/wizard/ExtraContextField.tsx
  </files>
  <read_first>
    src/features/price-comparison/wizard/types.ts,
    src/features/price-comparison/wizard/wizard-store.ts,
    src/lib/ai-wizard.ts,
    src/features/price-comparison/AdvicePanel.tsx,
    src/features/price-comparison/store.ts,
    src/features/school-profile/store.ts,
    src/data/differentiators.ts,
    src/data/providers/dia.ts,
    src/data/providers/jij.ts,
    src/data/providers/cito.ts,
    src/data/providers/index.ts,
    .planning/phases/16-ai-wizard-verbetering-prijsvergelijking-harmonisatie/16-UI-SPEC.md
  </read_first>
  <action>
    **ExtraContextField.tsx** -- Structured extra context input per D-21, UI-SPEC:
    - Heading: "Aanvullende context (optioneel)" (`text-sm font-semibold text-cito-primary`)
    - 3 stacked labeled input fields, all optional:
      1. Label: "Eventuele korting concurrent", placeholder: "bijv. 15% korting op DIA Basis", `<input type="text" />`
      2. Label: "DMU-focus", placeholder: "bijv. MT is prijsbewust, coordinator wil tijdwinst", `<input type="text" />`
      3. Label: "Bijzonderheden", placeholder: "Andere relevante informatie voor het advies...", `<textarea rows={2} />`
    - Props: `{ value: ExtraContextInput; onChange: (update: Partial<ExtraContextInput>) => void }`
    - All inputs: `bg-white border border-neutral-200 rounded-lg px-3 py-2 text-sm` + `focus:ring-2 focus:ring-cito-accent/20 focus:border-cito-accent`
    - Labels: `text-sm font-semibold text-neutral-700 mb-1`

    **WizardStep3Advice.tsx** -- AI advice generation and result display per D-15, D-16, D-17, D-18, D-19, D-22, D-24, D-25, UI-SPEC:

    Layout sections (top to bottom):
    1. Heading: "AI vergelijkingsadvies"
    2. ExtraContextField component, bound to wizard store extraContext via setExtraContext
    3. "Genereer advies" primary CTA button (`bg-cito-accent text-white min-h-[44px] w-full rounded-lg font-semibold`)
       - Disabled during streaming, shows spinner + "Genereren..." text
       - After advice generated: changes to "Opnieuw genereren"
    4. Streaming display container: `bg-cito-primary/5 border border-cito-primary/20 rounded-lg p-4`
       - Text appears character-by-character from streamingText in wizard store
       - Blinking cursor: `<span className="animate-pulse">|</span>` appended to text during streaming
       - Hidden when no streaming has started
    5. Parsed advice result (shown after streaming completes):
       - Samenvatting banner: `bg-cito-primary text-white rounded-lg p-4` with samenvatting text
       - Matching uitleg: `bg-white border border-neutral-200 rounded-lg p-4` with matchingUitleg text and recommended Cito bundle highlighted
       - Advice cards: extend TYPE_CONFIG with `strategie: { label: 'Strategie', color: 'text-indigo-700', bgColor: 'bg-indigo-50 border-indigo-200' }`. Each card: type badge (colored pill) + titel (semibold) + tekst (body)
       - DMU strategie section (if present): collapsible section with per-role strategy text
    6. Editable matching section per D-19: for each module in adjustedSelections, show module name + current provider + variant, with ability to toggle provider/variant (simpler than step 2 -- just dropdown selects, not full card grid). Initialize adjustedSelections from variantSelections when advice is first generated.
    7. "Pas tabel aan" full-width primary CTA (`bg-cito-accent text-white min-h-[44px] w-full rounded-lg font-semibold text-base`)
       - Only enabled after advice is generated (aiAdvice !== null)
       - On click: call `useWizardStore.getState().applyToTable()` which writes to comparison store
       - Per D-24: this is the explicit confirmation

    8. AI disclaimer: `text-[11px] text-neutral-400 mt-3` with text: "Dit advies is automatisch gegenereerd op basis van de vergelijkingsdata en provider-informatie. Controleer de informatie altijd voor gebruik in een gesprek."

    "Genereer advies" onClick handler:
    1. Set isGeneratingAdvice true, setStreamingText(''), setAiAdvice(null)
    2. Build payload: variantSelections (or adjustedSelections if regenerating), schoolProfile from useSchoolProfileStore.getState(), differentiators from MODULE_DIFFERENTIATORS filtered to selected modules, providerData from DIA_PACKAGES/JIJ_LICENSE_TIERS/CITO_BUNDLES, extraContext from wizard store
    3. Call `streamWizardAdvice(...)` async generator
    4. For each chunk: call appendStreamingText(chunk)
    5. After stream completes: call parseAdviceFromText(fullText), store via setAiAdvice
    6. Initialize adjustedSelections from variantSelections
    7. Set isGeneratingAdvice false
    8. Wrap in try/catch: on error, show inline error message (UI-SPEC error state copy)
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - WizardStep3Advice.tsx imports streamWizardAdvice and parseAdviceFromText from '@/lib/ai-wizard'
    - WizardStep3Advice.tsx imports useWizardStore
    - WizardStep3Advice.tsx contains applyToTable call
    - WizardStep3Advice.tsx contains "Genereer advies" string literal
    - WizardStep3Advice.tsx contains "Pas tabel aan" string literal
    - WizardStep3Advice.tsx contains "Opnieuw genereren" string literal
    - WizardStep3Advice.tsx contains "animate-pulse" for blinking cursor
    - WizardStep3Advice.tsx contains the strategie TYPE_CONFIG entry with 'text-indigo-700' and 'bg-indigo-50'
    - WizardStep3Advice.tsx contains AI disclaimer text starting with "Dit advies is automatisch gegenereerd"
    - ExtraContextField.tsx has 3 input fields with labels "Eventuele korting concurrent", "DMU-focus", "Bijzonderheden"
    - ExtraContextField.tsx has Props type with value: ExtraContextInput and onChange
    - npm run build passes
  </acceptance_criteria>
  <done>Step 3 generates streaming AI advice with typed result cards, editable matching, extra context input, and "Pas tabel aan" confirmation button</done>
</task>

<task type="auto">
  <name>Task 2: Replace AdvicePanel with ComparisonWizard in PriceComparisonPage</name>
  <files>
    src/features/price-comparison/PriceComparisonPage.tsx,
    src/features/price-comparison/wizard/ComparisonWizard.tsx
  </files>
  <read_first>
    src/features/price-comparison/PriceComparisonPage.tsx,
    src/features/price-comparison/wizard/ComparisonWizard.tsx,
    src/features/price-comparison/wizard/WizardStep3Advice.tsx,
    src/features/price-comparison/wizard/wizard-store.ts,
    src/features/price-comparison/store.ts
  </read_first>
  <action>
    **PriceComparisonPage.tsx** -- Replace AdvicePanel with ComparisonWizard per D-01, D-04:
    1. Remove import of `AdvicePanel` from `'./AdvicePanel'`
    2. Add import of `ComparisonWizard` from `'./wizard/ComparisonWizard'`
    3. Replace `<AdvicePanel />` JSX with `<ComparisonWizard />`
    4. The wizard renders in the same position as AdvicePanel was (bovenaan de vergelijkingspagina per D-04, between page header area and ComparisonTable)
    5. Keep ALL other imports and components unchanged: ComparisonSummary, ComparisonChart, ComparisonTable, BusinessCaseCTA, DisclaimerFooter, PeriodToggle, CitoBundleSelector, AnalysisPanel
    6. Do NOT remove AdvicePanel.tsx file itself -- just remove the import and usage from PriceComparisonPage

    **ComparisonWizard.tsx** -- Add Step3 rendering (update from Plan 02):
    1. Import WizardStep3Advice from './WizardStep3Advice'
    2. In the step rendering switch/conditional: when currentStep === 2, render `<WizardStep3Advice />`
    3. Verify the navigation buttons at bottom are correctly wired:
       - Step 0: only "Volgende stap" (handled by WizardStep1Notes internally)
       - Step 1: "Vorige stap" + "Volgende stap" (next validates at least 1 module has variant selected)
       - Step 2: "Vorige stap" only (step 3 has its own "Genereer advies" and "Pas tabel aan" buttons)
    4. Add a gap of `mt-8` (`xl` spacing per UI-SPEC) between the wizard card and the ComparisonTable below it in the page layout
  </action>
  <verify>
    <automated>npm run build && npx vitest run --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - PriceComparisonPage.tsx does NOT contain "import { AdvicePanel }" or "import { AdvicePanel"
    - PriceComparisonPage.tsx does NOT contain "<AdvicePanel" JSX
    - PriceComparisonPage.tsx contains "import { ComparisonWizard }" or "import ComparisonWizard" from './wizard/ComparisonWizard'
    - PriceComparisonPage.tsx contains "<ComparisonWizard" JSX
    - ComparisonWizard.tsx imports WizardStep3Advice
    - ComparisonWizard.tsx renders WizardStep3Advice when currentStep === 2
    - npm run build passes with zero errors
    - npx vitest run passes (all existing tests still green)
  </acceptance_criteria>
  <done>AdvicePanel replaced by ComparisonWizard in PriceComparisonPage -- wizard is the primary AI comparison interface</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual verification of complete wizard flow</name>
  <files>none</files>
  <action>
    Human verifies the complete 3-step AI comparison wizard flow end-to-end in the browser. No code changes in this task -- purely visual and functional verification of all wizard steps, AI streaming, table harmonization, and collapsed/expanded states.
  </action>
  <what-built>
    Complete 3-step AI comparison wizard replacing AdvicePanel:
    - Step 1: Conversation notes input with AI extraction or "Niet bekend" skip
    - Step 2: Per-module variant selection with DIA packages, JIJ tiers, smart suggestions, confidence indicators
    - Step 3: Extra context input, AI advice streaming, typed advice cards, editable matching, "Pas tabel aan" confirmation
    - Collapsed state with "Opnieuw doorlopen" button
    - Table updates after wizard confirmation (same data, single source of truth)
  </what-built>
  <how-to-verify>
    1. Open the app in browser (npm run dev)
    2. Navigate to a school profile with selected modules (or create one via wizard)
    3. Go to the Vergelijking tab

    **Verify wizard presence:**
    - [ ] Wizard card visible bovenaan with title "AI Vergelijkingsadvies" and subtitle
    - [ ] 3-step progress bar visible with "Gespreksnotities", "Variant-selectie", "Advies & Resultaat"
    - [ ] Step 1 is active (highlighted in accent color)

    **Verify Step 1:**
    - [ ] Textarea with placeholder text visible
    - [ ] "Niet bekend -- handmatig selecteren" button visible below textarea
    - [ ] Click "Niet bekend" -> advances to step 2 with empty selections

    **Verify Step 2:**
    - [ ] Each selected module listed with provider radio buttons (DIA / JIJ / Geen)
    - [ ] Selecting DIA shows package cards with prices
    - [ ] One package shows "Aanbevolen" badge
    - [ ] Selecting JIJ shows tier cards with prices
    - [ ] Confidence indicators show (gray "Niet bekend" when skipped from step 1)

    **Verify Step 3:**
    - [ ] Extra context fields visible (Korting, DMU-focus, Bijzonderheden) -- all optional
    - [ ] "Genereer advies" button visible
    - [ ] Click "Genereer advies" -> streaming text appears with blinking cursor
    - [ ] After streaming: advice cards appear with colored type badges
    - [ ] "Pas tabel aan" button enabled after advice generated

    **Verify harmonization:**
    - [ ] Click "Pas tabel aan" -> wizard collapses to summary bar
    - [ ] ComparisonTable below reflects the selected providers and Cito bundle type
    - [ ] "Opnieuw doorlopen" button visible in collapsed state
    - [ ] Click "Opnieuw doorlopen" -> wizard expands back to step 1 with previous data preserved
  </how-to-verify>
  <verify>Human visual verification</verify>
  <done>User approves complete wizard flow: all 3 steps work, AI streaming functions, table harmonization confirmed, collapsed/expanded states correct</done>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
- `npm run build` -- production build succeeds
- `npx vitest run` -- all tests pass (existing + new wizard tests from Plan 01)
- PriceComparisonPage no longer imports or renders AdvicePanel
- PriceComparisonPage imports and renders ComparisonWizard
- Wizard flow: notes -> variant selection -> AI advice -> apply to table -> table updates
- All UI text in Dutch per CLAUDE.md
- No modifications to files in src/data/ (prices untouched)
</verification>

<success_criteria>
- Step 3 streams AI advice with typed cards and Cito bundle recommendation
- Extra context field provides structured input for AI prompt
- User can adjust matching before confirming
- "Pas tabel aan" writes wizard results to comparison store (single source of truth per D-23)
- PriceComparisonPage renders ComparisonWizard instead of AdvicePanel (per D-01)
- Table reflects wizard selections after confirmation (per D-24)
- Visual verification confirms complete flow works end-to-end
</success_criteria>

<output>
After completion, create `.planning/phases/16-ai-wizard-verbetering-prijsvergelijking-harmonisatie/16-03-SUMMARY.md`
</output>

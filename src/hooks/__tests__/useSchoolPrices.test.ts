import { describe, it } from 'vitest';

// import { useSchoolPrices, useCreateSchoolPrice, useActivateSchoolPrice, useDeleteSchoolPrice } from '@/hooks/useSchoolPrices';

describe('useSchoolPrices hooks', () => {
  it.todo('useSchoolPrices returns prices filtered by schoolId');
  it.todo('useCreateSchoolPrice inserts a new price entry');
  it.todo('useActivateSchoolPrice deactivates all then activates one');
  it.todo('useDeleteSchoolPrice removes a price entry');
});

import { describe, it, expect } from 'vitest';

// import { parseExtractionFromText, streamIntakeFromNotes } from '@/lib/ai-intake';

describe('streamIntakeFromNotes v2', () => {
  it.todo('yields SSE text chunks from streaming response');
  it.todo('parseExtractionFromText strips markdown fences and parses JSON');
  it.todo('parseExtractionFromText validates against IntakeExtractionSchemaV2');
  it.todo('parseExtractionFromText throws descriptive Dutch error on invalid JSON');
});

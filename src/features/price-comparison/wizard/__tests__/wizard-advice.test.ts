import { describe, it, expect } from 'vitest';
// parseAdviceFromText will be created in Task 3 (src/lib/ai-wizard.ts)
// Using it.todo until then

describe('parseAdviceFromText', () => {
  it.todo('parses valid JSON string with all WizardAdviceResult fields');
  it.todo('parses JSON wrapped in markdown code fences');
  it.todo('returns fallback result with empty adviezen for invalid text');
});

import { describe, it, expect } from 'vitest';

// import { ConfigEditor } from '@/features/pricing/components/ConfigEditor';

describe('config editor UI', () => {
  it.todo('loads current pricing config for selected provider');
  it.todo('validates config_data against provider schema');
  it.todo('saves updated config with incremented version');
  it.todo('shows validation errors for invalid config values');
  it.todo('reloads config after successful save');
});

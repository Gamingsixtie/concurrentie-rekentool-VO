import { describe, it, expect } from 'vitest';

// import { computeDiff } from '@/features/school-profile/utils/diff-view';

describe('DiffView logic', () => {
  it.todo('marks extracted module as new when not in existing school profile');
  it.todo('marks extracted module as existing when already in school profile');
  it.todo('marks contact as conflict when name matches but details differ');
  it.todo('defaults new items to checked and existing items to disabled');
  it.todo('allows user to edit extracted field values before saving (INTAKE-03 correction)');
});

import { describe, it, expect } from 'vitest';

// import { mergeIntakeExtraction } from '@/features/school-profile/utils/intake-merge';

describe('intake merge (append-only)', () => {
  it.todo('appends new modules without removing existing ones');
  it.todo('appends new contacts without overwriting existing contacts');
  it.todo('appends new actions without modifying existing actions');
  it.todo('preserves existing moduleSetups when merging new extraction');
});

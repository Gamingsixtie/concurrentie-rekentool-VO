import { describe, it, expect } from 'vitest';

// import { recalculateOnApproval } from '@/engine/recalculation';

describe('auto-recalculation on proposal approval', () => {
  it.todo('triggers recalculation for affected school comparisons');
  it.todo('uses new approved price in recalculated results');
  it.todo('does not recalculate for rejected proposals');
  it.todo('updates all active comparisons that reference the changed module+provider');
});
